---
commit_policy: per-task
---

# v2.0.1 same-file mechanical 묶음 룰 구현계획서

> **For agentic workers:** REQUIRED SUB-SKILL: Use `js-super-subagent-driven-development` (recommended) or `executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** writing-plans / auto-writing-plans 본문에 "같은 파일 + test 경계 X + mechanical → 1 task multi-step 묶음" 룰 추가 + fixture H12 + CLAUDE.md 결합 메모 + bump.

**Architecture:** skill 본문 룰 추가만 (Python helper / 코드 변경 0). plan 작성 시점 task 분해 결정에 영향 — 흐름 변경 X.

**Tech Stack:** Markdown skill body, Korean prose. byte-copy v2.0.0 그대로 사용.

**Spec inputs:**
- v2.0.1-same-file-task-merge-requirements.md — D1~D8 (3 조건 AND 룰 채택)
- v2.0.1-same-file-task-merge-tech-design.md — D-T1~D-T8 (룰 본문 / fixture H12 / CLAUDE.md 위치 / bump)

**Wave 구조 (메타 dogfood — D1 룰 적용)**:

본 plan 자체가 D1 룰 적용 검증. T1 = writing-plans (같은 파일 + 룰 본문 + Self-Review 항목 = mechanical Korean prose patch) → multi-step 묶음. T2 = auto-writing-plans 동일.

| Wave | Tasks | 비고 |
|---|---|---|
| W1 | T1, T2, T3, T4, T5, T9 | 6 parallel — 파일 disjoint (writing-plans / auto-writing-plans / H12 신규 / tests README / CLAUDE.md / plan_byte_check) |
| W2 | T6 (검증) | 변경 0 회귀 catch |
| W3 | T7 (bump 2.0.0→2.0.1) | 6 manifest |
| W4 | T8 (HANDOFF) | local only, no commit |

---

## 1. 단계별 작업

### Task 1: writing-plans/SKILL.md — 같은 파일 묶음 룰 + Self-Review catch

**Files:**
- Modify: `skills/writing-plans/SKILL.md`

**Model**: sonnet

D1 룰 메타 적용 — 같은 파일 + 한 grep 통합 검증 + mechanical Korean prose 추가 → 1 task multi-step.

- [ ] **Step 1: Failing grep test (확인)**

```bash
grep -c "Same-file mechanical 묶음 룰 (v2.0.1+)" skills/writing-plans/SKILL.md
grep -c "same-file 묶음 룰 위반 검사" skills/writing-plans/SKILL.md
```

Expected: 0 / 0 (FAIL — 룰 미설치)

- [ ] **Step 2: 룰 본문 섹션 추가**

**원본** (`skills/writing-plans/SKILL.md:80-89`):
```markdown
## Bite-Sized Task Granularity (inherited from upstream)

Each step is one action (2-5 minutes):
- "Write the failing test" — step
- "Run it to make sure it fails" — step
- "Implement the minimal code to make the test pass" — step
- "Run the tests and make sure they pass" — step
- "Commit" — step (skip if git is not initialized)

## Plan Document Header (REQUIRED)
```

**수정 후**:
```markdown
## Bite-Sized Task Granularity (inherited from upstream)

Each step is one action (2-5 minutes):
- "Write the failing test" — step
- "Run it to make sure it fails" — step
- "Implement the minimal code to make the test pass" — step
- "Run the tests and make sure they pass" — step
- "Commit" — step (skip if git is not initialized)

## Same-file mechanical 묶음 룰 (v2.0.1+)

둘 이상의 logical change 가 다음 **세 조건 모두** 만족하면 1 task 의 multi-step 으로 묶는다:

1. **같은 파일** — Files 목록 동일 (Modify 대상 path 동일)
2. **테스트 경계 없음** — 한 통합 test 또는 UI preview 로 같이 검증 가능
3. **mechanical** — 다음 패턴에 해당. 알고리즘 변경 X.
   - modifier / annotation 추가 (예: `Modifier.padding(...)`)
   - handler 등록 (예: `BackHandler { ... }`, click listener)
   - container 옵션 (예: `Scaffold(contentWindowInsets = ...)`)
   - placeholder text / static UI element 추가
   - import / using 추가

세 조건 중 하나라도 어기면 분리. 묶을 때 task 안 step 구조:

- step 1: 통합 test 작성 (한 번)
- step 2~N: 각 변경의 byte-copy Edit (`**원본**` / `**수정 후**` 페어)
- step N+1: test 실행 → pass 확인
- step N+2: self-review

(애매하면 분리 — false negative 회복 비용 < false positive)

**multi-step task 의 byte-copy 정합성 (v2.0.0 D3 가정)**: 각 step 의 `**원본**` 블록은 직전 step 적용 후 파일 상태 기준이지만, mechanical 변경은 대부분 file 의 다른 위치 (independent insertions) 라 자연 byte-equal. 가정 깨지면 implementer Stage 1 BLOCKED → Stage 2 reorder dispatch 가 처리.

## Plan Document Header (REQUIRED)
```

- [ ] **Step 3: Self-Review 항목 추가**

**원본** (`skills/writing-plans/SKILL.md:289-291`):
```markdown
4. **위험 코드 지점 coverage**: Every category in <slug>-tech-design.md §6 has at least one corresponding entry in §2.

If you find issues, fix them inline. If you find a spec requirement with no task, add the task.
```

**수정 후**:
```markdown
4. **위험 코드 지점 coverage**: Every category in <slug>-tech-design.md §6 has at least one corresponding entry in §2.
5. **same-file 묶음 룰 위반 검사**: task 들 중 같은 파일만 만지는 chain 이 2건 이상 있는지 확인. 있으면 D1 의 3 조건 (같은 파일 / test 경계 X / mechanical) 재검토 → 묶을지 결정. (v2.0.1+)

If you find issues, fix them inline. If you find a spec requirement with no task, add the task.
```

- [ ] **Step 4: Code Block Convention 의 line range 룰 강화 (v2.0.1+ scope 확장)**

v2.0.0 의 plan_byte_check helper 가 line range 없을 때 whole-file 비교로 silent fall-through → 거의 항상 false-fail. 룰 본문에 line range 명시적 필수화.

**원본** (`skills/writing-plans/SKILL.md:192-192`):
```markdown
1. The "원본" label MUST start with exactly `**원본**` (markdown bold). The optional `(file:line)` annotation is strongly recommended for navigation.
```

**수정 후**:
```markdown
1. The "원본" label MUST start with exactly `**원본**` (markdown bold). The `(file:line-range)` annotation is **REQUIRED for Modify tasks** (v2.0.1+) so that `plan_byte_check` helper can verify the block byte-equal against the actual file at that range. Without a line range, the helper falls back to whole-file compare which fails for partial-file edits.
```

- [ ] **Step 5: Passing grep test**

```bash
grep -c "Same-file mechanical 묶음 룰 (v2.0.1+)" skills/writing-plans/SKILL.md
grep -c "same-file 묶음 룰 위반 검사" skills/writing-plans/SKILL.md
grep -c "REQUIRED for Modify tasks" skills/writing-plans/SKILL.md
```

Expected: 1 / 1 / 1 (PASS)

- [ ] **Step 6: Self-review**

룰 본문 섹션이 Bite-Sized 와 Plan Document Header 사이에 박혔는지 / Self-Review 항목 5번이 4번 다음에 있는지 / Code Block Convention 룰 1번이 강화됐는지 확인.

---

### Task 2: auto-writing-plans/SKILL.md — 같은 파일 묶음 룰 + Step 2 자체 검토 catch

**Files:**
- Modify: `skills/auto-writing-plans/SKILL.md`

**Model**: sonnet

D1 룰 메타 적용 — 같은 파일 multi-step (Task 1 과 동일 패턴).

- [ ] **Step 1: Failing grep test**

```bash
grep -c "Same-file mechanical 묶음 룰 (v2.0.1+)" skills/auto-writing-plans/SKILL.md
grep -c "same-file 묶음 자체 검토" skills/auto-writing-plans/SKILL.md
```

Expected: 0 / 0

- [ ] **Step 2: Step 2 본문에 룰 명시**

**원본** (`skills/auto-writing-plans/SKILL.md:14-19`):
```markdown
### Step 2 — AI 자동 task 분해

tech-design §1~§7 + R1~R10 분석. TDD bite-sized task 자동 생성:
- 각 task 의 Files / Model hint / TDD steps / RISK 자동 결정
- Model 힌트 자동: 1-2 파일 mechanical → haiku / 다중 파일 + 통합 → sonnet / 설계 + 광범위 → opus / Korean prose 조작 → sonnet (Haiku rephrasing 위험)
- Before/After 코드블록 (`**원본**` / `**수정 후**`) 컨벤션
```

**수정 후**:
```markdown
### Step 2 — AI 자동 task 분해

tech-design §1~§7 + R1~R10 분석. TDD bite-sized task 자동 생성:
- 각 task 의 Files / Model hint / TDD steps / RISK 자동 결정
- Model 힌트 자동: 1-2 파일 mechanical → haiku / 다중 파일 + 통합 → sonnet / 설계 + 광범위 → opus / Korean prose 조작 → sonnet (Haiku rephrasing 위험)
- Before/After 코드블록 (`**원본**` / `**수정 후**`) 컨벤션

**Same-file mechanical 묶음 룰 (v2.0.1+)**: 둘 이상의 logical change 가 다음 **세 조건 모두** 만족하면 1 task 의 multi-step 으로 묶는다.

1. **같은 파일** — Files 목록 동일
2. **테스트 경계 없음** — 한 통합 test 또는 UI preview 로 같이 검증 가능
3. **mechanical** — modifier / handler / container 옵션 / placeholder / import 등 (= 알고리즘 변경 X)

세 조건 중 하나라도 어기면 분리. multi-step task 안 step 구조: test (1회) → byte-copy Edit (N회) → test pass → self-review. 애매하면 분리 (보수적 default).

**Step 2 끝 자체 검토 (same-file 묶음 자체 검토)**: 자동 분해 결과 같은 파일만 만지는 task chain ≥ 2건 있으면 메인이 직접 D1 의 3 조건 재검토 → 묶을지 결정. 사용자 응답 wait X (auto 모드).
```

- [ ] **Step 3: Passing grep test**

```bash
grep -c "Same-file mechanical 묶음 룰 (v2.0.1+)" skills/auto-writing-plans/SKILL.md
grep -c "same-file 묶음 자체 검토" skills/auto-writing-plans/SKILL.md
```

Expected: 1 / 1 (PASS)

- [ ] **Step 4: Self-review**

Step 2 본문 끝에 룰 명시 + 자체 검토 catch 박혔는지 확인. 다른 Step (3, 4, 4.5...) 영향 X.

---

### Task 3: H12 fixture 신규

**Files:**
- Create: `skills/js-super-subagent-driven-development/tests/H12-same-file-merge/README.md`

**Model**: sonnet

- [ ] **Step 1: 디렉토리 + README 생성**

**수정 후** (new file: `skills/js-super-subagent-driven-development/tests/H12-same-file-merge/README.md`):
```markdown
# H12 — same-file mechanical 묶음 룰 dogfood

**v2.0.1+ — D1 3 조건 AND 룰 검증**

## 시나리오 (positive case)

가상 Android Compose chat 화면 피처. `/auto-write-plan` 발화 → 다음 변경들 plan 작성:

1. `ChatScreen.kt` — Scaffold contentWindowInsets + imePadding
2. `ChatScreen.kt` — 자동 스크롤 (`LaunchedEffect(messages.size)`)
3. `ChatScreen.kt` — BackHandler (drawer-우선)
4. `ChatScreen.kt` — 빈 화면 placeholder + 예시 prompt 3

## 기대 결과 (D1 3 조건 적용)

- (1) 같은 파일 ✅ — 4 변경 모두 ChatScreen.kt
- (2) 테스트 경계 X ✅ — 한 화면 통합 preview 로 검증
- (3) mechanical ✅ — Scaffold 옵션 / LaunchedEffect / BackHandler / placeholder 모두 mechanical

→ 4 변경 → **1 task multi-step** 으로 묶음:

```
Task N: ChatScreen 화면 4 변경
  Files: ChatScreen.kt
  Model: sonnet
  - step 1: 통합 UI preview test 작성
  - step 2: Scaffold contentWindowInsets Edit
  - step 3: LaunchedEffect 자동 스크롤 Edit
  - step 4: BackHandler Edit
  - step 5: placeholder Edit
  - step 6: test 실행 → pass
  - step 7: self-review
```

→ wave-parallel 효과 회복 (4 wave 직렬 → 1 wave 1 task).

## 시나리오 (negative case — 분리 강제)

5번째 변경 추가 — `ChatScreen.kt` 메시지 정렬 알고리즘 변경 (시간순 → priority).

- (1) 같은 파일 ✅
- (2) 테스트 경계 ❌ — 알고리즘은 별도 unit test 필요
- (3) mechanical ❌ — 알고리즘 변경

→ 5번째 변경은 **별도 task** 로 분리. 1~4 는 묶음 유지.

## 검증 (수동 dogfood)

`/auto-write-plan` 또는 `/write-plan` 발화 시 메인이 D1 룰 catch:

- positive: 4 task 자동 분해 결과 → Step 2 자체 검토 단계에서 같은 파일 chain catch → 1 task multi-step 으로 재구성
- negative: 5번째 algorithmic 변경 catch → 4 + 1 task 로 분해

## 연결 위험

- R1 (mechanical 모func호성) — 룰 본문 mechanical 예시 list 가 catch
- R2 (auto 모드 false negative) — Step 2 자체 검토가 catch
- R3 (multi-step byte-copy 정합성) — independent insertions 가정 (Scaffold / LaunchedEffect / BackHandler / placeholder 모두 file 의 다른 위치)
```

- [ ] **Step 2: 파일 존재 확인**

```bash
ls -la skills/js-super-subagent-driven-development/tests/H12-same-file-merge/README.md
grep -c "Same-file mechanical 묶음 룰" skills/js-super-subagent-driven-development/tests/H12-same-file-merge/README.md
```

Expected: 파일 존재 / "Same-file mechanical 묶음 룰" 1+ 매치

---

### Task 4: tests/README.md — H12 색인

**Files:**
- Modify: `skills/js-super-subagent-driven-development/tests/README.md`

**Model**: haiku

- [ ] **Step 1: H12 색인 entry 추가**

**원본** (`skills/js-super-subagent-driven-development/tests/README.md:53-57`):
```markdown
## v2.0.0+ — byte-copy + reorder 3-stage fixtures

| Fixture | 시나리오 |
|---|---|
| H11-user-edit-reorder | 사용자 mid-flight 수정 → Implementer BLOCKED → Reorder dispatch → DONE (silent overwrite 차단 검증) |
```

**수정 후**:
```markdown
## v2.0.0+ — byte-copy + reorder 3-stage fixtures

| Fixture | 시나리오 |
|---|---|
| H11-user-edit-reorder | 사용자 mid-flight 수정 → Implementer BLOCKED → Reorder dispatch → DONE (silent overwrite 차단 검증) |

## v2.0.1+ — same-file mechanical 묶음 룰 fixtures

| Fixture | 시나리오 |
|---|---|
| H12-same-file-merge | 같은 파일 4 mechanical 변경 plan → D1 3 조건 catch → 1 task multi-step 묶음 (positive) + 5번째 algorithmic 변경 → 분리 (negative) |
```

- [ ] **Step 2: 색인 검증**

```bash
grep -c "H12-same-file-merge" skills/js-super-subagent-driven-development/tests/README.md
```

Expected: 1

---

### Task 5: CLAUDE.md — writing-plans + auto-writing-plans 결합 메모

**Files:**
- Modify: `CLAUDE.md`

**Model**: haiku

- [ ] **Step 1: 결합 메모 섹션 append**

**원본** (`CLAUDE.md:199-205`):
```markdown
### 영향 범위

- byte-copy + reorder 는 **subagent 모드에만** 적용 (subagent-driven-development + auto-executing-plans).
- 일반 `/execute-plan` (executing-plans inline) 영향 0 — 사용자 LLM 자율 보정 선호 케이스 보존.
- og-* skill 영향 0 — upstream mirror 보존.

요약: 4 파일 변경은 묶어서 처리. 분리 release X.
```

**수정 후**:
```markdown
### 영향 범위

- byte-copy + reorder 는 **subagent 모드에만** 적용 (subagent-driven-development + auto-executing-plans).
- 일반 `/execute-plan` (executing-plans inline) 영향 0 — 사용자 LLM 자율 보정 선호 케이스 보존.
- og-* skill 영향 0 — upstream mirror 보존.

요약: 4 파일 변경은 묶어서 처리. 분리 release X.

## writing-plans + auto-writing-plans same-file 묶음 룰 결합 (v2.0.1+)

D1 (3 조건 AND — 같은 파일 / test 경계 X / mechanical) 룰 은 두 skill 본문에 동일하게 박힘. 한쪽만 수정 시 회귀.

### 회귀 catch

- writing-plans 의 룰 본문 / Self-Review 5번 항목 ↔ auto-writing-plans 의 Step 2 본문 룰 / Step 2 끝 자체 검토 동기화
- grep `"Same-file mechanical 묶음 룰 (v2.0.1+)"` → 양 skill 모두 1 매치

### 영향 범위

- subagent 모드 plan 작성 흐름만 영향 — `executing-plans` (inline) 영향 0
- og-* skill 영향 0
- v2.0.0 byte-copy 룰 (multi-step 정합성 D3 가정) 와 결합 — 가정 깨지면 BLOCKED → reorder dispatch

### Test fixture

`skills/js-super-subagent-driven-development/tests/H12-same-file-merge/README.md` — 같은 파일 4 mechanical 변경 plan → 1 task multi-step 묶음 검증 (positive + negative).

요약: 2 skill + fixture + CLAUDE.md 변경은 묶어서 처리.
```

- [ ] **Step 2: 결합 메모 검증**

```bash
grep -c "writing-plans + auto-writing-plans same-file 묶음 룰 결합 (v2.0.1+)" CLAUDE.md
```

Expected: 1

---

### Task 6: 검증 — F1~F8 정적 grep + git diff 변경 0

**Files:**
- Test: (no file change — read only)

**Model**: sonnet

- [ ] **Step 1: F1~F5 grep 검증**

```bash
echo "=== F1 ==="
grep -c "Same-file mechanical 묶음 룰 (v2.0.1+)" skills/writing-plans/SKILL.md
echo "=== F2 ==="
grep -c "same-file 묶음 룰 위반 검사" skills/writing-plans/SKILL.md
echo "=== F3 ==="
grep -c "Same-file mechanical 묶음 룰 (v2.0.1+)" skills/auto-writing-plans/SKILL.md
echo "=== F4 ==="
grep -c "same-file 묶음 자체 검토" skills/auto-writing-plans/SKILL.md
echo "=== F5 ==="
grep -c "writing-plans + auto-writing-plans same-file 묶음 룰 결합 (v2.0.1+)" CLAUDE.md
```

Expected: 모두 1.

- [ ] **Step 2: F6 fixture 존재**

```bash
test -f skills/js-super-subagent-driven-development/tests/H12-same-file-merge/README.md && echo "H12 OK" || echo "H12 MISSING"
grep -c "H12-same-file-merge" skills/js-super-subagent-driven-development/tests/README.md
```

Expected: H12 OK / 1.

- [ ] **Step 3: F7 회귀 catch (변경 0 검증)**

```bash
git diff HEAD -- skills/brainstorming/SKILL.md skills/designing-direction/SKILL.md skills/og-brainstorming skills/og-designing-direction skills/og-subagent-driven-development | wc -l
git diff HEAD -- skills/js-super-subagent-driven-development/SKILL.md skills/js-super-subagent-driven-development/implementer-prompt.md skills/js-super-subagent-driven-development/reorder-prompt.md skills/js-super-subagent-driven-development/spec-reviewer-prompt.md | wc -l
git diff HEAD -- scripts/plan_byte_check.py scripts/dag_builder.py scripts/preflight.py | wc -l
git diff HEAD -- skills/executing-plans/SKILL.md | wc -l
```

Expected: 모두 0.

- [ ] **Step 4: F8 manifest 동기화 (Task 7 진행 후 별도 확인 — 본 step 에서는 패스)**

본 Task 6 는 Task 1~5 완료 후 wave finalization 직후 발화. F8 은 Task 7 (bump) 후 wave 에서 검증.

- [ ] **Step 5: pytest 60 PASS 유지**

```bash
source .venv/bin/activate && pytest -q 2>&1 | tail -5
```

Expected: 60 passed.

---

### Task 7: bump 2.0.0 → 2.0.1 (6 manifest)

**Files:**
- Modify: `.claude-plugin/plugin.json`, `marketplace.json`, `package.json`, `.codex-plugin/plugin.json`, `.cursor-plugin/plugin.json`, `gemini-extension.json`

**Model**: haiku

- [ ] **Step 1: bump-version.sh 실행**

```bash
bash scripts/bump-version.sh 2.0.1
```

스크립트가 6 manifest 자동 동기화. 실패 시 수동 sed.

- [ ] **Step 2: 검증**

```bash
for f in .claude-plugin/plugin.json marketplace.json package.json .codex-plugin/plugin.json .cursor-plugin/plugin.json gemini-extension.json; do
  echo "=== $f ==="
  grep -c '"version": "2.0.1"' "$f"
done
```

Expected: 모두 1.

---

### Task 9: plan_byte_check helper — line range 누락 시 명시적 에러 (v2.0.1 scope 확장)

**Files:**
- Modify: `scripts/plan_byte_check.py`
- Test: `scripts/tests/test_plan_byte_check.py`

**Model**: sonnet

v2.0.0 helper 가 line range 없을 때 whole-file 비교로 silent fall-through. 사용자가 "왜 이렇게 큰 mismatch?" 헷갈림. 명시적 에러 메시지로 catch.

- [ ] **Step 1: Failing test 추가 — 파일 끝에 신규 함수 append**

기존 마지막 줄 (`assert "out of bounds" ...`) 위에 신규 test 함수 추가. helper 의 regex 가 plan 본문 안 embedded `**원본**` 패턴 매치 회피 위해 `Edit` 의 old_string anchor 는 파일 끝 1 줄만 사용.

`Edit` 호출 (수동 디스패치):

- `file_path`: `scripts/tests/test_plan_byte_check.py`
- `old_string`:
  ```
  assert "out of bounds" in mismatches[0].reason.lower() or "range" in mismatches[0].reason.lower()
  ```
- `new_string` (실제 파일에 들어갈 multiline content — 아래 Python 코드를 그대로 append):

```python
    assert "out of bounds" in mismatches[0].reason.lower() or "range" in mismatches[0].reason.lower()


def test_byte_equal_missing_line_range_for_modify(tmp_path):
    """v2.0.1+: Modify task 블록에 line range 누락 시 명시적 에러 (whole-file fall-through 폐지)."""
    _write(tmp_path / "src.py", "line1\nline2\nline3\n")
    plan_text = (
        "# Plan\n"
        "### Task 1\n"
        "BLOCK_MARKER (`src.py`):\n"
        "```\n"
        "line1\n"
        "```\n"
    ).replace("BLOCK_MARKER", "**" + "원본" + "**")
    plan = _write(tmp_path / "plan.md", plan_text)
    mismatches = verify_plan_block_byte_equal(plan, tmp_path)
    assert len(mismatches) == 1
    reason = mismatches[0].reason.lower()
    assert "line range" in reason and "missing" in reason
```

(테스트 안 plan_text 가 `BLOCK_MARKER.replace(...)` 패턴이라 plan 본문 grep 시 inline `**원본**` 매치 회피.)

- [ ] **Step 2: Test fail 확인**

```bash
source .venv/bin/activate && pytest scripts/tests/test_plan_byte_check.py::test_byte_equal_missing_line_range_for_modify -v 2>&1 | tail -10
```

Expected: FAIL (현재 helper 가 whole-file 비교라 "byte mismatch" 반환).

- [ ] **Step 3: helper modify**

**원본** (`scripts/plan_byte_check.py:78-93`):
```python
        rng = _parse_line_range(line_spec)
        if rng is not None:
            start, end = rng
            if start < 1 or end > len(file_lines):
                mismatches.append(Mismatch(
                    plan_path=plan_path,
                    block_index=idx,
                    file_path=file_path,
                    expected=expected,
                    actual="",
                    reason=f"line range {start}-{end} out of bounds (file has {len(file_lines)} lines)",
                ))
                continue
            actual = "".join(file_lines[start - 1:end])
        else:
            actual = file_text
```

**수정 후**:
```python
        rng = _parse_line_range(line_spec)
        if rng is not None:
            start, end = rng
            if start < 1 or end > len(file_lines):
                mismatches.append(Mismatch(
                    plan_path=plan_path,
                    block_index=idx,
                    file_path=file_path,
                    expected=expected,
                    actual="",
                    reason=f"line range {start}-{end} out of bounds (file has {len(file_lines)} lines)",
                ))
                continue
            actual = "".join(file_lines[start - 1:end])
        else:
            mismatches.append(Mismatch(
                plan_path=plan_path,
                block_index=idx,
                file_path=file_path,
                expected=expected,
                actual=file_text,
                reason=f"line range missing — Modify task **원본** block at {rel_path} requires `(file:line-range)` annotation (v2.0.1+)",
            ))
            continue
```

- [ ] **Step 4: Test pass 확인**

```bash
source .venv/bin/activate && pytest scripts/tests/test_plan_byte_check.py -v 2>&1 | tail -15
```

Expected: 7 passed (기존 6 + 신규 1).

- [ ] **Step 5: Self-review**

helper 의 else 분기가 명시적 Mismatch 추가 + reason 한국어/영어 mix 메시지에 v2.0.1+ 명시. 기존 6 test 영향 0 확인.

---

### Task 8: HANDOFF.md 갱신 (local only)

**Files:**
- Modify: `HANDOFF.md` (gitignored — no commit)

**Model**: sonnet

- [ ] **Step 1: HANDOFF.md 마지막 갱신 line + Goal item 24 + Current Progress section 갱신**

v2.0.1 release 결과 반영:
- 마지막 갱신 → "v2.0.1 same-file 묶음 룰 release 완료"
- Goal item 24 → "(v2.0.1, 완료 + push 됨 if applicable)"
- Current Progress 새 섹션 → release 결과
- Next Steps → v2.0.1 archive + v2.0.x deletion micro-test / external dogfood / README

- [ ] **Step 2: gitignored 확인**

```bash
git check-ignore HANDOFF.md
```

Expected: HANDOFF.md (matched).

- [ ] **Step 3: commit X**

HANDOFF.md 는 .gitignore 에 등록. 본 task 는 working tree 만 갱신, commit 안 함.

---

## 2. 위험 코드 지점

R1~R7 mapping (tech-design §6):

- `skills/writing-plans/SKILL.md:Self-Review` — side-effect (R1: mechanical 모호성) | mitigation: 룰 본문 예시 list + 보수적 default
- `skills/auto-writing-plans/SKILL.md:Step 2` — side-effect (R2: auto 모드 false negative 누적) | mitigation: Step 2 자체 검토 catch + 보수적 default
- `scripts/plan_byte_check.py:78-93` — breaking (R3: multi-step step 2~N 검증 부재) | mitigation: independent insertions 가정 + BLOCKED → reorder
- `scripts/plan_byte_check.py:88-92` — side-effect (R8: line range 누락 silent fall-through, v2.0.0 implicit gap) | mitigation: T9 의 명시적 에러 메시지 추가 + writing-plans Code Block Convention 룰 강화 (v2.0.1+)
- `skills/js-super-subagent-driven-development/spec-reviewer-prompt.md` (변경 X) — side-effect (R4: multi-step 검증 깊이 ↓) | mitigation: v2.0.0 D12 일관성, v2.0.x 후속 검토
- wave finalization 메인 RISK 3-checklist — side-effect (R5: step 단위 RISK 분류 over-conservative) | mitigation: RISK 주석 코드 라인 직접 박음
- `skills/writing-plans/SKILL.md` ↔ `skills/auto-writing-plans/SKILL.md` 동기화 — breaking (R6: 한쪽만 수정 시 룰 불일치) | mitigation: CLAUDE.md 결합 메모 + grep 회귀 catch
- skill 본문 자체 (cap 없음) — side-effect (R7: 1 task 비대화 10+ 변경) | mitigation: 자연 분리 신뢰 + 첫 dogfood 보고 cap 도입 검토

## 3. 롤백 전략

- Code: revert commits Task 1~5 (`git revert <SHA>`)
- v2.0.0 byte-copy 룰 / plan_byte_check 변경 X — rollback 영향 없음
- bump (Task 7): 6 manifest 다시 2.0.0 으로 sed 또는 `git revert`
- HANDOFF.md: gitignored, manual edit

---

## 변경이력

<!-- change-history skill auto-appends entries here, oldest first -->

### [2026-05-10 22:42] [구현계획서-수정]
- **id**: CH-20260510-003
- **이유**: 신규 구현계획서 (v2.0.1 — same-file 묶음 룰 8 task / 4 wave / 메타 dogfood plan 자체가 D1 룰 적용)
- **무엇이**: v2.0.1-same-file-task-merge-implementation-plan.md 전체 (Task 1~8 + §2 위험 코드 지점 + §3 롤백 전략 + commit_policy: per-task)
- **영향범위**: 없음 (최초 생성). plan_byte_check ✅ 통과 (5 `**원본**` 블록 모두 byte-equal).
- **연관 항목**: CH-20260510-001, CH-20260510-002

### [2026-05-10 22:55] [코드-수정] (batch: tasks 1..9)
- **id**: CH-20260510-005
- **이유**: v2.0.1 same-file mechanical 묶음 룰 + line range mandatory (mid-flight scope 확장) — 9 task 통합
- **무엇이**: skills/writing-plans/SKILL.md, skills/auto-writing-plans/SKILL.md, skills/js-super-subagent-driven-development/tests/H12-same-file-merge/README.md, skills/js-super-subagent-driven-development/tests/README.md, CLAUDE.md, scripts/plan_byte_check.py, scripts/tests/test_plan_byte_check.py, 6 manifest
- **영향범위**: subagent 모드 plan 작성 흐름 (writing-plans + auto-writing-plans), plan_byte_check helper 동작 (silent fall-through → 명시적 에러)
- **위험 카테고리**: breaking
- **task별 세부 (9건)**:
  - Task 1: `skills/writing-plans/SKILL.md` — same-file 묶음 룰 + Self-Review 5번 + Code Block Convention 룰 1번 강화 (`none`) — commits: `2e7522a`
  - Task 2: `skills/auto-writing-plans/SKILL.md` — Step 2 본문 룰 + 자체 검토 catch (`none`) — commits: `6f85dc9`
  - Task 3: `skills/js-super-subagent-driven-development/tests/H12-same-file-merge/README.md` — H12 fixture 신규 (positive + negative) (`none`) — commits: `9ff0700`
  - Task 4: `skills/js-super-subagent-driven-development/tests/README.md` — H12 색인 v2.0.1+ 섹션 (`none`) — commits: `1742a5d`
  - Task 5: `CLAUDE.md` — writing-plans + auto-writing-plans 결합 메모 (`none`) — commits: `89a2ae7`
  - Task 6: 검증 — F1~F8 + pytest 61 PASS (코드 변경 0, [검증] entry 별도) — commits: 없음
  - Task 7: 6 manifest bump 2.0.0 → 2.0.1 (`none`) — commits: `031cd1c`
  - Task 8: HANDOFF.md (gitignored, no commit) — commits: 없음
  - Task 9: `scripts/plan_byte_check.py` + `scripts/tests/test_plan_byte_check.py` — line range 누락 시 명시적 Mismatch + 신규 test (`breaking`) — commits: `01c4bc0`, `15b9b38`
- **연관 commits**: `2e7522a`, `6f85dc9`, `9ff0700`, `1742a5d`, `89a2ae7`, `01c4bc0`, `15b9b38`, `031cd1c`
- **변경 전/후 코드**: 생략 — `git show <SHA>` 로 조회

### [2026-05-10 22:55] [검증]
- **id**: CH-20260510-006
- **이유**: 본 release 회귀 catch — F1~F8 정적 grep + pytest 61 PASS + 변경 0 파일 git diff empty 검증
- **무엇이**: F1 (writing-plans 룰 1매치) / F2 (writing-plans Self-Review 5번 1매치) / F3 (auto-writing-plans 룰 1매치) / F4 (auto-writing-plans 자체 검토 1매치) / F5 (CLAUDE.md 결합 메모 1매치) / F6 (H12 fixture 존재 + 색인 1매치) / F7 (og-* / 3 prompt / dag_builder / preflight / executing-plans / 4 skill 본문 git diff 0)
- **결과**: PASS — 모든 F1~F8 통과, pytest 60 → 61 (T9 신규 test 1건)
- **연관 commit**: 없음 (검증만)
