# 개발방향: v2.0.1 same-file mechanical task — plan 작성 시 1 task 묶음 룰

> **For agentic workers:** This document is the technical spec (architecture, components, decisions, risks, test strategy). Anchored to `<slug>-requirements.md` (PRD), consumed by `<slug>-implementation-plan.md`. NEXT STEP: invoke `writing-plans` (or `/write-plan`) to produce `<slug>-implementation-plan.md`.

## 1. 아키텍처 개요

```
[plan 작성 흐름 — writing-plans / auto-writing-plans]
                │
                ▼
   ┌────────────────────────────────┐
   │  Task 분해 단계                │
   │   - 기존: 1 logical change      │
   │     = 1 task                    │
   │   - v2.0.1+: D1 룰 적용         │
   │     (3 조건 AND → 1 task multi-step) │
   └────────────────┬───────────────┘
                    ▼
   ┌────────────────────────────────┐
   │  Self-Review 단계 (catch)      │
   │   - same-file chain ≥ 2 검사    │
   │   - 위반 시 D1 재검토           │
   └────────────────┬───────────────┘
                    ▼
   ┌────────────────────────────────┐
   │  plan_byte_check (v2.0.0)      │
   │   - 첫 step 만 검증 (D3 가정)   │
   │   - 변경 X (본 release)        │
   └────────────────┬───────────────┘
                    ▼
   [실행 단계 — js-super-subagent-driven-development]
                    │
                    ▼
   같은 파일 4 변경 = 1 task multi-step
   → 1 wave, 1 commit (wave-parallel 효과 회복)
```

본 release 는 **skill 본문 룰 추가만** — Python helper / 코드 변경 0. 흐름 자체 변경 0, plan 작성 시점 task 분해 결정만 영향.

## 2. 영향 받는 컴포넌트/파일

| 파일 | 변경 타입 | 변경 요약 |
|---|---|---|
| `skills/writing-plans/SKILL.md` | Modify | "Same-file mechanical 묶음 룰 (v2.0.1+)" 섹션 추가 + Self-Review 항목 |
| `skills/auto-writing-plans/SKILL.md` | Modify | Step 2 본문에 동일 룰 명시 + Step 2 자체 검토에 catch 추가 |
| `skills/js-super-subagent-driven-development/tests/H12-same-file-merge/README.md` | Create | 메타 dogfood fixture (입력 plan + 기대 묶음 결과) |
| `skills/js-super-subagent-driven-development/tests/README.md` | Modify | H12 색인 (v2.0.1+ 섹션) |
| `CLAUDE.md` | Modify | "writing-plans + auto-writing-plans 묶음 룰 결합 (v2.0.1+)" 섹션 추가 |
| 6 manifest (plugin.json / package.json / marketplace.json / .codex / .cursor / gemini) | Modify | 2.0.0 → 2.0.1 bump |

**변경 0 (회귀 catch)**:

- `skills/js-super-subagent-driven-development/SKILL.md` — Per-wave Sequence / W-2 분기 그대로
- `skills/js-super-subagent-driven-development/implementer-prompt.md` / `reorder-prompt.md` / `spec-reviewer-prompt.md` — 변경 X
- `scripts/plan_byte_check.py` / `dag_builder.py` / `preflight.py` — 변경 X
- og-* 3 skill — 변경 X (upstream mirror 보존)
- 일반 `executing-plans` (inline) — 변경 X (subagent 모드만 영향)

## 3. 데이터 모델/스키마 변경 — N/A: 본 피처는 DB/스키마 무관 (skill 본문 + fixture + manifest 변경만)

## 4. 외부 인터페이스 — N/A: API/event 노출 없음

## 5. 핵심 결정 + 대안 비교 (D-T1 ~ D-T8)

### D-T1. 룰 본문 정확한 표현 — backlog 3 조건 AND 그대로

writing-plans / auto-writing-plans 양쪽에 박을 룰 본문:

```markdown
## Same-file mechanical 묶음 룰 (v2.0.1+)

둘 이상의 logical change 가 다음 **세 조건 모두** 만족하면 1 task 의 multi-step 으로 묶는다:

1. **같은 파일** — Files 목록 동일 (Modify 대상 path 동일)
2. **테스트 경계 없음** — 한 통합 test 또는 UI preview 로 같이 검증 가능
3. **mechanical** — 다음 패턴에 해당. 알고리즘 변경 X.
   - modifier / annotation 추가 (예: `Modifier.padding(...)`)
   - handler 등록 (예: `BackHandler { ... }`, click listener)
   - container 옵션 (예: `Scaffold(contentWindowInsets = ...)`)
   - placeholder text / static UI element 추가
   - import / using 추가

세 조건 중 하나라도 어기면 분리. 묶을 때 task 안 step 구조:

- step 1: 통합 test 작성 (한 번)
- step 2~N: 각 변경의 byte-copy Edit (`**원본**` / `**수정 후**` 페어)
- step N+1: test 실행 → pass 확인
- step N+2: self-review

(애매하면 분리 — false negative 회복 비용 < false positive)
```

**대안**:

- (A) "1 task = 1 file" 단일 룰 — TDD cycle 깨짐 위험. **거부**.
- (B) "(1)+(2) 만 (mechanical drop)" — algorithmic 변경 묶임 위험. **거부**.
- (C) **[채택]** 3 조건 AND — 안전판 3중. 보수적 default. 회귀 0.

reasoning: 우려 1 (mechanical 모호성) 은 룰 본문 mechanical 예시 list 명시로 대부분 해결. 사용자 catch override 가능.

### D-T2. cap 룰 (1 task 최대 N step) — 본 release 명시 X, 자연 분리 신뢰

backlog 우려 2 — 같은 파일 10건 묶이면 task 비대화. cap 명시 가능 (예: "최대 5건").

**대안**:

- (A) cap 5 명시 — deterministic, 단 마법 숫자.
- (B) cap 명시 X, 자연 분리 신뢰 — 5+ 건 mechanical 만으로 끝나는 경우 드뭄 (구조 변경 동반 → mechanical 룰 위반 → 분리). **[채택]**

reasoning: 첫 dogfood 결과 보고 v2.0.x micro-patch 로 추가 가능. 보수적 default 유지.

### D-T3. multi-step task 의 byte-copy 정합성 — 첫 step 만 검증 (defer 가정)

requirements §3 우려 3 / D3 그대로.

**대안**:

- (A) plan_byte_check 시뮬레이션 확장 — step N-1 적용 후 가상 파일 만들고 step N `**원본**` 검증. 안전성 ↑, 단 helper 복잡 + 새 unit test 4-5 건. 6 → 7 파일 변경. **거부 (defer)**.
- (B) **[채택]** 첫 step 만 검증 + independent insertions 가정 + BLOCKED → reorder 안전망. plan_byte_check 변경 X. 5 파일 + 6 manifest.

reasoning: mechanical 변경 (modifier / handler / Scaffold 옵션 등) 은 대부분 file 의 다른 위치 → independent insertions → 자연 byte-equal. 가정 깨지면 v2.0.0 의 BLOCKED → reorder dispatch 가 처리. 첫 dogfood 결과 보고 시뮬레이션 확장 검토.

### D-T4. spec-reviewer prompt patch — multi-step 검증 가이드 추가

multi-step task 가 들어오면 spec-reviewer 가 step 별로 각각 검증해야. 현재 prompt 는 task 단위 검증 가이드만.

**대안**:

- (A) `skills/js-super-subagent-driven-development/spec-reviewer-prompt.md` 본문 patch — "multi-step task 인 경우 step 별 plan ↔ 코드 line-by-line 검증" 한 단락 추가. 1 파일 추가.
- (B) **[채택]** 본 release 미포함 — spec-reviewer 의 기본 line-by-line 검증이 자연스럽게 step 들 포괄 (한 task 안 변경이라 cross-reference). v2.0.x 후속 검토.

reasoning: spec-reviewer 변경 0 = v2.0.0 D12 (변경 0) 일관성 유지. multi-step task 가 첫 dogfood 에서 spec-reviewer review 빈도 낮음 / 깊이 부족 catch 시 후속 patch.

### D-T5. fixture H12 시나리오 명세

`skills/js-super-subagent-driven-development/tests/H12-same-file-merge/README.md` 본문:

- **시나리오**: ChatScreen.kt 4 변경 (Scaffold / LaunchedEffect / BackHandler / placeholder) plan 작성 요청
- **기대**: writing-plans / auto-writing-plans 가 D1 3 조건 검사 → 모두 만족 → 1 task multi-step 으로 묶음
- **검증**: plan 본문에 task 1건 (파일 ChatScreen.kt) + step 5개 (test / 4 Edit / pass / self-review) 구조
- **negative case**: 5번째 변경이 algorithmic (정렬 알고리즘) 추가 → mechanical 조건 위반 → 분리 (4 mechanical task multi-step + 1 algorithmic task)
- **정적 검증**: fixture README 의 "기대 plan 구조" 와 실제 사용 시 작성된 plan grep 비교 (수동 검증 fixture, 자동 unit test X)

기존 H1~H11 fixture 패턴 따름.

### D-T6. CLAUDE.md 결합 메모 위치

기존 `## implementer-prompt + reorder-prompt + plan_byte_check 결합 (v2.0.0+)` 섹션 다음에 신규 섹션 추가:

```markdown
## writing-plans + auto-writing-plans same-file 묶음 룰 결합 (v2.0.1+)

D1 룰 (3 조건 AND) 은 두 skill 본문에 동일하게 박힘. 한쪽만 수정 시 회귀.

회귀 catch:
- writing-plans Self-Review 의 mechanical 예시 list ↔ auto-writing-plans Step 2 의 동일 list 동기화
- grep "Same-file mechanical 묶음 룰" → 양 skill 모두 1 매치

영향 범위:
- subagent 모드만 — `executing-plans` (inline) 영향 0
- og-* skill 영향 0
- v2.0.0 byte-copy 룰 (multi-step 정합성 D3 가정) 와 결합 — 가정 깨지면 BLOCKED → reorder
```

### D-T7. 본 release plan 자체가 메타 dogfood

v2.0.1 implementation-plan.md 본문이 D1 룰 적용한 형태로 작성:

- writing-plans + auto-writing-plans 두 skill 본문 patch — file 분리 (다른 path) → 분리된 task
- writing-plans 본문 안 룰 본문 추가 + Self-Review catch 추가 → mechanical 한 추가지만 같은 파일 → **묶음 가능**
- auto-writing-plans 도 동일

→ 본 release plan 작성 단계에서 룰 적용 검증 (메타 dogfood). 사용자가 plan 검토 단계에서 "묶음 잘 됐나" 확인 가능.

### D-T9. byte-copy line range 필수화 (mid-flight scope 확장, requirements D9)

writing-plans Code Block Convention 룰 1번 (`SKILL.md:192`) 강화 + plan_byte_check helper 의 line range 누락 분기 명시 에러.

**대안**:

- (A) skill 룰만 강화 — helper 변경 X. silent fall-through 그대로 단 plan 작성자가 룰 의식. **거부** (catch 신뢰 약함).
- (B) helper 만 수정 — skill 룰 그대로. plan 작성자가 옛 plan 패턴 재사용 시 mismatch 의도 안 보임. **거부** (룰 일관성 약함).
- (C) **[채택]** skill 룰 + helper 둘 다 강화. helper else 분기 명시 Mismatch + skill 본문 "REQUIRED for Modify tasks (v2.0.1+)" 명시. 새 unit test 1건.

reasoning: v2.0.1 plan 작성 mid-flight catch. 같은 주제 진행 중이라 묶음 자연. 별도 backlog 만들면 v2.0.0 의 implicit gap 이 다음 release 까지 노출.

### D-T8. version bump 결정 — minor (2.0.0 → 2.0.1)

skill 본문 룰 추가 = backward compat 100% (기존 plan 영향 0). breaking change X. minor patch.

**대안**:

- patch (2.0.0 → 2.0.0.1) — js-super 는 X.Y.Z 만 사용. patch 없음.
- minor (2.0.0 → 2.0.1) — **[채택]**.
- minor 큰 (2.0.0 → 2.1.0) — 새 기능 단위 큰 변경. 본 release 룰 추가만이라 과한 bump.

reasoning: 룰 추가는 minor 적합. v2.1+ 는 Python 자동 merge helper 도입 시 사용.

## 6. 위험/사이드이펙트 (R1~R8)

| ID | 카테고리 | 위치 | 설명 | mitigation |
|---|---|---|---|---|
| R1 | side-effect | writing-plans `**Model**:` 필드 평가 시점 | mechanical 판정 모호 → 메인이 보수적 분리 (false negative) | 룰 본문 mechanical 예시 list 명시 + 사용자 catch override 가능 |
| R2 | side-effect | auto-writing-plans Step 2 자동 흐름 | auto 모드는 사용자 catch 부족 → false negative 누적 | 메인 자체 검토 (Step 2 끝) + 보수적 default = false negative 가 false positive 보다 회복 쉬움 |
| R3 | breaking | plan_byte_check (v2.0.0 그대로) | multi-step task 의 step 2~N `**원본**` 검증 부재 | independent insertions 가정 (D3) + BLOCKED → reorder 안전망 |
| R4 | side-effect | spec-reviewer (변경 X) | multi-step task 한 번에 N 변경 검증 → 깊이 ↓ 가능 | v2.0.0 D12 (spec-reviewer 변경 X) 일관성. 후속 patch 후보 (D-T4 (B)) |
| R5 | side-effect | wave finalization RISK 분류 | multi-step 한 step 만 RISK 트리거 시 task 단위 over-conservative | RISK 주석 코드 라인 직접 박음 (정확). 메인 3-checklist step 별 분리 가능 |
| R6 | breaking | writing-plans / auto-writing-plans 두 skill 동기화 | 한쪽만 수정 시 룰 불일치 | CLAUDE.md 결합 메모 (D-T6) + grep 회귀 catch |
| R7 | side-effect | task 비대화 (cap 없음) | 같은 파일 10+ 변경 묶임 | 자연 분리 신뢰 (D-T2 reasoning) + 첫 dogfood 보고 cap 도입 검토 |
| R8 | side-effect | `scripts/plan_byte_check.py:88-92` (else 분기) | v2.0.0 helper line range 누락 시 whole-file 비교 silent fall-through → false-fail 헷갈림 | T9: helper else 분기 명시 Mismatch + writing-plans Code Block Convention 룰 강화 (D-T9) |

## 7. 테스트 전략

### F1~F5 정적 grep 검증 (skill 본문 룰 박힘 확인)

- F1: `skills/writing-plans/SKILL.md` — "Same-file mechanical 묶음 룰" 섹션 1 매치
- F2: `skills/writing-plans/SKILL.md` — Self-Review 항목에 "same-file chain" 1 매치
- F3: `skills/auto-writing-plans/SKILL.md` — 동일 룰 본문 1 매치
- F4: `skills/auto-writing-plans/SKILL.md` — Step 2 자체 검토 catch 1 매치
- F5: `CLAUDE.md` — "writing-plans + auto-writing-plans same-file 묶음 룰 결합" 1 매치

### F6 fixture H12 존재

- `skills/js-super-subagent-driven-development/tests/H12-same-file-merge/README.md` 존재
- `skills/js-super-subagent-driven-development/tests/README.md` 의 H12 색인 1 매치

### F7 회귀 catch (변경 0 검증)

- og-* 3 skill — git diff empty
- subagent-dd SKILL.md / 3 prompt 파일 — git diff empty
- `scripts/plan_byte_check.py` / `dag_builder.py` / `preflight.py` — git diff empty
- `executing-plans` (inline) SKILL.md — git diff empty

### F8 manifest 동기화 (6 파일)

- 6 manifest 모두 2.0.1

### G — unit test 신규 0건

본 release 는 skill 본문 룰만 추가 — Python 코드 변경 0 → unit test 신규 X. 기존 60 PASS 유지.

### H12 메타 dogfood

본 release plan 자체가 D1 룰 적용 검증. 사용자가 plan 검토 단계에서 같은 파일 task chain catch.

---

## 변경이력

<!-- change-history skill auto-appends entries here, oldest first -->

### [2026-05-10 22:35] [개발방향-수정]
- **id**: CH-20260510-002
- **이유**: 신규 기술 설계 (v2.0.1 — same-file 묶음 룰 skill 본문 patch + fixture H12 + CLAUDE.md 결합 메모)
- **무엇이**: v2.0.1-same-file-task-merge-tech-design.md 전체 (D-T1~D-T8 + R1~R7 + F1~F8 + H12)
- **영향범위**: 없음 (최초 생성)
- **연관 항목**: CH-20260510-001
