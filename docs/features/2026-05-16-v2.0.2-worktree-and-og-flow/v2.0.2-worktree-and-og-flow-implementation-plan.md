---
commit_policy: per-task
---

# v2.0.2 worktree 일반화 + og-flow subagent 누수 fix 구현계획서

> **For agentic workers:** REQUIRED SUB-SKILL: Use `js-super-subagent-driven-development` (rename 후 `js-super-sub-driven`) or `executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v2.0.2 minor release — worktree skill body 의 `.env*` 한정 → "로컬 빌드 환경 파일" LLM-judged 일반화 + js-super-subagent-driven-development → js-super-sub-driven rename + og-writing-plans 핸드오프 강화 (2 concerns 묶음).

**Architecture:** rename 영향 그래프는 14 live 파일 cross-ref + immutable 27 파일 보존. worktree 일반화는 단일 파일 skill body 의 Step 2 / HARD-GATE / Defaults / Procedure / Anti-Patterns / Acceptance multi-step 갱신. 두 변경은 파일 disjoint — wave-parallel dispatch 가능.

**Tech Stack:** Python (preflight/dag_builder helper), Markdown (skill bodies + commands + plans), Bash (scripts).

**Spec inputs:**

- `v2.0.2-worktree-and-og-flow-requirements.md` — 배경 / 핵심 결정 (og-flow A+B / worktree recipe) / 우려·해결 6건
- `v2.0.2-worktree-and-og-flow-tech-design.md` — D-T1~5 / R1~8 / 영향 파일 list (live 14 / immutable 27)

**Task 묶음 룰**:

- D1 same-file 묶음 룰 (v2.0.1+) 은 `**원본**` 블록 (byte-copy precondition) 이 있는 task 에만 strict 적용
- 본 release 의 rename 류 task (T2~T4) 는 `**원본**` 블록 없이 `Edit ... replace_all=true` 단일 호출 → 같은 카테고리 (skill body / commands / scripts) 안 multi-step 묶음 허용
- worktree skill body (T7) + og B handoff (T6) + 신규 fixture (T9) + CLAUDE.md (T5) 는 byte-copy 적용

---

## 1. 단계별 작업

### Task 1: skill 디렉토리 + 내부 self-ref rename (atomic)

**Files:**

- Modify: `skills/js-super-subagent-driven-development/` → `skills/js-super-sub-driven/` (디렉토리 git mv)
- Modify: `skills/js-super-sub-driven/SKILL.md` (post-mv self-ref)
- Modify: `skills/js-super-sub-driven/implementer-prompt.md`
- Modify: `skills/js-super-sub-driven/spec-reviewer-prompt.md`
- Modify: `skills/js-super-sub-driven/reorder-prompt.md`
- Modify: `skills/js-super-sub-driven/tests/H10-auto-execute-blocked-task/README.md`
- Modify: `skills/js-super-sub-driven/tests/H11-user-edit-reorder/README.md`
- Modify: `skills/js-super-sub-driven/tests/H12-same-file-merge/README.md` (있다면)
- Modify: `skills/js-super-sub-driven/tests/README.md` (있다면)

**Model**: sonnet

- [ ] **Step 1: 검증 test 작성 (grep)**

```bash
# 예상: rename 전 → live 파일에 old name 다수, new path 없음
grep -l "js-super-subagent-driven-development" skills/ commands/ scripts/ CLAUDE.md 2>/dev/null | wc -l
ls skills/js-super-sub-driven/ 2>&1 | head -5
```

Expected: count > 0 + "No such file or directory"

- [ ] **Step 2: git mv directory**

```bash
git mv skills/js-super-subagent-driven-development skills/js-super-sub-driven
```

- [ ] **Step 3: 내부 self-ref 갱신 (각 파일 Edit replace_all=true)**

각 파일에 대해 `old_string=js-super-subagent-driven-development` + `new_string=js-super-sub-driven` + `replace_all=true`:

- `skills/js-super-sub-driven/SKILL.md`
- `skills/js-super-sub-driven/implementer-prompt.md`
- `skills/js-super-sub-driven/spec-reviewer-prompt.md`
- `skills/js-super-sub-driven/reorder-prompt.md`
- `skills/js-super-sub-driven/tests/H10-auto-execute-blocked-task/README.md`
- `skills/js-super-sub-driven/tests/H11-user-edit-reorder/README.md`
- `skills/js-super-sub-driven/tests/H12-same-file-merge/README.md` (Read 로 존재 확인 후)
- `skills/js-super-sub-driven/tests/README.md` (Read 로 존재 확인 후)

- [ ] **Step 4: 검증 grep**

```bash
ls skills/js-super-sub-driven/ | head -5
ls skills/js-super-subagent-driven-development/ 2>&1  # expected: No such file
grep -c "js-super-subagent-driven-development" skills/js-super-sub-driven/*.md skills/js-super-sub-driven/tests/*/README.md 2>/dev/null
```

Expected: new path 파일 list, old path 없음, 내부 self-ref 0

- [ ] **Step 5: Commit**

```bash
git add skills/js-super-sub-driven/
git commit -m "task 1: skills/js-super-subagent-driven-development → js-super-sub-driven rename + internal self-ref (v2.0.2)"
```

---

### Task 2: skill body cross-ref rename (5 파일)

**Files:**

- Modify: `skills/writing-plans/SKILL.md` (5 occurrences)
- Modify: `skills/executing-plans/SKILL.md` (4 occurrences)
- Modify: `skills/auto-executing-plans/SKILL.md` (2 occurrences)
- Modify: `skills/finishing-a-development-branch/SKILL.md` (2 occurrences)
- Modify: `skills/change-history/SKILL.md` (1 occurrence)

**Model**: haiku (mechanical text replace)

**Deps**: 없음 (T1 과 file-disjoint — 단순 텍스트 변경, T1 의 git mv 무관)

- [ ] **Step 1: 검증 test (grep before)**

```bash
grep -c "js-super-subagent-driven-development" skills/writing-plans/SKILL.md skills/executing-plans/SKILL.md skills/auto-executing-plans/SKILL.md skills/finishing-a-development-branch/SKILL.md skills/change-history/SKILL.md
```

Expected: 5 / 4 / 2 / 2 / 1 (=14 total)

- [ ] **Step 2: 각 파일 Edit replace_all=true**

다음 5 파일 각각에 대해 `old_string=js-super-subagent-driven-development` + `new_string=js-super-sub-driven` + `replace_all=true`:

- `skills/writing-plans/SKILL.md`
- `skills/executing-plans/SKILL.md`
- `skills/auto-executing-plans/SKILL.md`
- `skills/finishing-a-development-branch/SKILL.md`
- `skills/change-history/SKILL.md`

- [ ] **Step 3: 검증 grep after**

```bash
grep -c "js-super-subagent-driven-development" skills/writing-plans/SKILL.md skills/executing-plans/SKILL.md skills/auto-executing-plans/SKILL.md skills/finishing-a-development-branch/SKILL.md skills/change-history/SKILL.md
```

Expected: 0 / 0 / 0 / 0 / 0

- [ ] **Step 4: Commit**

```bash
git add skills/writing-plans/SKILL.md skills/executing-plans/SKILL.md skills/auto-executing-plans/SKILL.md skills/finishing-a-development-branch/SKILL.md skills/change-history/SKILL.md
git commit -m "task 2: 5 skill body cross-ref rename → js-super-sub-driven (v2.0.2)"
```

---

### Task 3: commands cross-ref rename (4 파일)

**Files:**

- Modify: `commands/auto-execute-plan.md` (1 occurrence)
- Modify: `commands/og-execute-plan.md` (1 occurrence)
- Modify: `commands/og-write-plan.md` (1 occurrence)
- Modify: `commands/execute-plan.md` (2 occurrences)

**Model**: haiku

**Deps**: 없음 (T1, T2 와 file-disjoint)

- [ ] **Step 1: 검증 grep before**

```bash
grep -c "js-super-subagent-driven-development" commands/auto-execute-plan.md commands/og-execute-plan.md commands/og-write-plan.md commands/execute-plan.md
```

Expected: 1 / 1 / 1 / 2 (=5 total)

- [ ] **Step 2: 각 파일 Edit replace_all=true**

다음 4 파일 각각에 대해 `old_string=js-super-subagent-driven-development` + `new_string=js-super-sub-driven` + `replace_all=true`:

- `commands/auto-execute-plan.md`
- `commands/og-execute-plan.md`
- `commands/og-write-plan.md`
- `commands/execute-plan.md`

- [ ] **Step 3: 검증 grep after**

```bash
grep -c "js-super-subagent-driven-development" commands/auto-execute-plan.md commands/og-execute-plan.md commands/og-write-plan.md commands/execute-plan.md
```

Expected: 0 / 0 / 0 / 0

- [ ] **Step 4: Commit**

```bash
git add commands/auto-execute-plan.md commands/og-execute-plan.md commands/og-write-plan.md commands/execute-plan.md
git commit -m "task 3: commands/ cross-ref rename → js-super-sub-driven (v2.0.2)"
```

---

### Task 4: scripts cross-ref rename (3 파일)

**Files:**

- Modify: `scripts/dag_builder.py` (1 occurrence)
- Modify: `scripts/preflight.py` (2 occurrences)
- Modify: `scripts/tests/test_changelog_buffer.py` (1 occurrence)

**Model**: haiku

**Deps**: 없음

- [ ] **Step 1: 검증 grep before**

```bash
grep -c "js-super-subagent-driven-development" scripts/dag_builder.py scripts/preflight.py scripts/tests/test_changelog_buffer.py
```

Expected: 1 / 2 / 1 (=4 total)

- [ ] **Step 2: 각 파일 Edit replace_all=true**

다음 3 파일 각각에 대해 `old_string=js-super-subagent-driven-development` + `new_string=js-super-sub-driven` + `replace_all=true`:

- `scripts/dag_builder.py`
- `scripts/preflight.py`
- `scripts/tests/test_changelog_buffer.py`

- [ ] **Step 3: 검증 grep after + pytest 회귀**

```bash
grep -c "js-super-subagent-driven-development" scripts/dag_builder.py scripts/preflight.py scripts/tests/test_changelog_buffer.py
source .venv/bin/activate && pytest scripts/tests/ -v --tb=short
```

Expected: 0 / 0 / 0 + 모든 pytest PASS

- [ ] **Step 4: Commit**

```bash
git add scripts/dag_builder.py scripts/preflight.py scripts/tests/test_changelog_buffer.py
git commit -m "task 4: scripts/ cross-ref rename → js-super-sub-driven (v2.0.2)"
```

---

### Task 5: CLAUDE.md cross-ref rename + worktree 결합 메모 추가 (same-file 묶음, D1)

**Files:**

- Modify: `CLAUDE.md` (9 cross-ref occurrences + 신규 worktree 결합 메모 섹션 1건)

**Model**: sonnet (Korean prose + 신규 섹션 추가)

**Deps**: 없음

- [ ] **Step 1: 검증 grep before**

```bash
grep -c "js-super-subagent-driven-development" CLAUDE.md
```

Expected: 9

- [ ] **Step 2: cross-ref rename (replace_all)**

`old_string=js-super-subagent-driven-development` + `new_string=js-super-sub-driven` + `replace_all=true` on `CLAUDE.md`.

- [ ] **Step 3: worktree 결합 메모 추가 (신규 섹션 append)**

기존 마지막 결합 메모 섹션 (writing-plans + auto-writing-plans same-file 묶음 룰 결합) 끝에 신규 섹션 append:

**수정 후** (`CLAUDE.md` 마지막 결합 섹션 후 신규 append):

```markdown
## setting-up-worktrees ↔ commands/worktree.md 결합 (v2.0.2+)

v2.0.2+ 에서 `setting-up-worktrees` skill body 의 `.env*` hardcoded glob → "로컬 빌드 환경 파일" LLM-judged Procedure 로 일반화. 즉:

- `skills/setting-up-worktrees/SKILL.md` 의 Step 2 / HARD-GATE / Defaults 표 / Procedure / Anti-Patterns / Acceptance 의 "env 파일" 용어 → "로컬 빌드 환경 파일" 동기
- `commands/worktree.md` 본문 표현 동기

### 회귀 패턴 (한쪽만 변경 시)

| 누락 | 증상 |
|---|---|
| skill body 만 변경 | `/worktree` 슬래시 명령 본문이 옛 표현 유지 → 사용자 혼란 |
| commands 만 변경 | 메인이 skill body 따라 옛 글롭 적용 → 다른 플랫폼 (Android/iOS/desktop) 미커버 |

### 영향 범위

- worktree 생성 흐름만 영향 — `/worktree` 외 다른 skill 무관
- og-* skill 영향 0
- byte-copy 룰 / wave-parallel 영향 0

요약: 2 파일 (skill SKILL.md + commands/worktree.md) + CLAUDE.md 결합 메모 변경은 묶어서 처리.
```

- [ ] **Step 4: 검증 grep after**

```bash
grep -c "js-super-subagent-driven-development" CLAUDE.md
grep -c "setting-up-worktrees ↔ commands/worktree.md" CLAUDE.md
```

Expected: 0 + 1

- [ ] **Step 5: Commit**

```bash
git add CLAUDE.md
git commit -m "task 5: CLAUDE.md cross-ref rename + worktree 결합 메모 (v2.0.2)"
```

---

### Task 6: og-writing-plans 핸드오프 강화 (B)

**Files:**

- Modify: `skills/og-writing-plans/SKILL.md:140-159`

**Model**: sonnet (Korean prose insertion)

**Deps**: 없음

- [ ] **Step 1: 검증 test (현재 표현 catch)**

```bash
grep -n "subagent-driven-development" skills/og-writing-plans/SKILL.md
grep -c "js-super-sub-driven 매치 금지" skills/og-writing-plans/SKILL.md
```

Expected: line 11 + line 153 매치 / 0 (Anti-Pattern 미존재)

- [ ] **Step 2: Edit — Subagent-Driven 라벨 강화 (line 152-154)**

**원본** (`skills/og-writing-plans/SKILL.md:152-154`):

```markdown
**If Subagent-Driven chosen:**
- **REQUIRED SUB-SKILL:** Use superpowers:subagent-driven-development (untouched-upstream, no og- variant needed — js-super did not modify it)
- Fresh subagent per task + two-stage review
```

**수정 후**:

```markdown
**If Subagent-Driven chosen:**
- **REQUIRED SUB-SKILL:** Use **`superpowers:subagent-driven-development`** (upstream-untouched). ⚠️ NOT `js-super-sub-driven` — that's the js-super extended variant with wave-parallel + RISK + 변경이력. og 흐름은 upstream 원본만 사용.
- Fresh subagent per task + two-stage review
```

- [ ] **Step 3: Anti-Pattern 표 1줄 추가 (적절한 위치 — 본문 끝 또는 기존 Anti-Pattern 표 안)**

og-writing-plans 본문 의 적절한 Anti-Pattern 섹션 (있다면) 끝에 1줄 추가. 없다면 신규 ## Anti-Patterns 섹션 추가:

**수정 후** (Anti-Patterns 표에 1 row 추가 또는 신규 섹션):

```markdown
## Anti-Patterns (v2.0.2+ — og-flow subagent path 강화)

| Wrong | Right |
|---|---|
| `js-super-sub-driven` 매치 — js-super 확장 wave-parallel 발화 | `superpowers:subagent-driven-development` upstream 원본만 사용. og 흐름은 단순 fresh subagent + 2-stage review 유지. |
```

- [ ] **Step 4: 검증 grep after**

```bash
grep -c "js-super-sub-driven 매치 금지\|js-super-sub-driven.*js-super 확장" skills/og-writing-plans/SKILL.md
grep -c "\\*\\*\`superpowers:subagent-driven-development\`\\*\\*" skills/og-writing-plans/SKILL.md
```

Expected: ≥1 (Anti-Pattern 1줄) / 1 (bold prefix)

- [ ] **Step 5: Commit**

```bash
git add skills/og-writing-plans/SKILL.md
git commit -m "task 6: og-writing-plans 핸드오프 강화 — superpowers: prefix bold + Anti-Pattern (v2.0.2)"
```

---

### Task 7: setting-up-worktrees skill body — 레시피 형태 LLM-judged 일반화 (D1 same-file 묶음)

**Files:**

- Modify: `skills/setting-up-worktrees/SKILL.md` (multi-step same file)

**Model**: sonnet (Korean prose + multi-section rewrite)

**Deps**: 없음

본 task 는 D1 same-file 묶음 룰 (v2.0.1+) 적용 — 같은 파일 / test 경계 X (skill body 변경 정합성은 한 통합 grep 으로 검증) / mechanical (섹션 변형 + 용어 rename). 5 변경 모두 1 task multi-step.

- [ ] **Step 1: 검증 test (현재 hardcoded .env\* 표현 catch)**

```bash
grep -cE "\\.env\\*|env\\* 파일|env 파일" skills/setting-up-worktrees/SKILL.md
```

Expected: > 0 (다수 — Step 2 / HARD-GATE / Defaults / Procedure / Anti-Patterns / Acceptance)

- [ ] **Step 2: HARD-GATE 본문 갱신 (line 15)**

**원본** (`skills/setting-up-worktrees/SKILL.md:15-15`):

```markdown
NEVER ask the user which env files to copy. ALWAYS auto-glob `.env*` in the project root and copy every match. The user only specifies branch names — env-file selection is automatic. The single exception: if the user EXPLICITLY says "X 파일은 복사하지 마" (exclude pattern), honor that.
```

**수정 후**:

```markdown
NEVER ask the user which 로컬 빌드 환경 파일 to copy. 메인 에이전트가 프로젝트 컨텍스트 (Android / iOS / Node / Flutter / Rust / etc.) 를 읽고 적절한 후보를 판단 + 후보 list 사용자에게 노출 + 복사. The user only specifies branch names — 파일 선택은 LLM-judged. The single exception: if the user EXPLICITLY says "X 파일은 복사하지 마" (exclude pattern), honor that.
```

- [ ] **Step 3: Defaults 표 본문 갱신 (line 32 근방)**

**원본** (`skills/setting-up-worktrees/SKILL.md:32`):

```markdown
| Env files copied | **Auto-glob `.env*` (excludes `.env.example`, `.env.sample`)**. ALL matches copied to every worktree. |
```

**수정 후**:

```markdown
| 로컬 빌드 환경 파일 복사 | **메인 에이전트가 프로젝트 컨텍스트 기반 후보 판단** (Node/Web → `.env*` / Android → `local.properties`, keystore 류 / iOS → `*.xcconfig` 등 / Desktop → `secrets.*`). 후보 list 사용자 노출 후 자동 복사. committed templates / build artifacts 자동 제외. |
```

- [ ] **Step 4: Step 2 본문 — bash glob 폐지 + LLM-judged Procedure 로 교체**

`skills/setting-up-worktrees/SKILL.md:82-97` 의 "**Step 2 — Auto-detect `.env*` files (NO user prompt)**" 섹션 (bash glob 포함) 을 Edit 으로 전체 교체. **원본** 블록은 nested fence 충돌로 인해 plan_byte_check 친화 표현 불가 — 구현 시 Read 로 정확한 범위 catch 후 Edit 호출.

**교체 내용 (수정 후 본문)** — 다음 markdown 블록을 위 line range 위치에 정확히 삽입:

- 새 제목: `**Step 2 — LLM-judged 로컬 빌드 환경 파일 후보 결정 (NO bash glob, NO user prompt)**`
- 본문: "메인 에이전트가 프로젝트 루트 + 설정 파일 (`package.json`, `build.gradle`, `Podfile`, `pubspec.yaml`, `Cargo.toml`) 을 read → 프로젝트 종류 추론 → 다음 **레시피** 참고하여 후보 list 결정"
- 레시피 표 (markdown table):
  - Node / Web: `.env`, `.env.local`, `.env.production`, `.env.development`, `.envrc` (committed templates `.env.example` / `.env.sample` 제외)
  - Android: `local.properties` (SDK 경로), `keystore.properties`, `*.keystore`, `*.jks`, `google-services.json` (gitignored 인 경우)
  - iOS / macOS: `*.xcconfig` (local-only), `GoogleService-Info.plist` (gitignored 인 경우), `Secrets.swift`
  - Desktop / 기타: `secrets.properties`, `secrets.json`, `config.local.*`
  - Flutter: `android/local.properties` + `android/key.properties` + iOS 항목 + `.env.*`
  - Rust: `.env*`, `.cargo/credentials` (gitignored 인 경우)
- 컨셉 한 줄: "**컨셉**: '로컬 빌드 환경 파일' = 빌드/실행 시 필요하지만 git committed 가 아닌 파일. build artifact (`dist/` / `build/` / `target/` / `.gradle/` / `node_modules/`) 와 IDE 디렉토리와 OS 임시 파일 (`.DS_Store`) 은 **제외**."
- Procedure 5-step (numbered list):
  1. 메인이 프로젝트 종류 추론 (위 레시피 기준)
  2. 루트에서 후보 파일 존재 확인 (`ls` + `git ls-files --error-unmatch <path>` 로 gitignored 인지 판정 — committed 면 git checkout 자동 복원 가능하므로 복사 skip)
  3. 후보 list 사용자에게 한 줄 노출: `ℹ️ 감지된 로컬 빌드 환경 파일: <list>. 추가/제외 알려주세요.`
  4. 사용자 응답 catch (기존 HARD-GATE 의 EXCLUDE 옵션 유지)
  5. ENV_FILES (또는 LOCAL_BUILD_FILES) 배열에 최종 후보 저장 → 다음 Step 으로 전달
- 마지막 fallback 한 줄: "후보가 없으면 한 줄 안내 (`ℹ️ 프로젝트 루트에 로컬 빌드 환경 파일 후보 없음 — 복사 skip`) 후 다음 Step 진행. Don't abort."

- [ ] **Step 5: Anti-Patterns 표 갱신 (line 178-181 의 처음 2 row + 신규 3rd row 추가)**

**원본** (`skills/setting-up-worktrees/SKILL.md:178-181`):

```markdown
| Wrong | Right |
|---|---|
| Asking the user "which env files to copy?" | Forbidden by HARD-GATE. Auto-glob `.env*` and copy all (except templates). |
| Skipping env copy because user didn't mention it | Always copy all detected `.env*`. The point is build-ready worktrees. |
```

**수정 후**:

```markdown
| Wrong | Right |
|---|---|
| Asking the user "which 로컬 빌드 환경 파일 to copy?" | Forbidden by HARD-GATE. 메인이 프로젝트 컨텍스트 보고 후보 자동 판단 + 노출 + 복사. |
| Skipping copy because user didn't mention it | 항상 메인이 감지된 후보 모두 복사 시도 (사용자 명시 exclude 만 예외). build-ready worktree 가 목적. |
| Hardcoded `.env*` glob 만 사용 (Android/iOS/desktop 미커버) | LLM-judged 레시피 적용. 새 플랫폼 (Flutter / RN / Rust) 도 컨텍스트 추론으로 cover. |
```

- [ ] **Step 6: 검증 grep after**

```bash
# 옛 hardcoded 표현 → 거의 모두 사라져야 함 (예시 in 레시피 표는 OK)
grep -cE "Auto-glob \\.env\\*|auto-glob \`\\.env\\*\`" skills/setting-up-worktrees/SKILL.md
# 신규 표현
grep -c "로컬 빌드 환경 파일" skills/setting-up-worktrees/SKILL.md
grep -c "LLM-judged" skills/setting-up-worktrees/SKILL.md
```

Expected: 0 / ≥3 / ≥1

- [ ] **Step 7: Commit**

```bash
git add skills/setting-up-worktrees/SKILL.md
git commit -m "task 7: setting-up-worktrees 일반화 — 로컬 빌드 환경 파일 LLM-judged Procedure (v2.0.2)"
```

---

### Task 8: commands/worktree.md 본문 동기

**Files:**

- Modify: `commands/worktree.md`

**Model**: sonnet (Korean prose)

**Deps**: 없음

- [ ] **Step 1: Read 로 현재 표현 catch + 검증 grep**

```bash
grep -c "env 파일\|\\.env\\*\|env file" commands/worktree.md
```

Expected: > 0

- [ ] **Step 2: Edit — Korean 용어 일반화 (`env 파일` → `로컬 빌드 환경 파일`)**

Read commands/worktree.md → 본문 안 "env 파일" / "env file" 등 표현을 "로컬 빌드 환경 파일" 로 갱신. `.env*` glob 예시는 레시피 형태로 변환 (한 줄 안내 — "메인이 프로젝트 종류 보고 후보 자동 판단").

(byte-copy block 은 Read 결과 보고 정확한 line range 와 본문 catch 후 Edit 호출 — 본 plan 에 미리 박지 않음 — sonnet 이 판단)

- [ ] **Step 3: 검증 grep after**

```bash
grep -c "로컬 빌드 환경 파일" commands/worktree.md
```

Expected: ≥1

- [ ] **Step 4: Commit**

```bash
git add commands/worktree.md
git commit -m "task 8: commands/worktree.md 본문 동기 — 로컬 빌드 환경 파일 (v2.0.2)"
```

---

### Task 9: H13 fixture 신규 + tests/README 색인

**Files:**

- Create: `skills/js-super-sub-driven/tests/H13-og-flow-subagent-routing/README.md`
- Modify: `skills/js-super-sub-driven/tests/README.md`

**Model**: sonnet (Korean prose fixture 작성)

**Deps**: T1 (path skills/js-super-sub-driven/ 존재 전제)

- [ ] **Step 1: 검증 test (fixture 존재 X)**

```bash
ls skills/js-super-sub-driven/tests/H13-og-flow-subagent-routing/README.md 2>&1 | head -1
grep -c "H13" skills/js-super-sub-driven/tests/README.md
```

Expected: "No such file" / 0

- [ ] **Step 2: H13 fixture Create (new file)**

**수정 후** (new file: `skills/js-super-sub-driven/tests/H13-og-flow-subagent-routing/README.md`):

````markdown
# H13 — og-flow Subagent path 매칭 검증

**v2.0.2+ — A (rename) + B (handoff 강화) dogfood**

## 시나리오 (positive — 매칭이 upstream 원본)

가상 외부 피처 dogfood:

1. 사용자: `/og-brainstorm 새-피처-이름`
2. og-brainstorming → og-designing-direction → `/og-write-plan`
3. plan 작성 끝 Execution Handoff 게이트 → 사용자: "1" 또는 "Subagent-Driven"
4. 메인 에이전트가 매칭하는 skill ID 검증

## 기대 결과 (rename 후)

- 매칭된 skill ID: **`superpowers:subagent-driven-development`** (upstream-untouched)
- NOT `js-super:js-super-sub-driven` (js-super 확장)
- og-writing-plans Anti-Pattern 표 + bold prefix 가 LLM 매칭 catch

## 검증 (수동 dogfood)

`/og-write-plan` 끝 게이트에서 메인이 다음 중 어느 skill 을 invoke 하는지 확인:

- ✅ `superpowers:subagent-driven-development` — 정상
- ❌ `js-super:js-super-sub-driven` — 회귀 (A rename 했는데도 매칭 충돌 잔존 → B handoff 강화 추가 필요 catch)

## 시나리오 (negative — js-super 정식 흐름)

대조군 — `/brainstorm` (js-super 정식) → `/write-plan` → Subagent 선택 시:

- 매칭된 skill ID: **`js-super:js-super-sub-driven`** (정상)
- NOT `superpowers:subagent-driven-development`

## 연결 위험

- R8 (og 흐름 dogfood 미검증) — 본 fixture 가 catch
- R1 (rename breaking — cross-ref 누락) — 매칭 실패 시 누락 file 추적 시작
````

- [ ] **Step 3: tests/README.md 색인 갱신 (Edit)**

Read `skills/js-super-sub-driven/tests/README.md` → 적절한 위치에 H13 한 줄 추가:

**수정 후** (Edit by appending after existing H 시리즈 section 또는 신규 v2.0.2+ 섹션):

```markdown
## v2.0.2+ — og-flow rename + 핸드오프 강화 fixtures

- `H13-og-flow-subagent-routing/` — og-flow Subagent path 매칭 검증 (upstream 원본 매칭 / js-super-sub-driven 미매칭)
```

- [ ] **Step 4: 검증 grep after**

```bash
ls skills/js-super-sub-driven/tests/H13-og-flow-subagent-routing/README.md
grep -c "H13" skills/js-super-sub-driven/tests/README.md
```

Expected: file 존재 / 1

- [ ] **Step 5: Commit**

```bash
git add skills/js-super-sub-driven/tests/H13-og-flow-subagent-routing/ skills/js-super-sub-driven/tests/README.md
git commit -m "task 9: H13 fixture — og-flow Subagent routing 검증 + README 색인 (v2.0.2)"
```

---

### Task 10: 검증 (정적 grep + pytest 회귀)

**Files:**

- 변경 0 (검증만 — `[검증]` entry 별도 CH-id)

**Model**: haiku (mechanical grep + pytest)

**Deps**: T1, T2, T3, T4, T5, T6, T7, T8, T9 (모든 변경 완료 후)

- [ ] **Step 1: live cross-ref 완전 갱신 확인 (R1 catch)**

```bash
# live 파일 (docs/ 제외) 에 old name 없어야 함
grep -rn "js-super-subagent-driven-development" \
  --include="*.md" --include="*.py" \
  --exclude-dir=docs \
  --exclude-dir=.worktrees \
  --exclude-dir=.git \
  . 2>/dev/null
```

Expected: 출력 없음 (또는 의도된 immutable history 만)

- [ ] **Step 2: pytest 회귀 (scripts 갱신 후 import 깨짐 catch)**

```bash
source .venv/bin/activate && pytest -v --tb=short
```

Expected: 모든 case PASS (기존 61 baseline + 회귀 X)

- [ ] **Step 3: worktree skill body 표현 검증 (R2 catch)**

```bash
grep -c "로컬 빌드 환경 파일\|LLM-judged" skills/setting-up-worktrees/SKILL.md
grep -c "로컬 빌드 환경 파일" commands/worktree.md
```

Expected: ≥3 / ≥1

- [ ] **Step 4: H13 fixture 존재 + 색인 (R8 catch)**

```bash
ls skills/js-super-sub-driven/tests/H13-og-flow-subagent-routing/README.md
grep -c "H13" skills/js-super-sub-driven/tests/README.md
```

Expected: file 존재 / 1

- [ ] **Step 5: og-writing-plans B 강화 catch**

```bash
grep -c "\\*\\*\`superpowers:subagent-driven-development\`\\*\\*" skills/og-writing-plans/SKILL.md
grep -c "js-super-sub-driven.*NOT\|NOT.*js-super-sub-driven\|js-super-sub-driven 매치" skills/og-writing-plans/SKILL.md
```

Expected: ≥1 / ≥1

- [ ] **Step 6: 변경 0 commit (검증 entry 별도)**

본 task 는 변경 없음. `[검증]` entry 는 end-of-run consolidator 에서 별도 CH-id 로 append.

---

### Task 11: 6 manifest bump 2.0.1 → 2.0.2

**Files:**

- Modify: `.claude-plugin/plugin.json`
- Modify: `.claude-plugin/marketplace.json`
- Modify: `package.json`
- Modify: `.codex-plugin/plugin.json`
- Modify: `.cursor-plugin/plugin.json`
- Modify: `gemini-extension.json`

**Model**: haiku (mechanical version bump)

**Deps**: T10 (검증 통과 후)

- [ ] **Step 1: 검증 test (현재 버전)**

```bash
grep "2.0.1" .claude-plugin/plugin.json .claude-plugin/marketplace.json package.json .codex-plugin/plugin.json .cursor-plugin/plugin.json gemini-extension.json 2>/dev/null
```

Expected: 모든 파일 2.0.1 매치

- [ ] **Step 2: bump 스크립트 실행**

```bash
bash scripts/bump-version.sh 2.0.2
```

- [ ] **Step 3: 검증 grep after**

```bash
grep "2.0.2" .claude-plugin/plugin.json .claude-plugin/marketplace.json package.json .codex-plugin/plugin.json .cursor-plugin/plugin.json gemini-extension.json
grep "2.0.1" .claude-plugin/plugin.json .claude-plugin/marketplace.json package.json .codex-plugin/plugin.json .cursor-plugin/plugin.json gemini-extension.json
```

Expected: 6 / 0

- [ ] **Step 4: Commit**

```bash
git add .claude-plugin/plugin.json .claude-plugin/marketplace.json package.json .codex-plugin/plugin.json .cursor-plugin/plugin.json gemini-extension.json
git commit -m "chore: bump version 2.0.1 → 2.0.2"
```

---

### Task 12: HANDOFF.md 갱신 (local only, no commit)

**Files:**

- Modify: `HANDOFF.md` (gitignored, local-only)

**Model**: sonnet (Korean prose)

**Deps**: T11 (bump 완료 후 최종 상태 기록)

- [ ] **Step 1: HANDOFF 갱신 — Goal 25 → 완료 마킹 / Current Progress → v2.0.2 push 결과 / Next Steps 재정렬**

- 마지막 갱신 line → 2026-05-16 갱신
- Goal 25 → "(v2.0.2 minor, 완료 + push 됨)"
- Goal 26 NEW (다음 후보) → v2.0.3 또는 Haiku byte-copy 진짜 dogfood
- Current Progress → v2.0.2 push 결과 + 산출물 + 검증 결과 + git history (12 task ~14 commits) + Wave 실행 table
- Next Steps 재정렬 → v2.0.2 complete / Haiku dogfood + H13/H14 외부 dogfood / AskUserQuestion schema 보강 / README 21~25 항목 갱신
- Backlog 색인 → v2.0.2 archived / v2.0.3 후보 (있다면) pending

- [ ] **Step 2: 검증 (HANDOFF gitignored 확인)**

```bash
git status HANDOFF.md 2>&1 | head -3
```

Expected: gitignored 안내 또는 untracked (no commit needed)

- [ ] **Step 3: No commit** — HANDOFF 는 gitignored. local 보관만.

---

## 2. 위험 코드 지점

(tech-design §6 R1~R8 ↔ plan 의 file:line + mitigation 매핑)

- `skills/js-super-sub-driven/` (post-rename) 외부 14 live 파일 — **breaking**: cross-reference 누락 시 dispatch failure 또는 매칭 모호성 잔존. Mitigation: T10 정적 grep 검증 (`grep -rn "js-super-subagent-driven-development" --exclude-dir=docs`) → 0 hit 보장
- `skills/setting-up-worktrees/SKILL.md:Step 2 (LLM-judged Procedure)` — **side-effect**: 세션마다 다른 파일 복사 가능성 (비결정성). Mitigation: 본문 레시피 + 사용자 노출 단계 강제 (`ℹ️ 감지된 후보: <list>`) → 사용자 catch 가능
- `skills/og-writing-plans/SKILL.md:152-154 + Anti-Pattern 표` — **side-effect**: B (handoff 강화) 가 A (rename) 와 의미 중복. Mitigation: B 는 "future-proof 안전망" 으로 명시. layered safety framing 으로 중복 X
- `docs/features/` 10 immutable 파일 — **side-effect**: rename 전 식별자 그대로 (history 보존). Mitigation: plan T10 의 grep 명령에 `--exclude-dir=docs` 추가 → false positive 회피
- 6 manifest — **breaking** (release 메타): bump 누락 시 marketplace 옛 버전 표시. Mitigation: T11 의 `bump-version.sh` 자동 6 파일 동기 + grep 검증
- `CLAUDE.md` 3 기존 결합 메모 + 1 신규 worktree 결합 메모 — **side-effect**: rename 갱신 누락 시 다음 회귀 catch 패턴 약화. Mitigation: T5 multi-step 안 grep 검증 (전후 9 → 0)
- `skills/setting-up-worktrees/SKILL.md:Procedure` — **side-effect**: Firebase config / google-services 등 ambiguous 케이스. Mitigation: 본문에 "committed 인 파일은 git checkout 자동 복원 → 복사 skip" 명시 + `git ls-files --error-unmatch` 로 판정
- og-flow A+B 실제 작동 검증 — **side-effect**: 본 release 내에서는 검증 불가 (rename 후 외부 og-flow dogfood 필요). Mitigation: T9 H13 fixture 신규 (수동 dogfood 안내) + HANDOFF 에 catch entry 추가 안내

## 3. 롤백 전략

- **Code (T1 dir rename)**: `git revert <T1 SHA>` — git mv 는 git 추적 → revert 로 원복 가능. 단 후속 cross-ref task 들도 같이 revert 해야 일관성 유지.
- **Code (T2~T9 cross-ref + worktree + og B)**: `git revert <SHA-list>` 또는 `git reset --hard <T0 SHA>` (release 전 시점).
- **Config (manifest bump)**: `bash scripts/bump-version.sh 2.0.1` 또는 manifest 6 파일 수동 원복.
- **DB**: N/A — 본 release DB 무관.
- **HANDOFF**: local-only — git 영향 0. 사용자가 직접 edit.

---

## 변경이력
<!-- change-history skill auto-appends entries here, oldest first -->

### [2026-05-16 14:45] [구현계획서-수정]
- **id**: CH-20260516-003
- **이유**: 신규 구현계획서 — v2.0.2 12 task TDD bite-sized 분해 (rename + 5 cross-ref bundle + worktree + og B + fixture + 검증 + bump + HANDOFF)
- **무엇이**: v2.0.2-worktree-and-og-flow-implementation-plan.md 전체 (frontmatter commit_policy=per-task / §1 12 task / §2 위험 8 entry / §3 롤백 전략)
- **영향범위**: 14 live 파일 cross-ref + setting-up-worktrees skill body + og-writing-plans handoff + CLAUDE.md 결합 메모 + 신규 H13 fixture + 6 manifest + HANDOFF (local)
- **연관 항목**: CH-20260516-001 (요구사항), CH-20260516-002 (개발방향)

### [2026-05-16 15:30] [코드-수정] (batch: tasks 1..9, 11)
- **id**: CH-20260516-004
- **이유**: v2.0.2 minor release — worktree LLM-judged 일반화 + js-super-sub-driven rename + og-writing-plans 핸드오프 강화 통합 완료
- **무엇이**: skill rename (atomic) + 14 live 파일 cross-ref + setting-up-worktrees skill body + og-writing-plans handoff + CLAUDE.md + commands/worktree.md + H13 fixture + 6 manifest bump
- **영향범위**: 14 live 파일 / 신규 1 fixture / 6 manifest. immutable history (docs/features 10) 보존
- **위험 카테고리**: breaking (rename), side-effect (worktree LLM-judged 비결정성)
- **task별 세부 (10건)**:
  - Task 1: `skills/js-super-sub-driven/*` — git mv + internal self-ref (breaking) — commit: `41a0bbf`
  - Task 2: 5 skill body cross-ref (breaking) — commit: `e48a339`
  - Task 3: 4 commands cross-ref (breaking) — commit: `88c6529`
  - Task 4: 3 scripts cross-ref + pytest 61 PASS (breaking) — commit: `9dc235c`
  - Task 5: CLAUDE.md cross-ref + worktree 결합 메모 (breaking, side-effect) — commit: `7d13168`
  - Task 6: og-writing-plans superpowers: prefix bold + Anti-Pattern — commit: `c132fd4`
  - Task 7: setting-up-worktrees 일반화 — 로컬 빌드 환경 파일 LLM-judged (side-effect) — commit: `3b99f28`
  - Task 8: commands/worktree.md 본문 동기 — commit: `545a905`
  - Task 9: H13 fixture + tests/README 색인 — commit: `58e133d`
  - Task 11: 6 manifest bump 2.0.1→2.0.2 — commit: `ecfaa22`
- **연관 commits**: `5787814..ecfaa22` (10 commits)
- **변경 전/후 코드**: 생략 — `git show <SHA>` 로 조회

### [2026-05-16 15:30] [검증] (task: Task 10 — 정적 grep + pytest)
- **id**: CH-20260516-005
- **이유**: v2.0.2 모든 변경 후 정합성 검증
- **무엇이**: F1 live cross-ref grep / F2 pytest 회귀 / F3 worktree 표현 / F4 H13 fixture / F5 og-writing-plans B
- **결과**: PASS — F1 0 hit (HANDOFF gitignored 만) / F2 61 PASS / F3 worktree SKILL.md 11 + commands 2 / F4 fixture 존재 + 색인 1 / F5 bold prefix 1 + js-super-sub-driven 2
- **연관 commit**: 변경 0 (검증만)
