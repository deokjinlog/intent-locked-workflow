---
name: v2.5-no-ask-flag
commit_policy: per-task
---

# v2.5 — `--no-ask` 플래그 구현 계획

## 개요 — anchor 검증 catch

tech-design D1 (8 skill 모두 적용) 의 본문 검증 결과:

- **anchor 본격 4** (`brainstorming` / `designing-direction` / `writing-plans` / `auto-brainstorming`): body 안 "## 사용자 질문 룰 (v2.0.3+)" 섹션 존재 → 직후에 `--no-ask` 분기 sub-section 본격 추가
- **anchor 짧은 reference 4** (`executing-plans` / `auto-designing-direction` / `auto-writing-plans` / `auto-executing-plans`): "사용자 질문 룰" 섹션 자체 없거나 본문이 이미 도구 호출 X 명시. body 끝 (Anti-Patterns 또는 Related Skills 직전) 에 짧은 reference sub-section 추가

본질 동작 동일 (`--no-ask` 진입 시 도구 호출 0 보장). 표현 schema 만 두 갈래.

## §1 Task 분해 (Wave 0~4, 총 23 task)

### Wave 0 — CLAUDE.md 결합 메모 (1 task)

#### Task 1 — CLAUDE.md `--no-ask` 결합 메모 추가

- **Files**: `CLAUDE.md`
- **Model**: sonnet
- **Risk**: R-6 (회귀 catch grep 결합)

**원본** (CLAUDE.md 본문 마지막 라인):

```
요약: v2.4 메이저 — A 광범위 (10+ skill + 10+ commands + README + CLAUDE.md) + B 4 룰 (generating-html + auto-* 3 race delay + `/sync-html --check`). atomic 처리.
```

**수정 후**:

```
요약: v2.4 메이저 — A 광범위 (10+ skill + 10+ commands + README + CLAUDE.md) + B 4 룰 (generating-html + auto-* 3 race delay + `/sync-html --check`). atomic 처리.

## --no-ask 플래그 ↔ 8 skill body 결합 (v2.5+)

`--no-ask` 플래그는 8 skill body 의 분기 sub-section 으로 구현. 사용자가 슬래시 명령에 `--no-ask` 토큰 명시 시에만 진입. 메인 자체 판단 활성화 X.

### 적용 범위 (8 skill)

- anchor 본격 4 (brain / design / write / auto-brain): "사용자 질문 룰" 섹션 직후 sub-section
- anchor 짧은 reference 4 (executing / auto-design / auto-write / auto-execute): body 끝 sub-section
- og-* / fast-tasks / worktree-merge-back — 비적용 (회귀 catch grep 으로 보장)

### 핵심 룰

- 도구 호출 0 보장 (AskUserQuestion 흐름 전 구간 호출 X)
- 게이트 자체는 살아 있음 (질문은 그대로, 도구만 우회)
- skill 진입 시 1회 boilerplate prose (`ℹ️ --no-ask 모드 진입 ...`)
- 위험 명령 진입 직전 prose 보강 (`⚠️ 위험 명령 진입 — 응답 기다림`)
- auto-* 4 의 내부 escalation 경로 (BLOCKED 자가복구 / critical 7 재질문 / Other 모호 응답) 도 도구 호출 0 보장

### 회귀 catch grep (release 직전 메인 dogfood)

```bash
grep -c "--no-ask 플래그 (v2.5+)" \
  skills/brainstorming/SKILL.md \
  skills/designing-direction/SKILL.md \
  skills/writing-plans/SKILL.md \
  skills/executing-plans/SKILL.md \
  skills/auto-brainstorming/SKILL.md \
  skills/auto-designing-direction/SKILL.md \
  skills/auto-writing-plans/SKILL.md \
  skills/auto-executing-plans/SKILL.md
# expected: 각 ≥ 1

grep -l "--no-ask" \
  skills/og-brainstorming/SKILL.md \
  skills/og-designing-direction/SKILL.md \
  skills/og-writing-plans/SKILL.md \
  skills/og-executing-plans/SKILL.md \
  commands/fast-tasks.md \
  skills/worktree-merge-back/SKILL.md 2>/dev/null
# expected: empty
```

요약: 8 skill body 분기 + 8 commands 안내 + CLAUDE.md 결합 메모 + 6 manifest bump = 23 파일 atomic patch.
```

### Wave 1 — anchor 본격 4 skill 의 분기 boilerplate (4 task)

**공통 boilerplate** (4 task `**수정 후**` 블록 안 동일 본문):

```
### 예외 — `--no-ask` 플래그 (v2.5+)

사용자가 슬래시 명령에 `--no-ask` 토큰을 **명시** 한 경우에만 진입. 메인 자체 판단으로 활성화 X.

- 모든 사용자 질문을 prose (메인 turn 자유 텍스트) 로 처리
- `AskUserQuestion` 도구 호출 **0 보장**
- 게이트 자체는 살아 있음 — 사용자 prose 응답 기다림
- 알람 fire X (사용자가 명시 invoke 했으니 인지 가정)

#### skill 진입 시 1회 boilerplate

skill 진입 직후 다음 한 줄을 prose 로 출력:

> ℹ️ `--no-ask` 모드 진입 — AskUserQuestion 도구 호출 X, 응답 알람 X. 백그라운드 작업 중이면 응답 시점을 직접 체크해주세요.

#### 위험 명령 진입 직전 보강

critical 7 케이스 (파일 삭제 / `git push --force` / DB migration / mass commit / 외부 메시지 등) 실행 직전에는 다음 한 줄을 prose 로 출력:

> ⚠️ 위험 명령 진입 — 응답 기다림. 백그라운드 작업 중이면 직접 catch 해주세요.

`⚠️` 마커 + 별도 줄로 일반 prose 보다 두드러지게.
```

#### Task 2 — `skills/brainstorming/SKILL.md` 분기 추가

- **Files**: `skills/brainstorming/SKILL.md`
- **Model**: sonnet
- **Risk**: R-1 / R-2 / R-4 / R-7

**원본** (line 8):

```
## 사용자 질문 룰 (v2.0.3+) — 항상 AskUserQuestion
```

**수정 후**: 위 line 직후에 공통 boilerplate sub-section 박음 (heading anchor `### 예외 — \`--no-ask\` 플래그 (v2.5+)` 보장).

**Acceptance**: `grep -c "--no-ask 플래그 (v2.5+)" skills/brainstorming/SKILL.md` = ≥1

#### Task 3 — `skills/designing-direction/SKILL.md` 분기 추가

- **Files**: `skills/designing-direction/SKILL.md`
- **Model**: sonnet
- **Risk**: R-1 / R-2 / R-4 / R-7

**원본** (line 8):

```
## 사용자 질문 룰 (v2.0.3+) — 항상 AskUserQuestion
```

**수정 후**: Task 2 와 동일 schema — 공통 boilerplate sub-section 박음.

**Acceptance**: `grep -c "--no-ask 플래그 (v2.5+)" skills/designing-direction/SKILL.md` = ≥1

#### Task 4 — `skills/writing-plans/SKILL.md` 분기 추가

- **Files**: `skills/writing-plans/SKILL.md`
- **Model**: sonnet
- **Risk**: R-1 / R-2 / R-4 / R-7

**원본** (line 8):

```
## 사용자 질문 룰 (v2.0.3+) — 항상 AskUserQuestion
```

**수정 후**: Task 2 와 동일 schema — 공통 boilerplate sub-section 박음.

**Acceptance**: `grep -c "--no-ask 플래그 (v2.5+)" skills/writing-plans/SKILL.md` = ≥1

#### Task 5 — `skills/auto-brainstorming/SKILL.md` 분기 추가

- **Files**: `skills/auto-brainstorming/SKILL.md`
- **Model**: sonnet
- **Risk**: R-1 / R-2 / R-3 / R-4 / R-7

**원본** (line 12):

```
## 사용자 질문 룰 (v2.0.3+) — 항상 AskUserQuestion
```

**수정 후**: Task 2 와 동일 schema + auto-\* 추가 룰 sub-section 끝에 박음:

```
#### auto-\* 4 skill 추가 룰

내부 escalation 경로 (BLOCKED 자가복구 실패 / critical 7 재질문 / Other 모호 응답 재질문) 에서도 `AskUserQuestion` 호출 **0 보장**. clarifying Q 자체는 그대로 prose 유지.
```

**Acceptance**: `grep -c "--no-ask 플래그 (v2.5+)" skills/auto-brainstorming/SKILL.md` = ≥1 + `grep -c "auto-\* 4 skill 추가 룰" skills/auto-brainstorming/SKILL.md` = ≥1

### Wave 2 — anchor 짧은 reference 4 skill (4 task)

**공통 reference** (4 task `**수정 후**` 블록 안 동일):

```
## --no-ask 플래그 (v2.5+) — 짧은 reference

본 skill 흐름은 `AskUserQuestion` 호출이 본문에 명시 X (clarifying Q 자체가 prose default). `--no-ask` 플래그 진입 시 추가 분기 없음 — 본문 그대로 도구 호출 0 보장.

단 내부 escalation (BLOCKED 자가복구 실패 / critical 7 재질문 / Other 모호 응답) 에서도 도구 호출 0 보장. 자세한 룰은 `skills/brainstorming/SKILL.md` 의 `### 예외 — \`--no-ask\` 플래그 (v2.5+)` 답습.
```

#### Task 6 — `skills/executing-plans/SKILL.md` reference 추가

- **Files**: `skills/executing-plans/SKILL.md`
- **Model**: sonnet
- **Risk**: R-1 / R-4

**원본** (body 끝 — Anti-Patterns 직전 anchor):

```
본 룰은 프로젝트 `CLAUDE.md` 의 글로벌 "AskUserQuestion 도구 우선 (v2.3.5+)" 룰의 skill body 측 cross-reference.
```

**수정 후**: 위 line 직후에 공통 reference sub-section 박음.

**Acceptance**: `grep -c "--no-ask 플래그 (v2.5+)" skills/executing-plans/SKILL.md` = ≥1

#### Task 7 — `skills/auto-designing-direction/SKILL.md` reference 추가

- **Files**: `skills/auto-designing-direction/SKILL.md`
- **Model**: sonnet
- **Risk**: R-1 / R-3

**원본** (body 끝 — Anti-Patterns 직전):

```
`parse_interrupt` 매치 시 exit + `ℹ️ 알겠습니다. /write-plan 은 나중에 직접 실행해주세요.` 안내. 매치 X → `js-super:auto-writing-plans` invoke.
```

**수정 후**: 위 line 직후 + 빈 줄 + `## Anti-Patterns` 헤더 직전에 공통 reference sub-section 박음.

**Acceptance**: `grep -c "--no-ask 플래그 (v2.5+)" skills/auto-designing-direction/SKILL.md` = ≥1

#### Task 8 — `skills/auto-writing-plans/SKILL.md` reference 추가

- **Files**: `skills/auto-writing-plans/SKILL.md`
- **Model**: sonnet
- **Risk**: R-1 / R-3

**원본** (body 끝 — Anti-Patterns 직전):

```
`parse_interrupt` 매치 시 exit. 매치 X → `js-super:auto-executing-plans` invoke.
```

**수정 후**: 위 line 직후 + 빈 줄 + `## Anti-Patterns` 직전에 공통 reference sub-section 박음.

**Acceptance**: `grep -c "--no-ask 플래그 (v2.5+)" skills/auto-writing-plans/SKILL.md` = ≥1

#### Task 9 — `skills/auto-executing-plans/SKILL.md` reference 추가

- **Files**: `skills/auto-executing-plans/SKILL.md`
- **Model**: sonnet
- **Risk**: R-1 / R-3

**원본** (body 끝 — Anti-Patterns 직전 적당 위치):

본 skill body 의 Step 끝 line 확인 후 Anti-Patterns 직전 anchor 활용.

**수정 후**: Anti-Patterns 헤더 직전에 공통 reference sub-section 박음.

**Acceptance**: `grep -c "--no-ask 플래그 (v2.5+)" skills/auto-executing-plans/SKILL.md` = ≥1

### Wave 3 — commands 8 본문 보강 (8 task)

**공통 본문** (8 task `**수정 후**` 블록 안 거의 동일):

```
## `--no-ask` 플래그 (v2.5+)

`AskUserQuestion` 도구가 느리거나 불안정할 때, 도구 호출을 완전히 우회하고 싶을 때 사용:

`/<command-name> <slug> --no-ask`

질문은 그대로 받지만 메인 에이전트가 채팅 메시지 (prose) 형식으로 묻습니다. 사용자도 채팅으로 응답하면 됩니다. 알람 fire X — 백그라운드 작업 중이면 응답 시점을 직접 체크해야 합니다.

플래그 위치 자유 (`<slug> --no-ask` 또는 `--no-ask <slug>` 모두 가능).
```

각 task 의 `<command-name>` 만 다름 (brainstorm / design / write-plan / execute-plan / auto-brainstorm / auto-design / auto-write-plan / auto-execute-plan).

#### Task 10~17 — commands/\*.md 본문 끝에 `--no-ask` 섹션 추가

각 task 의 **Files** / **Model** / **anchor** :

| Task | Files | Model | anchor 위치 |
|---|---|---|---|
| 10 | `commands/brainstorm.md` | sonnet | 본문 끝 (마지막 라인 직후) |
| 11 | `commands/design.md` | sonnet | 동일 |
| 12 | `commands/write-plan.md` | sonnet | 동일 |
| 13 | `commands/execute-plan.md` | sonnet | 동일 |
| 14 | `commands/auto-brainstorm.md` | sonnet | 동일 |
| 15 | `commands/auto-design.md` | sonnet | 동일 |
| 16 | `commands/auto-write-plan.md` | sonnet | 동일 |
| 17 | `commands/auto-execute-plan.md` | sonnet | 동일 |

**원본** (각 commands/\*.md 의 마지막 라인 — task 별 별도 확인):

각 task 시작 직전 `tail -3 <file>` 으로 anchor 확정 후 Edit.

**수정 후**: 위 anchor 직후 + 빈 줄 + 공통 본문 박음. `<command-name>` 만 각 task 별 치환.

**Risk**: R-1 / R-5 (parsing 약함 catch — commands 본문에 "위치 자유" 명시 포함)

**Acceptance**: `grep -c "--no-ask" commands/<file>.md` = ≥1 (각 task)

### Wave 4 — 6 manifest version bump (6 task)

**공통 패턴**: 각 manifest 파일의 `"version": "2.4.2"` → `"version": "2.5.0"` 1 줄 변경.

#### Task 18~23 — 각 manifest version bump

| Task | Files | Model | 변경 |
|---|---|---|---|
| 18 | `.claude-plugin/plugin.json` | haiku | `"version": "2.4.2"` → `"version": "2.5.0"` |
| 19 | `.claude-plugin/marketplace.json` | haiku | 동일 |
| 20 | `.cursor-plugin/plugin.json` | haiku | 동일 |
| 21 | `.codex-plugin/plugin.json` | haiku | 동일 |
| 22 | `gemini-extension.json` | haiku | 동일 |
| 23 | `package.json` | haiku | 동일 |

**원본** (각 task 별 — JSON 안 `"version": "2.4.2"` 라인 grep 확정 후 Edit):

```
  "version": "2.4.2",
```

**수정 후**:

```
  "version": "2.5.0",
```

**Risk**: 0 (mechanical, backward compatible)

**Acceptance**: 각 manifest 의 `"version": "2.5.0"` grep 매치 보장.

## §2 위험 코드 지점

| Risk | Task 매핑 | Mitigation 위치 |
|---|---|---|
| R-1 (도구 호출 잔존) | Task 2~9 | 분기 본문 안 "0 보장" 명시 + Acceptance grep |
| R-2 (게이트 skip) | Task 2~5 | 분기 본문 "게이트는 살아 있음 — 응답 기다림" 명시 |
| R-3 (auto-\* 내부 escalation) | Task 5 / 7 / 8 / 9 | auto-\* 추가 룰 sub-section + reference 안 명시 |
| R-4 (위험 명령 catch 실패) | Task 2~5 | 분기 본문 안 `⚠️` 마커 + 별도 줄 |
| R-5 (플래그 parsing 약함) | Task 10~17 | commands 본문 "위치 자유" 명시 |
| R-6 (og-\* 회귀) | Task 1 (CLAUDE.md 결합 메모 grep) | 회귀 catch grep — og-\* 매치 0 보장 |
| R-7 (메인 자체 판단 활성화) | Task 2~5 | 분기 본문 "명시 만, 메인 자체 판단 X" 명시 |

## §3 Acceptance grep (release 직전 메인 dogfood)

tech-design §5.2 의 4 grep block 답습:

```bash
# 1. 8 skill body 분기 박힘
grep -c "--no-ask 플래그 (v2.5+)" \
  skills/brainstorming/SKILL.md \
  skills/designing-direction/SKILL.md \
  skills/writing-plans/SKILL.md \
  skills/executing-plans/SKILL.md \
  skills/auto-brainstorming/SKILL.md \
  skills/auto-designing-direction/SKILL.md \
  skills/auto-writing-plans/SKILL.md \
  skills/auto-executing-plans/SKILL.md
# expected: 각 ≥ 1

# 2. 8 commands 본문 보강
grep -c "--no-ask" \
  commands/brainstorm.md \
  commands/design.md \
  commands/write-plan.md \
  commands/execute-plan.md \
  commands/auto-brainstorm.md \
  commands/auto-design.md \
  commands/auto-write-plan.md \
  commands/auto-execute-plan.md
# expected: 각 ≥ 1

# 3. og-* 비적용
grep -l "--no-ask" \
  skills/og-brainstorming/SKILL.md \
  skills/og-designing-direction/SKILL.md \
  skills/og-writing-plans/SKILL.md \
  skills/og-executing-plans/SKILL.md 2>/dev/null
# expected: empty

# 4. fast-tasks / worktree-merge-back 비적용
grep -l "--no-ask" \
  commands/fast-tasks.md \
  skills/worktree-merge-back/SKILL.md 2>/dev/null
# expected: empty

# 5. 6 manifest bump
grep -c '"version": "2.5.0"' \
  .claude-plugin/plugin.json \
  .claude-plugin/marketplace.json \
  .cursor-plugin/plugin.json \
  .codex-plugin/plugin.json \
  gemini-extension.json \
  package.json
# expected: 각 ≥ 1

# 6. 2.4.x 잔존 0
grep -c '"version": "2.4' \
  .claude-plugin/plugin.json \
  .claude-plugin/marketplace.json \
  .cursor-plugin/plugin.json \
  .codex-plugin/plugin.json \
  gemini-extension.json \
  package.json
# expected: 각 0
```

## §4 dogfood (release 후)

| 시나리오 | 검증 |
|---|---|
| `/brainstorm v2.5-dogfood --no-ask` | skill 진입 boilerplate prose 출력 + clarifying Q prose / `AskUserQuestion` 호출 0 |
| `/auto-brainstorm v2.5-dogfood --no-ask` | 동일 + 내부 escalation 도구 호출 0 |
| `/execute-plan v2.5-dogfood --no-ask` | 위험 명령 진입 시 `⚠️` 마커 prose 출력 |
| 플래그 위치 변경 (slug 앞/뒤) | 두 위치 모두 정상 인식 |
| 일반 `/brainstorm` (no flag) | v2.4.2 동작 그대로 — backward compatible |

## §5 commit 정책 + Wave 마무리

- `commit_policy: per-task` — 각 task 단독 commit. wave-parallel subagent dispatch 도 task 단위로 commit
- Wave 별 task 모두 완료 후 메인이 end-of-run consolidator [log] commit 1 회 추가 (v2.4.2 패턴 답습)
- 모든 task 끝나면 사용자 push + tag v2.5.0 + marketplace + reload

## 변경이력

- [구현계획서-수정] CH-20260524-003 | 2026-05-24 | v2.5-no-ask-flag-implementation-plan.md 신규 생성 | 23 task 분해 (Wave 0 CLAUDE.md + Wave 1 4 PRD skill + Wave 2 4 reference + Wave 3 8 commands + Wave 4 6 manifest). anchor 검증 catch — anchor 본격 4 vs anchor 짧은 reference 4 분기 schema. R-1~R-7 → task 매핑, 6 Acceptance grep block, dogfood 5 시나리오. plan_byte_check pass. commit_policy per-task.
- [코드-수정] CH-20260524-004 | 2026-05-24 | v2.5.0 Wave 0~4 batch | 메인 직접 inline 처리 (Wave 별 1 commit × 5). Wave 0 (`fc3f7cf`): CLAUDE.md 결합 메모. Wave 1 (`521ef86`): PRD 4 skill 분기 boilerplate (brain / design / write / auto-brain "사용자 질문 룰" 직후 sub-section). Wave 2 (`d51035e`): anchor 짧은 reference 4 skill (executing / auto-design / auto-write / auto-execute body 끝 짧은 reference). Wave 3 (`8e15be5`): commands 8 본문 보강 (8 슬래시 명령 끝에 사용 예 + 플래그 위치 자유 + 알람 X). Wave 4 (`b846957`): 6 manifest 통일 2.4.2 → 2.5.0. Acceptance grep 6 block 모두 통과 (8 skill v2.5+ marker 각 ≥1 + `--no-ask` 토큰 각 3 / og-* + fast-tasks + worktree-merge-back empty / 6 manifest "2.5.0" 각 1 / 2.4.x 잔존 0).
