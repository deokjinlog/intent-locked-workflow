---
slug: v2.5.1-worktree-cleanup-auto
commit_policy: per-task
execution_mode: main-inline-or-subagent
note: "메인 inline 실행 가정 — `**원본**` 블록 생략. wave-parallel subagent 호출 시 implementer 가 직접 Read + byte-copy."
---

# v2.5.1 — Worktree Cleanup 자동화 (implementation-plan)

**Slug**: v2.5.1-worktree-cleanup-auto
**작성일**: 2026-05-25
**총 task 수**: 8 (Wave 0~4)
**참조**: requirements (CH-20260525-001), tech-design (CH-20260525-002)

---

## §1. Tasks

### Wave 0 — CLAUDE.md 결합 메모

#### Task 1 — CLAUDE.md v2.0.4+ Anti-Pattern 표 보강 + v2.5.1 신규 결합 메모 섹션 추가

- **Files**: `CLAUDE.md`
- **Model**: sonnet (Korean prose 조작 + 신규 섹션 ≈ 80 라인)
- **TDD steps**:
  1. v2.0.4+ Anti-Pattern 표에 "재귀 머지 자동 시도 (안전)" vs "--strategy ours/theirs 자동 (위험)" 분기 명시 추가
  2. v2.5.1 신규 결합 메모 섹션 (`## worktree cleanup 자동화 결합 (v2.5.1+)`) 추가 — 5 파일 atomic patch 룰, 회귀 catch grep, 결합 영역
  3. 회귀 grep 으로 신규 섹션 1 매치 확인
- **RISK**: side-effect (contributor 메모 갱신, skill body 영향 X)
- **Self-Review**: 신규 섹션이 기존 v2.0.4+ 표 바로 다음 + 다른 v2.X 섹션과 일관된 양식 사용

### Wave 1 — worktree-merge-back skill body 변경

#### Task 2 — Step 3 머지 대상 변경 + 충돌 처리 완화 + Step 4.5 신규 (env 동기화)

- **Files**: `skills/worktree-merge-back/SKILL.md`
- **Model**: sonnet (다중 분기 + 신규 Step + Korean prose)
- **TDD steps**:
  1. Step 3 본문 — `git fetch origin && git merge origin/$MAIN_BRANCH` → `git merge $MAIN_BRANCH` (로컬만)
  2. Step 3 충돌 처리 — `AskUserQuestion` 게이트 → "git default 재귀 머지 자동 시도 + semantic conflict 발생만 사용자 prose 안내" 로 본문 교체
  3. Step 4.5 신규 — env 파일 동기화 (LLM 변경 의미 판단 + 각 파일 1줄 prose 보고 + 선택적 cp + symlink `-P` 보존). 대상 파일 감지 룰 `setting-up-worktrees` 의 LLM-judged Procedure 와 동일 표현
  4. 회귀 grep: `git fetch origin` 0건 (Anti-Pattern catch 라인 외), `git default 재귀 머지` ≥ 1, `Step 4.5` ≥ 1
- **RISK**: side-effect + breaking (v2.0.5 의 안전성 게이트 일부 완화), shared state (env cp)
- **Self-Review**: HARD-GATE worktree-only 표현 유지 (변경 X). `--strategy ours/theirs` 자동 적용 본문 부재 확인

#### Task 3 — Step 5 종료 메시지 보강 + Anti-Patterns 표 갱신 + Why v2.5.1 섹션

- **Files**: `skills/worktree-merge-back/SKILL.md`
- **Model**: sonnet (Korean prose)
- **TDD steps**:
  1. Step 5 종료 메시지 본문 — `/worktree-remove` 호출 안내 + R-3 remote 동기화 안내 추가
  2. Anti-Patterns 표 — "Step 3 충돌을 자동 해결 (ours / theirs)" 라인 유지 + "재귀 머지 자동 시도는 git default 동작이라 OK" 분기 추가
  3. "Why v2.0.5 slim" 섹션 다음에 "Why v2.5.1" 섹션 신규 추가 — 사용자 dogfood 결과 + 4 결정 요약
  4. 회귀 grep: `worktree-remove` ≥ 1, `git -C <main-path> pull` ≥ 1
- **RISK**: side-effect (안내문 변경, 로직 변경 X)
- **Self-Review**: Anti-Pattern 표 행수 catch (기존 행 삭제 X)

### Wave 2 — commands/worktree-merge-back.md 안내 동기화

#### Task 4 — commands/worktree-merge-back.md 본문 정합성 검사 + 필요 시 안내 갱신

- **Files**: `commands/worktree-merge-back.md`
- **Model**: haiku (trivial — 짧은 본문 + 1~2줄 수정)
- **TDD steps**:
  1. 현재 본문 Read — 1줄 description 만 있는지 확인
  2. v2.5.1 변경 (자동화 / env / `/worktree-remove`) 안내 추가 필요 시 1~2 줄 추가. 불필요 시 skip
  3. 회귀 grep: skill body 의 핵심 변경이 commands 안내와 충돌 0
- **RISK**: none (description 메타)
- **Self-Review**: 사용자 노출 안내 톤 한국어 친화

### Wave 3 — worktree-remove 신규 (skill + command)

#### Task 5 — skills/worktree-remove/SKILL.md 신규 파일 작성

- **Files**: `skills/worktree-remove/SKILL.md` (신규)
- **Model**: sonnet (신규 skill body 작성, ≈ 100 라인 Korean + 안전성 표)
- **TDD steps**:
  1. SKILL.md frontmatter 작성 (name / description / model frontmatter — opus 보호 아님, sonnet default)
  2. HARD-GATE 섹션 — feature 워크트리 안에서만 호출 (main 워크트리 차단). 패턴은 worktree-merge-back 의 HARD-GATE 답습
  3. Process Step 1~4 — working tree 검사 + parent 추론 + `git -C $MAIN_PATH worktree remove $CURRENT_PATH` + `git -C $MAIN_PATH branch -d $CURRENT_BRANCH`
  4. `--force` 플래그 옵트인 분기 (D-6) — 명시 시만 `branch -D`
  5. 종료 메시지 — `cd <parent-path>` 안내 (R-4)
  6. Anti-Patterns 표 + Related Skills
  7. 회귀 grep: `HARD-GATE — Worktree-Only` 1 매치, `git branch -d` ≥ 1, `--force` 분기 ≥ 1
- **RISK**: breaking (신규 destructive 명령 — worktree 제거 + 브랜치 삭제)
- **Self-Review**: D-5 (HARD-GATE 유지) / D-6 (safe default) 명시 답습. `git -C` 패턴 사용 (cd 안 함)

#### Task 6 — commands/worktree-remove.md 신규 슬래시 명령 추가

- **Files**: `commands/worktree-remove.md` (신규)
- **Model**: haiku (짧은 신규 파일 — 1~3 줄 description)
- **TDD steps**:
  1. frontmatter description — "현재 워크트리 정리 (워크트리 제거 + 브랜치 안전 삭제)"
  2. 본문 — skill 호출 안내 1 줄
  3. 사용자 노출 톤 한국어 친화
- **RISK**: none (description 메타)
- **Self-Review**: 다른 commands/*.md 와 양식 일관

### Wave 4 — fixture 갱신

#### Task 7 — skills/worktree-merge-back/tests/H15-conflict/README.md 갱신

- **Files**: `skills/worktree-merge-back/tests/H15-conflict/README.md`
- **Model**: haiku (prose 갱신)
- **TDD steps**:
  1. README Read — v2.0.5 의 "모든 충돌 게이트" 시나리오 가정 catch
  2. D-2 변경 반영 — "재귀 머지 자동 + semantic conflict 만 게이트" 시나리오로 갱신
  3. Expected behavior 섹션 갱신
- **RISK**: none (테스트 시뮬레이션 prose 만)
- **Self-Review**: 다른 fixture README (H14 / H16) 양식과 일관

### Wave 5 — 6 manifest 버전 bump

#### Task 8 — 6 manifest 동시 2.5.0 → 2.5.1

- **Files**: `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.cursor-plugin/plugin.json`, `.codex-plugin/plugin.json`, `gemini-extension.json`, `package.json`
- **Model**: haiku (6 파일 mechanical version string swap — `"version": "2.5.0"` → `"version": "2.5.1"`)
- **TDD steps**:
  1. 각 manifest 의 version 필드 swap (sed-style 정확 매치)
  2. JSON 유효성 검증 (`python -m json.tool < <file>` 또는 `cat <file>`)
  3. 회귀 grep: `"version": "2.5.0"` 0 매치 (전체 manifest)
- **RISK**: none (mechanical)
- **Self-Review**: 6 파일 모두 갱신 (누락 catch)

---

## §2. 위험 코드 지점

### R-1 (CLAUDE.md v2.0.4+ Anti-Pattern 표 약화 회귀)

- **위치**: `CLAUDE.md` 의 `## worktree-merge-back skill 결합 (v2.0.4+)` 섹션의 Anti-Pattern 표
- **변경**: 표 자체 유지 + "재귀 머지 자동 시도 OK (git default 동작)" 분기 신규 라인 추가
- **Mitigation**: `--strategy ours/theirs` 자동 적용 catch 라인은 그대로 유지. 회귀 catch grep 으로 정상성 검증 (Task 3 회귀 grep)

### R-2 (LLM env 파일 판단 오판 → 변수 노출/누락)

- **위치**: `skills/worktree-merge-back/SKILL.md` Step 4.5 본문
- **변경**: env cp 결정 시 silent 금지 명시 + 각 파일 1줄 prose 보고 룰
- **Mitigation**: cp 명령 본문 직전에 사용자 stop 가능 표현 명시. Step 4.5 본문에 "silent cp 금지" 명시

### R-3 (parent 로컬 stale → push 시점 충돌)

- **위치**: `skills/worktree-merge-back/SKILL.md` Step 5 종료 메시지
- **변경**: "remote 동기화 필요 시 `git -C <main-path> pull` 별도 실행" 안내 추가
- **Mitigation**: 자동 fetch X 명시 (사용자 의도 보존). 안내만

### R-4 (worktree-remove 호출 후 cwd 사라짐)

- **위치**: `skills/worktree-remove/SKILL.md` 종료 메시지 (Task 5)
- **변경**: `cd <parent-path>` 안내 명시
- **Mitigation**: skill 내부 자동 cd 금지 (사용자 shell 영향 안 줌). 안내만

### R-5 (환경 파일 symlink 처리)

- **위치**: `skills/worktree-merge-back/SKILL.md` Step 4.5 본문 (Task 2)
- **변경**: cp `-P` (symlink 보존) default 명시 + symlink 발견 시 prose 보고 분기
- **Mitigation**: D-3 의 LLM 판단 단계에서 symlink 자체를 1줄 prose 보고

---

## §3. 변경이력

### [2026-05-25 12:55] [구현계획서-수정]
- **id**: CH-20260525-003
- **이유**: v2.5.1 신규 피처 최초 implementation-plan 작성 (Wave 0~5 분할, 8 task)
- **무엇이**: v2.5.1-worktree-cleanup-auto-implementation-plan.md 전체 (frontmatter + §1 Task 1~8 + §2 R-1~R-5)
- **영향범위**: tech-design 의 D-1~D-6 + R-1~R-5 모두 task / 위험 코드 지점에 매핑. plan_byte_check PASS (0 mismatches — 슬림 plan, `**원본**` 블록 의도적 생략)
- **연관 항목**: CH-20260525-001 (requirements), CH-20260525-002 (tech-design)
