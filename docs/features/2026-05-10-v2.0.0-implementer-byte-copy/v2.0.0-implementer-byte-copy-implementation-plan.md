---
commit_policy: per-task
---

# v2.0.0 implementer byte-copy + reorder 3-stage 구현계획서

> **For agentic workers:** REQUIRED SUB-SKILL: Use `auto-executing-plans` (auto-flow chain) or `js-super-subagent-driven-development` (manual). Steps use checkbox (`- [ ]`) syntax.

**Goal:** subagent dispatch 패턴을 LLM transcription → byte-copy + reorder 3-stage 분담 으로 전환 + v1.1.16 backlog 2건 묶어 처리.

**Architecture:** Implementer (haiku 고정, byte-copy only) → BLOCKED 시 Reorder (sonnet, conditional) → Spec Reviewer (sonnet, 변경 X). plan-side `**원본**` byte-equal 강제 by `scripts/plan_byte_check.py` helper at writing-plans Self-Review.

**Tech Stack:** Python (pytest), markdown skill bodies, Claude Code Agent dispatch.

**Spec inputs:**
- `v2.0.0-implementer-byte-copy-requirements.md` — D1~D13 (release scope, 3-stage 분담, byte-copy 룰, plan_byte_check, v1.1.16 묶음, dogfood H11)
- `v2.0.0-implementer-byte-copy-tech-design.md` — D-T1~D-T10 (prompt 분리 / haiku 고정 / Mismatch API / SKILL.md W-2 분기 / 두 fix 명세 / H11 / CLAUDE.md), R1~R11, F1~F8 + G1~G3 + H11

**Wave 구조 (수동 — DAG bug 미해결, T1 이 fix):**

| Wave | Tasks | 비고 |
|---|---|---|
| W1 | T1, T2 | v1.1.16 fix 2건 |
| W2 | T3 | plan_byte_check helper + tests |
| W3 | T4, T5 | implementer-prompt + reorder-prompt |
| W4 | T6, T7, T8 | SKILL.md W-2 + writing-plans + auto-writing-plans |
| W5 | T9, T10, T11 | H11 fixture + tests/README + CLAUDE.md |
| W6 | T12 | 검증 (F1~F8 + pytest) |
| W7 | T13 | manifest bump 1.1.17 → 2.0.0 |
| W8 | T14 | HANDOFF (local) |

---

## 1. 단계별 작업

### Task 1: dag_builder deps ordering 버그 fix (v1.1.16-#1)

**Files:**
- Modify: `scripts/dag_builder.py:38-74` (build_waves loop)
- Test: `scripts/tests/test_dag_builder.py` (신규 케이스 추가)

**Model**: haiku

- [ ] **Step 1: Write the failing test (TDD)**

`scripts/tests/test_dag_builder.py` 끝에 추가:

**수정 후**:

```python
def test_strict_deps_ordering_with_file_overlap():
    """T2 deps=[T1]. 둘 다 같은 파일 만지면 T2 가 T1 앞에 wave 배치 X.
    
    Bug context (v1.1.16): Kahn indegree 0 후보가 T1 만일 때 정상이지만,
    file overlap 으로 T1, T2 가 같은 wave 못 들어가면 T2 는 다음 wave 로
    deferred. 이때 deps 우선순위 보장 — T2 가 W2 가 아니라 T1 의 W1 다음에
    배치되어야 함. 회귀 패턴: deferred 후 deps 재계산 누락.
    """
    from scripts.dag_builder import Task, build_waves
    tasks = [
        Task(id=1, name="A", files=["shared.py"], deps=[]),
        Task(id=2, name="B", files=["shared.py"], deps=[1]),
    ]
    waves = build_waves(tasks)
    assert len(waves) == 2
    assert [t.id for t in waves[0].tasks] == [1]
    assert [t.id for t in waves[1].tasks] == [2]


def test_deps_violated_when_file_overlap_defers_dep():
    """후행 task 의 deps 가 같은 wave 내 다른 candidate 보다 앞에 와야 함.
    
    예: T1, T2 (deps=[]), T3 deps=[T2]. T2/T3 파일 동일.
    Kahn 0 후보 = [T1, T2]. file overlap 없음 → 둘 다 W1.
    T3 는 deps=[T2] 만족 + T2 와 file overlap → W2.
    """
    from scripts.dag_builder import Task, build_waves
    tasks = [
        Task(id=1, name="A", files=["a.py"], deps=[]),
        Task(id=2, name="B", files=["b.py"], deps=[]),
        Task(id=3, name="C", files=["b.py"], deps=[2]),
    ]
    waves = build_waves(tasks)
    assert len(waves) == 2
    assert sorted(t.id for t in waves[0].tasks) == [1, 2]
    assert [t.id for t in waves[1].tasks] == [3]
```

- [ ] **Step 2: Run test to verify failure**

```bash
source .venv/bin/activate && pytest scripts/tests/test_dag_builder.py::test_strict_deps_ordering_with_file_overlap -v
```

Expected: FAIL (현재 build_waves 에 deps 우선순위 보장 누락 bug). 또는 PASS — bug 가 일관 발현 안 할 수 있음. 이 경우 더 큰 케이스 추가 필요.

- [ ] **Step 3: Fix build_waves**

`scripts/dag_builder.py:38-74` 의 indegree 갱신 로직 검토 + fix. 핵심: 매 wave 끝 후 indegree 재계산이 deps 우선순위 위반하지 않게.

가능한 fix 방향 (구현자 판단):
- (a) indegree 갱신 시 already-decremented 중복 방지 — 현재 코드의 `for tid, t in by_id.items() ... for dep in t.deps: if dep in placed: indeg[tid] -= 1` 가 누적 placed 에 대해 매 wave 마다 동일 dep 다시 -1 함. 첫 wave 후 wave 2 진입 시 deps=[T1] 인 T2 의 indeg 가 -1 또 적용됨 → indeg=-1 → clamp=0. 그래도 already 0 라 영향 X. **그런데 다음 wave 의 placed 에 또 T1 있어서 또 -1** → 무한 누적.
- 정확한 fix: `placed_in_this_wave: set[int] = {t.id for t in wave_tasks}` 로 좁혀서 갱신.

**원본** (`scripts/dag_builder.py:62-72`):

```python
        for t in wave_tasks:
            placed.add(t.id)

        for tid, t in by_id.items():
            if tid in placed:
                continue
            for dep in t.deps:
                if dep in placed:
                    indeg[tid] -= 1
                    if indeg[tid] < 0:
                        indeg[tid] = 0
```

**수정 후**:

```python
        placed_this_wave = {t.id for t in wave_tasks}
        for t in wave_tasks:
            placed.add(t.id)

        for tid, t in by_id.items():
            if tid in placed:
                continue
            for dep in t.deps:
                if dep in placed_this_wave:
                    indeg[tid] -= 1
                    if indeg[tid] < 0:
                        indeg[tid] = 0
```

- [ ] **Step 4: Run tests to verify pass**

```bash
source .venv/bin/activate && pytest scripts/tests/test_dag_builder.py -v
```

Expected: ALL PASS (기존 + 신규 2건).

---

### Task 2: preflight `_has_changelog_entries` regex false positive fix (v1.1.16-#2)

**Files:**
- Modify: `scripts/preflight.py:31-35` (`_has_changelog_entries` 함수)
- Test: `scripts/tests/test_preflight.py` (신규 케이스 추가)

**Model**: haiku

- [ ] **Step 1: Write the failing test (TDD)**

`scripts/tests/test_preflight.py` 끝에 추가:

**수정 후**:

```python
def test_changelog_in_code_block_not_matched(tmp_path):
    """Plan 본문 안 ```markdown ... ## 변경이력 ... ``` literal 매치 안 됨.
    
    Bug context (v1.1.16): docs-pretty / executing-plans pre-flight 가
    plan 본문의 markdown code block 안 리터럴 `## 변경이력` 을 footer
    entry 로 잘못 판정. fix: rsplit 으로 마지막 occurrence 만 검사.
    """
    from scripts.preflight import _has_changelog_entries
    text = '''
# Plan

## 1. Tasks

### Task 1: example

```markdown
## 변경이력

### [날짜] [요구사항-수정]
- 본문 예시
```

## 변경이력

<!-- empty footer -->
'''
    assert _has_changelog_entries(text) is False


def test_real_changelog_entry_still_detected(tmp_path):
    """진짜 footer entry 는 여전히 감지."""
    from scripts.preflight import _has_changelog_entries
    text = '''
# Plan

## 변경이력

### [2026-05-10 12:00] [요구사항-수정]
- **id**: CH-20260510-001
'''
    assert _has_changelog_entries(text) is True
```

- [ ] **Step 2: Run test to verify failure**

```bash
source .venv/bin/activate && pytest scripts/tests/test_preflight.py::test_changelog_in_code_block_not_matched -v
```

Expected: FAIL (현재 `_has_changelog_entries` 가 split 으로 첫 occurrence 만 보고 그 이후 entry 검색 → code block 안 entry 도 매치).

- [ ] **Step 3: Fix `_has_changelog_entries`**

**원본** (`scripts/preflight.py:31-35`):

```python
def _has_changelog_entries(text: str) -> bool:
    if "## 변경이력" not in text:
        return False
    footer = text.split("## 변경이력", 1)[1]
    return _CHANGELOG_ENTRY.search(footer) is not None
```

**수정 후**:

```python
def _has_changelog_entries(text: str) -> bool:
    if "## 변경이력" not in text:
        return False
    footer = text.rsplit("## 변경이력", 1)[1]
    return _CHANGELOG_ENTRY.search(footer) is not None
```

핵심: `split` → `rsplit`. footer 는 항상 파일 끝, last occurrence = 진짜 footer.

- [ ] **Step 4: Run tests to verify pass**

```bash
source .venv/bin/activate && pytest scripts/tests/test_preflight.py -v
```

Expected: ALL PASS (기존 + 신규 2건).

---

### Task 3: plan_byte_check helper + unit tests (D-T4 + G1)

**Files:**
- Create: `scripts/plan_byte_check.py`
- Create: `scripts/tests/test_plan_byte_check.py`

**Model**: sonnet

- [ ] **Step 1: Write the failing tests (TDD)**

**수정 후** (`scripts/tests/test_plan_byte_check.py`, new file):

```python
"""Unit tests for scripts.plan_byte_check."""
from pathlib import Path
from scripts.plan_byte_check import verify_plan_block_byte_equal, Mismatch


def _write(p: Path, content: str) -> Path:
    p.parent.mkdir(parents=True, exist_ok=True)
    p.write_text(content, encoding="utf-8")
    return p


def test_byte_equal_pass(tmp_path):
    src = _write(tmp_path / "src" / "foo.py", "def hello():\n    return 'hi'\n")
    plan = _write(tmp_path / "plan.md", f"""# Plan
### Task 1
**원본** (`src/foo.py:1-2`):
```python
def hello():
    return 'hi'
```
**수정 후**:
```python
def hello():
    return 'hello'
```
""")
    mismatches = verify_plan_block_byte_equal(plan, tmp_path)
    assert mismatches == []


def test_byte_equal_fail_one_char_different(tmp_path):
    _write(tmp_path / "src" / "foo.py", "def hello():\n    return 'hi'\n")
    plan = _write(tmp_path / "plan.md", """# Plan
### Task 1
**원본** (`src/foo.py:1-2`):
```python
def hello():
    return 'HI'
```
**수정 후**:
```python
def hello():
    return 'hello'
```
""")
    mismatches = verify_plan_block_byte_equal(plan, tmp_path)
    assert len(mismatches) == 1
    assert mismatches[0].file_path.name == "foo.py"


def test_byte_equal_multiple_mismatches(tmp_path):
    _write(tmp_path / "a.py", "x = 1\n")
    _write(tmp_path / "b.py", "y = 2\n")
    plan = _write(tmp_path / "plan.md", """# Plan
### Task 1
**원본** (`a.py:1`):
```python
x = 999
```
**수정 후**:
```python
x = 0
```
### Task 2
**원본** (`b.py:1`):
```python
y = 888
```
**수정 후**:
```python
y = 0
```
""")
    mismatches = verify_plan_block_byte_equal(plan, tmp_path)
    assert len(mismatches) == 2


def test_byte_equal_file_not_found(tmp_path):
    plan = _write(tmp_path / "plan.md", """# Plan
### Task 1
**원본** (`missing.py:1`):
```python
x = 1
```
""")
    mismatches = verify_plan_block_byte_equal(plan, tmp_path)
    assert len(mismatches) == 1
    assert "not found" in mismatches[0].reason.lower()


def test_byte_equal_create_task_no_원본_block(tmp_path):
    """Create task — no `**원본**` block, only `**수정 후**`. Should skip (no Mismatch)."""
    plan = _write(tmp_path / "plan.md", """# Plan
### Task 1
**수정 후** (new file: `new.py`):
```python
print("hi")
```
""")
    mismatches = verify_plan_block_byte_equal(plan, tmp_path)
    assert mismatches == []


def test_byte_equal_line_range_out_of_bounds(tmp_path):
    _write(tmp_path / "src.py", "line1\nline2\n")
    plan = _write(tmp_path / "plan.md", """# Plan
### Task 1
**원본** (`src.py:5-10`):
```
line1
line2
```
""")
    mismatches = verify_plan_block_byte_equal(plan, tmp_path)
    assert len(mismatches) == 1
    assert "out of bounds" in mismatches[0].reason.lower() or "range" in mismatches[0].reason.lower()
```

- [ ] **Step 2: Run tests to verify failures**

```bash
source .venv/bin/activate && pytest scripts/tests/test_plan_byte_check.py -v
```

Expected: ALL FAIL (helper 미존재).

- [ ] **Step 3: Implement plan_byte_check helper**

**수정 후** (`scripts/plan_byte_check.py`, new file):

```python
"""Plan byte-equal verifier (v2.0.0+).

Checks every `**원본**` code block in an implementation plan against the
actual repo file at the specified line range. Used by writing-plans and
auto-writing-plans during Self-Review to enforce byte-equal precondition
for the v2.0.0 byte-copy implementer.
"""
from __future__ import annotations
import re
from dataclasses import dataclass
from pathlib import Path
from typing import List, Optional


@dataclass(frozen=True)
class Mismatch:
    plan_path: Path
    block_index: int
    file_path: Path
    expected: str
    actual: str
    reason: str


_BLOCK_RE = re.compile(
    r"\*\*원본\*\*\s*\(\s*`([^`:]+)(?::([\d-]+))?`\s*\)\s*:\s*\n"
    r"```[a-zA-Z0-9_+-]*\n(.*?)```",
    re.DOTALL,
)


def _parse_line_range(spec: Optional[str]) -> Optional[tuple[int, int]]:
    if not spec:
        return None
    if "-" in spec:
        a, b = spec.split("-", 1)
        return int(a), int(b)
    n = int(spec)
    return n, n


def verify_plan_block_byte_equal(
    plan_path: Path,
    repo_root: Path,
) -> List[Mismatch]:
    """Verify every `**원본**` block in plan_path matches actual file content.
    
    Returns empty list if all match. Each Mismatch carries enough context
    for plan author to fix (which block, which file, expected vs actual,
    reason).
    """
    text = plan_path.read_text(encoding="utf-8")
    mismatches: List[Mismatch] = []
    for idx, match in enumerate(_BLOCK_RE.finditer(text)):
        rel_path = match.group(1).strip()
        line_spec = match.group(2)
        expected = match.group(3)
        if expected.endswith("\n"):
            expected_no_trail = expected[:-1]
        else:
            expected_no_trail = expected

        file_path = (repo_root / rel_path).resolve()
        if not file_path.exists():
            mismatches.append(Mismatch(
                plan_path=plan_path,
                block_index=idx,
                file_path=file_path,
                expected=expected,
                actual="",
                reason=f"file not found: {rel_path}",
            ))
            continue

        file_text = file_path.read_text(encoding="utf-8")
        file_lines = file_text.splitlines(keepends=True)

        rng = _parse_line_range(line_spec)
        if rng is not None:
            start, end = rng
            if start < 1 or end > len(file_lines):
                mismatches.append(Mismatch(
                    plan_path=plan_path,
                    block_index=idx,
                    file_path=file_path,
                    expected=expected,
                    actual="",
                    reason=f"line range {start}-{end} out of bounds (file has {len(file_lines)} lines)",
                ))
                continue
            actual = "".join(file_lines[start - 1:end])
        else:
            actual = file_text

        if actual.endswith("\n"):
            actual_no_trail = actual[:-1]
        else:
            actual_no_trail = actual

        if expected_no_trail != actual_no_trail:
            mismatches.append(Mismatch(
                plan_path=plan_path,
                block_index=idx,
                file_path=file_path,
                expected=expected,
                actual=actual,
                reason=f"byte mismatch at {rel_path}:{line_spec or 'whole-file'}",
            ))

    return mismatches
```

- [ ] **Step 4: Run tests to verify pass**

```bash
source .venv/bin/activate && pytest scripts/tests/test_plan_byte_check.py -v
```

Expected: 6 PASS.

---

### Task 4: implementer-prompt.md STRICT BYTE-COPY 룰 + haiku 고정 (D-T2)

**Files:**
- Modify: `skills/js-super-subagent-driven-development/implementer-prompt.md`

**Model**: sonnet (Korean prose 조작 — Haiku 거부)

- [ ] **Step 1: Read current implementer-prompt.md**

```bash
cat skills/js-super-subagent-driven-development/implementer-prompt.md
```

(이미 위에서 확인)

- [ ] **Step 2: Patch — model haiku 고정 + STRICT BYTE-COPY rule + Status enum 확장**

**원본** (`skills/js-super-subagent-driven-development/implementer-prompt.md:6-9`):

```
Task tool (general-purpose):
  model: "{{MODEL}}"   # Main injects per-task value from plan's `**Model**:` field; defaults to "sonnet" if missing. See SKILL.md "Model Selection" + writing-plans "Task Model Hint".
  description: "Implement Task N: [task name]"
  prompt: |
```

**수정 후**:

```
Task tool (general-purpose):
  model: "haiku"   # v2.0.0+ HAIKU FIXED — byte-copy mode does not need LLM transcription. plan's `**Model**:` hint is ignored under subagent-driven-development. (See CLAUDE.md "implementer-prompt + reorder-prompt + plan_byte_check" section.)
  description: "Implement Task N: [task name]"
  prompt: |
```

- [ ] **Step 3: Add STRICT BYTE-COPY section**

**원본** (`skills/js-super-subagent-driven-development/implementer-prompt.md:49-57`):

```
    ## Your Job

    Once you're clear on requirements:
    1. Implement exactly what the task specifies
    2. Write tests (following TDD if task says to)
    3. Verify implementation works (run tests in working tree, no commit)
    4. **DO NOT git commit** — main agent commits at wave end in plan order
    5. Self-review (see below)
    6. Report back
```

**수정 후**:

```
    ## STRICT BYTE-COPY (v2.0.0+)

    When the task has `**원본**` / `**수정 후**` code blocks, your Edit tool
    invocation MUST satisfy:

    - `old_string` argument == byte-identical content of the `**원본**` block
    - `new_string` argument == byte-identical content of the `**수정 후**` block

    DO NOT:
    - Paraphrase, reformat, "improve" the code
    - Normalize whitespace, indentation, or line endings
    - Add comments, type hints, docstrings not in the plan
    - Reorder imports, sort dictionary keys, or apply autofix

    For Create tasks (only `**수정 후**` block, no `**원본**`), Write tool's
    `content` argument == byte-identical content of the `**수정 후**` block.

    If Edit fails with "old_string not found":
    - DO NOT retry with paraphrased input
    - DO NOT search the file and approximate the location
    - Immediately report `Status: BLOCKED — 원본 mismatch at <file>` and stop.
      The main agent will dispatch a Reorder subagent (sonnet) to reconcile.

    ## Your Job

    Once you're clear on requirements:
    1. Implement exactly what the task specifies — byte-copy the blocks
    2. Write tests (following TDD if task says to) — same byte-copy rule
    3. Verify implementation works (run tests in working tree, no commit)
    4. **DO NOT git commit** — main agent commits at wave end in plan order
    5. Self-review (see below)
    6. Report back
```

- [ ] **Step 4: Update Status enum in Report Format**

**원본** (`skills/js-super-subagent-driven-development/implementer-prompt.md:135-145`):

```
    ## Report Format

    When done, report:
    - **Status:** DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
    - What you implemented (or what you attempted, if blocked)
    - What you tested and test results
    - Files changed (this list helps the main agent scope `git diff`)
    - Self-review findings (if any)
    - Any issues or concerns

    Use DONE_WITH_CONCERNS if you completed the work but have doubts about correctness.
    Use BLOCKED if you cannot complete the task. Use NEEDS_CONTEXT if you need
    information that wasn't provided. Never silently produce work you're unsure about.
```

**수정 후**:

```
    ## Report Format

    When done, report:
    - **Status:** DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
    - What you implemented (or what you attempted, if blocked)
    - What you tested and test results
    - Files changed (this list helps the main agent scope `git diff`)
    - Self-review findings (if any)
    - Any issues or concerns

    Status meanings:
    - DONE — byte-copy succeeded + tests pass
    - DONE_WITH_CONCERNS — completed but unsure about correctness
    - **BLOCKED — 원본 mismatch at <file>** (v2.0.0+) — Edit "old_string not found".
      The main agent will dispatch a Reorder subagent. Just report the file and stop.
    - BLOCKED — other reason (file system error, test runner crash, etc.)
    - NEEDS_CONTEXT — information missing from task description
    
    Never silently produce work you're unsure about.
```

- [ ] **Step 5: Verify (no test, just grep)**

```bash
grep -nE "STRICT BYTE-COPY|haiku|BLOCKED — 원본 mismatch" skills/js-super-subagent-driven-development/implementer-prompt.md
```

Expected: 모두 ≥1 매치.

---

### Task 5: reorder-prompt.md 신규 (D-T3)

**Files:**
- Create: `skills/js-super-subagent-driven-development/reorder-prompt.md`

**Model**: sonnet

- [ ] **Step 1: Write reorder-prompt.md**

**수정 후** (`skills/js-super-subagent-driven-development/reorder-prompt.md`, new file):

````
# Reorder/Resolve Subagent Prompt Template (v2.0.0+)

Used when implementer reports `Status: BLOCKED — 원본 mismatch`. Sonnet only.
The reorder subagent reconciles plan changes with user-side edits when
Edit "old_string not found" because the file diverged from plan's `**원본**`
block.

```
Task tool (general-purpose):
  model: "sonnet"   # v2.0.0+ FIXED — reorder needs free judgment + Korean prose handling. NOT haiku.
  description: "Reorder Task N: reconcile plan vs user edit"
  prompt: |
    You are reconciling Task N: [task name]

    The implementer (haiku, byte-copy mode) reported BLOCKED because
    Edit's old_string did not match the actual file. Your job is to
    figure out whether the file diverged due to:
    
    (a) User manual edit before plan execution — preserve user intent + apply plan's net change
    (b) Genuine plan-vs-file conflict — escalate to user via Status: NEEDS_USER

    DO NOT silently overwrite user edits. When in doubt, escalate.

    ## Inputs

    - Task description + `**원본**` block + `**수정 후**` block (from plan)
    - Current file state (read with Read tool — DO NOT trust the implementer's report)
    - BLOCKED reason from implementer

    ## Task Description

    [FULL TEXT of task from plan]

    ## Plan blocks

    **원본** (from plan):
    [original block byte-equal to plan]

    **수정 후** (from plan):
    [target block byte-equal to plan]

    ## Implementer report

    [BLOCKED reason verbatim]

    ## Your Job

    1. Read the actual file with Read tool. Identify what differs from `**원본**`.
    2. Categorize:
       (a) Compatible — user added an unrelated line, plan's net change still applies cleanly
       (b) Conflict — user edited the same lines plan wants to change
    3. (a) → apply both: write the file so user's change AND plan's net change are present.
            Use Edit with the actual current content as old_string + reconciled content as new_string.
            Verify by re-reading after.
    4. (b) → escalate. Do NOT pick a side.

    ## Status formats

    Status: DONE
      Reconciled successfully. Both user edit and plan change present.
      Files changed: <list>
      
    Status: NEEDS_USER
      Reason: <one-line description of conflict>
      Conflict:
        plan intent: <what plan wanted to change>
        user edit:   <what user changed>
      Files: <file:line>
      Suggested questions:
        - <question 1 the main agent could ask the user>
        - <question 2>

    ## Constraints

    - DO NOT git commit (main agent handles wave-end commits)
    - DO NOT touch the 변경이력 footer
    - DO NOT add RISK comments (main does this post-hoc)
    - DO NOT escalate to user directly (main handles AskUserQuestion)
    - DO NOT silently apply only the plan change if user edit is incompatible —
      that's exactly the silent overwrite v2.0.0 byte-copy is designed to prevent

    ## When to NEEDS_USER

    Default to NEEDS_USER if:
    - User edit and plan change touch overlapping lines
    - User intent unclear from the diff alone
    - Reconciliation requires architectural judgment (e.g., user changed a
      type signature; plan adds a feature that also touches that signature)

    Reconciliation is meant for purely additive cases. Anything else escalates.
```
````

- [ ] **Step 2: Verify (grep)**

```bash
grep -nE "Status: NEEDS_USER|model: \"sonnet\"|Conflict:|silent overwrite" skills/js-super-subagent-driven-development/reorder-prompt.md
```

Expected: 모두 ≥1 매치.

---

### Task 6: SKILL.md Per-wave Sequence W-2 분기 추가 (D-T5)

**Files:**
- Modify: `skills/js-super-subagent-driven-development/SKILL.md` (Per-wave Sequence W-2 섹션)

**Model**: sonnet

- [ ] **Step 1: Locate W-2 section**

```bash
grep -n "W-2" skills/js-super-subagent-driven-development/SKILL.md
```

- [ ] **Step 2: Patch W-2 — Stage 1/2/3 분기 추가**

W-2 섹션 (현재 "Pair-parallel dispatch" 본문) 본문 끝에 v2.0.0 분기 추가. 정확한 byte-equal 패치는 implementer 가 현재 SKILL.md 본문 보고 결정.

추가 본문 (W-2 끝):

```
### W-2 (v2.0.0+) Stage 1/2/3 분기

For each task within wave, dispatch sequence is now:

1. **Stage 1 — Implementer** (haiku, byte-copy)
   Dispatch via `./implementer-prompt.md` (model="haiku" fixed).
   - Status: DONE → proceed to Stage 3 (Spec Reviewer)
   - Status: BLOCKED — 원본 mismatch → proceed to Stage 2 (Reorder)
   - Status: BLOCKED — other → wave finalization failure isolation (D7), no Stage 2

2. **Stage 2 — Reorder/Resolve** (sonnet, conditional)
   Dispatch via `./reorder-prompt.md` (model="sonnet").
   - Status: DONE → proceed to Stage 3 (Spec Reviewer)
   - Status: NEEDS_USER → main agent invokes AskUserQuestion with reorder's
     Suggested questions. User answers → main reconciles directly via Edit
     → proceed to Stage 3.

3. **Stage 3 — Spec Reviewer** (sonnet, unchanged)
   Dispatch via `./spec-reviewer-prompt.md`. Same as v1.1.x.

Stage 1 BLOCKED → Stage 2 dispatch is automatic (no user gate). Stage 2
NEEDS_USER → main agent gate. Plan's `**Model**:` hint is IGNORED for
Stage 1 (haiku fixed); spec reviewer remains sonnet (D11/D-T2 PRD).
```

- [ ] **Step 3: Verify (grep)**

```bash
grep -nE "Stage 1 — Implementer|Stage 2 — Reorder|reorder-prompt\.md" skills/js-super-subagent-driven-development/SKILL.md
```

Expected: 모두 ≥1 매치.

---

### Task 7: writing-plans/SKILL.md plan_byte_check 호출 (D-T6)

**Files:**
- Modify: `skills/writing-plans/SKILL.md` (Self-Review section / Checklist Step 5)

**Model**: sonnet

- [ ] **Step 1: Locate Self-Review section**

```bash
grep -n "## Self-Review\|Self-review" skills/writing-plans/SKILL.md
```

- [ ] **Step 2: Insert plan_byte_check helper call**

Self-Review 섹션 (또는 Checklist Step 5) 끝에 추가:

```
### plan_byte_check helper (v2.0.0+)

After all tasks are written and self-review checks pass, run the byte-equal
verifier:

```bash
source .venv/bin/activate && python -c "
import sys
from pathlib import Path
from scripts.plan_byte_check import verify_plan_block_byte_equal
mismatches = verify_plan_block_byte_equal(
    Path('docs/features/<date>-<slug>/<slug>-implementation-plan.md'),
    Path('.'),
)
if mismatches:
    for m in mismatches:
        print(f'MISMATCH #{m.block_index} — {m.reason}')
        print(f'  file: {m.file_path}')
    sys.exit(1)
print('plan_byte_check ✅ all blocks byte-equal')
sys.exit(0)
" 2>&1
```

If exit 1 (mismatches found):
- Mismatch list shown to user.
- Plan is NOT saved. Fix the `**원본**` blocks (they must be byte-identical
  to current file content) and re-run the helper.
- This enforces v2.0.0 byte-copy implementer's precondition: it will
  fail-fast on mismatch with no LLM fuzzy match fallback.
```

- [ ] **Step 3: Verify (grep)**

```bash
grep -nE "plan_byte_check|verify_plan_block_byte_equal" skills/writing-plans/SKILL.md
```

Expected: ≥2 매치 (helper 이름 + bash 호출).

---

### Task 8: auto-writing-plans/SKILL.md plan_byte_check 호출 (D-T6)

**Files:**
- Modify: `skills/auto-writing-plans/SKILL.md` (Step 2 또는 Step 4)

**Model**: sonnet

- [ ] **Step 1: Locate Step 4 (산출물 자동 작성)**

```bash
grep -n "Step 4 — 산출물 자동 작성\|Step 5 — verifying-spec" skills/auto-writing-plans/SKILL.md
```

- [ ] **Step 2: Insert plan_byte_check between Step 4 and Step 5**

Step 4 끝 또는 Step 5 시작 직전에 추가:

```
### Step 4.5 — plan_byte_check 자동 (v2.0.0+)

Plan 본문 자동 작성 직후, 메인이 helper 자동 호출:

```bash
source .venv/bin/activate && python -c "
import sys
from pathlib import Path
from scripts.plan_byte_check import verify_plan_block_byte_equal
mismatches = verify_plan_block_byte_equal(
    Path('<PLAN_PATH>'),
    Path('.'),
)
if mismatches:
    for m in mismatches:
        print(f'MISMATCH #{m.block_index} — {m.reason}')
    sys.exit(1)
sys.exit(0)
"
```

미스매치 발견 시 메인이 즉시 plan 의 `**원본**` 블록 수정 후 재시도 (auto 모드 — 사용자 응답 wait X). 3회 재시도 후에도 실패 시 `ℹ️ plan_byte_check 3회 실패. 사용자 개입 필요.` 안내 후 종료. byte-copy 정밀도 강제는 v2.0.0 구현계획서의 핵심 precondition.
```

- [ ] **Step 3: Verify (grep)**

```bash
grep -nE "plan_byte_check|verify_plan_block_byte_equal" skills/auto-writing-plans/SKILL.md
```

Expected: ≥2 매치.

---

### Task 9: H11 dogfood fixture (D-T9)

**Files:**
- Create: `skills/js-super-subagent-driven-development/tests/H11-user-edit-reorder/README.md`

**Model**: sonnet

- [ ] **Step 1: Write fixture README**

**수정 후** (`skills/js-super-subagent-driven-development/tests/H11-user-edit-reorder/README.md`, new file):

```markdown
# H11 — 사용자 mid-flight 수정 + reorder dispatch (v2.0.0+)

## 시나리오

1. plan 작성 + 저장. plan_byte_check ✅ 통과 (작성 시점 byte-equal).
2. **사용자가 plan 실행 전 직접 `<file>` 편집** (한 줄 추가, 다른 위치).
3. `/auto-execute-plan` 또는 메인 wave-parallel dispatch 발화.
4. Wave 진입 → Implementer (haiku, byte-copy) 시도.
5. Edit "old_string not found" — 사용자 추가 한 줄 때문에 file 이 plan 의 `**원본**` 과 다름.
6. Implementer Status: BLOCKED — 원본 mismatch at <file>.
7. 메인이 자동으로 Reorder dispatch (sonnet, ./reorder-prompt.md).
8. Reorder 가 사용자 추가 line + plan net change 호환 가능 판정 → 둘 다 적용 → Status: DONE.
9. Spec Reviewer ✅.
10. Wave finalization commit — commit 안 plan 변경 + 사용자 추가 line 둘 다 포함.

## 검증 항목

- [ ] 메인이 reorder dispatch 발화했는지 (Stage 1 BLOCKED → Stage 2 자동)
- [ ] Reorder 가 silent overwrite 안 함 (사용자 line 손실 0)
- [ ] commit 안 plan 변경 + 사용자 변경 둘 다 포함
- [ ] Spec Reviewer 가 정상 통과

## 회귀 패턴

| 회귀 | 증상 |
|---|---|
| Stage 1 BLOCKED 후 Stage 2 안 부름 | 메인 SKILL.md W-2 분기 누락 |
| Reorder 가 user line 무시 | reorder-prompt 의 silent overwrite 차단 룰 약화 |
| Reorder 가 NEEDS_USER 안 escalate | reorder-prompt 의 escalation default 룰 약화 |
| plan_byte_check 가 작성 단계에서 false-pass | helper Mismatch detection 약화 (G1 fixture 이미 catch) |

## 의존

- `skills/js-super-subagent-driven-development/implementer-prompt.md` — STRICT BYTE-COPY rule
- `skills/js-super-subagent-driven-development/reorder-prompt.md` — Status: NEEDS_USER 형식
- `skills/js-super-subagent-driven-development/SKILL.md` — Per-wave Sequence W-2 분기
- `scripts/plan_byte_check.py` — 작성 단계 차단

## 다음 단계

본 fixture 는 release v2.0.0 자체 dogfood 와 자연스럽게 만나야 함. 본 release 의 plan 안 task 1개를 의도적으로 사용자 시뮬레이션 commit 으로 `<file>` 수정 → reorder dispatch 발화 검증.
```

- [ ] **Step 2: Verify (file exists)**

```bash
ls -la skills/js-super-subagent-driven-development/tests/H11-user-edit-reorder/README.md
```

Expected: file exists.

---

### Task 10: tests/README.md H11 색인 추가

**Files:**
- Modify: `skills/js-super-subagent-driven-development/tests/README.md`

**Model**: haiku

- [ ] **Step 1: Read current README**

```bash
cat skills/js-super-subagent-driven-development/tests/README.md
```

- [ ] **Step 2: Append v2.0.0 section**

파일 끝에 추가:

```
## v2.0.0+ — byte-copy + reorder 3-stage fixtures

| Fixture | 시나리오 |
|---|---|
| H11-user-edit-reorder | 사용자 mid-flight 수정 → Implementer BLOCKED → Reorder dispatch → DONE (silent overwrite 차단 검증) |
```

- [ ] **Step 3: Verify (grep)**

```bash
grep -n "H11-user-edit-reorder" skills/js-super-subagent-driven-development/tests/README.md
```

Expected: ≥1 매치.

---

### Task 11: CLAUDE.md 결합 메모 추가 (D-T10)

**Files:**
- Modify: `CLAUDE.md` (root)

**Model**: sonnet

- [ ] **Step 1: Read current CLAUDE.md**

```bash
cat CLAUDE.md | tail -40
```

- [ ] **Step 2: Append new section**

CLAUDE.md 끝 (마지막 섹션 다음) 추가:

```markdown
## implementer-prompt + reorder-prompt + plan_byte_check 결합 (v2.0.0+)

v2.0.0 메이저에서 subagent dispatch 패턴이 LLM transcription → byte-copy + reorder 3-stage 분담 으로 근본 변경. 다음 4 파일은 atomic 변경 규칙 적용:

1. `skills/js-super-subagent-driven-development/implementer-prompt.md` — STRICT BYTE-COPY 룰 + haiku 고정 + Status enum BLOCKED
2. `skills/js-super-subagent-driven-development/reorder-prompt.md` — Status NEEDS_USER 형식 + sonnet 고정 + silent overwrite 차단
3. `scripts/plan_byte_check.py` — `**원본**` 블록 byte-equal 검증 helper (writing-plans + auto-writing-plans 의 Self-Review)
4. `skills/js-super-subagent-driven-development/SKILL.md` — Per-wave Sequence W-2 의 Stage 1/2/3 분기

### 회귀 패턴 (한쪽만 변경 시)

| 누락 | 증상 |
|---|---|
| W-2 분기 빠짐 | implementer BLOCKED 보고했는데 메인이 reorder 안 부르고 그대로 fail |
| plan_byte_check 룰 약화 | plan 작성 시 byte-mismatch false-pass → 실행 단계 BLOCKED 빈도 ↑ |
| reorder-prompt silent overwrite 차단 약화 | 사용자 mid-flight 수정 손실 위험 (v2.0.0 핵심 안전성 손상) |
| implementer-prompt STRICT BYTE-COPY 약화 | drift 회귀 (v1.1.x 와 동일) |

### Test fixture

`skills/js-super-subagent-driven-development/tests/H11-user-edit-reorder/README.md` — 사용자 mid-flight 수정 시뮬레이션 + reorder dispatch 발화 검증.

### 영향 범위

- byte-copy + reorder 는 **subagent 모드에만** 적용 (subagent-driven-development + auto-executing-plans).
- 일반 `/execute-plan` (executing-plans inline) 영향 0 — 사용자 LLM 자율 보정 선호 케이스 보존.
- og-* skill 영향 0 — upstream mirror 보존.

요약: 4 파일 변경은 묶어서 처리. 분리 release X.
```

- [ ] **Step 3: Verify (grep)**

```bash
grep -nE "implementer-prompt \+ reorder-prompt \+ plan_byte_check 결합" CLAUDE.md
```

Expected: 1 매치.

---

### Task 12: 검증 (F1~F8 grep + pytest 통합)

**Files:**
- Test only (no code change)

**Model**: sonnet

- [ ] **Step 1: F1~F8 grep 검증**

```bash
# F1 — implementer-prompt
grep -nE "STRICT BYTE-COPY|model: \"haiku\"|BLOCKED — 원본 mismatch" \
  skills/js-super-subagent-driven-development/implementer-prompt.md

# F2 — reorder-prompt
grep -nE "Status: NEEDS_USER|model: \"sonnet\"|silent overwrite" \
  skills/js-super-subagent-driven-development/reorder-prompt.md

# F3 — SKILL.md W-2 분기
grep -nE "Stage 1 — Implementer|Stage 2 — Reorder|reorder-prompt" \
  skills/js-super-subagent-driven-development/SKILL.md

# F4 — writing-plans
grep -nE "plan_byte_check|verify_plan_block_byte_equal" skills/writing-plans/SKILL.md

# F5 — auto-writing-plans
grep -nE "plan_byte_check|verify_plan_block_byte_equal" skills/auto-writing-plans/SKILL.md

# F6 — CLAUDE.md
grep -n "implementer-prompt + reorder-prompt + plan_byte_check 결합" CLAUDE.md

# F7 — og-* unchanged
git diff HEAD -- skills/og-brainstorming/ skills/og-designing-direction/ skills/og-subagent-driven-development/

# F8 — executing-plans (inline) unchanged
git diff HEAD -- skills/executing-plans/SKILL.md
```

Expected:
- F1~F6: 모두 ≥1 매치
- F7: empty diff
- F8: empty diff

- [ ] **Step 2: pytest 통합**

```bash
source .venv/bin/activate && pytest -v
```

Expected: ALL PASS (기존 50 + G1 6 + G2 2 + G3 2 = 60).

- [ ] **Step 3: Fixture 디렉토리 검증**

```bash
ls -la skills/js-super-subagent-driven-development/tests/H11-user-edit-reorder/
```

Expected: README.md 존재.

- [ ] **Step 4: 검증 결과 [검증] entry append (change-history)**

manual entry append (variable changelog buffer 가 잡지 못하는 검증 task):

```bash
TIMESTAMP=$(date "+%Y-%m-%d %H:%M")
CH_ID=$(source .venv/bin/activate && python -c "from datetime import date; from pathlib import Path; from scripts.change_id import next_change_id; print(next_change_id(Path('docs/features/2026-05-10-v2.0.0-implementer-byte-copy'), date.today()))")
# Append [검증] entry to plan 변경이력
```

Entry 본문 (메인이 직접 Edit):

```
### [<TIMESTAMP>] [검증] (task: Task 12 — F1~F8 grep + pytest 통합)
- **id**: <CH_ID>
- **이유**: 정적 grep + pytest 통합으로 v2.0.0 + v1.1.16 변경 검증
- **무엇이**: F1~F8 grep / pytest 60 PASS / H11 fixture 존재
- **결과**: PASS — 모두 통과 (또는 FAIL 시 상세)
```

---

### Task 13: 6 manifest 버전 bump 1.1.17 → 2.0.0

**Files:**
- Modify: `.claude-plugin/plugin.json`, `marketplace.json`, `package.json`, `.codex-plugin/plugin.json`, `.cursor-plugin/plugin.json`, `gemini-extension.json`

**Model**: haiku

- [ ] **Step 1: Run bump-version script**

```bash
bash scripts/bump-version.sh 2.0.0
```

- [ ] **Step 2: Verify all 6 manifests synced**

```bash
grep -E '"version":\s*"2.0.0"' .claude-plugin/plugin.json marketplace.json package.json .codex-plugin/plugin.json .cursor-plugin/plugin.json gemini-extension.json
```

Expected: 6 매치 (모두 v2.0.0).

---

### Task 14: HANDOFF.md 갱신 (local only — gitignored)

**Files:**
- Modify: `HANDOFF.md`

**Model**: sonnet

- [ ] **Step 1: Read current HANDOFF**

```bash
head -80 HANDOFF.md
```

- [ ] **Step 2: Update with v2.0.0 release summary**

수정 항목:
- 마지막 갱신 line → "v2.0.0 push + tag 완료"
- 한 줄 요약 → v2.0.0 항목 추가 (24번)
- Current Progress 섹션 → v2.0.0 산출물
- Goal items 24 추가
- git status / push 워크플로우 정리
- Backlog status — v2.0.0 archive (was pending)
- Backlog status — v1.1.16 archive (FR로 통합)

(자세한 본문은 implementer 가 메모리/HANDOFF 본문 보고 작성)

- [ ] **Step 3: Verify (no commit — gitignored)**

```bash
git status HANDOFF.md
```

Expected: HANDOFF.md 가 gitignored 라 git status 안 노출. 정상.

---

## 2. 위험 코드 지점

- `skills/js-super-subagent-driven-development/implementer-prompt.md:6,49-57,135-145` — **breaking** (R1, R3): model 핀 변경 + Status enum + STRICT BYTE-COPY rule. mitigation: H11 fixture + 메이저 bump
- `skills/js-super-subagent-driven-development/reorder-prompt.md` (신규) — **breaking** (R1): 신규 dispatch path. mitigation: F2 grep + H11 dogfood
- `scripts/plan_byte_check.py` (신규) — **side-effect** (R2, R4): writing-plans 마찰 ↑ — plan 저장 거부 가능. mitigation: 명확한 Mismatch 메시지 + G1 unit test
- `skills/writing-plans/SKILL.md` Self-Review section — **side-effect** (R2): plan_byte_check 호출 추가. mitigation: F4 grep
- `skills/auto-writing-plans/SKILL.md` Step 4.5 — **side-effect** (R2): 동일. mitigation: F5 grep
- `skills/js-super-subagent-driven-development/SKILL.md` W-2 — **breaking** (R1): Stage 1/2/3 분기. mitigation: F3 grep + H11
- `scripts/dag_builder.py:62-72` — **side-effect** (R5): build_waves indegree 갱신 fix. mitigation: G2 신규 strict deps test
- `scripts/preflight.py:31-35` — **side-effect** (R6): regex split → rsplit. mitigation: G3 신규 code block test
- `CLAUDE.md` 결합 메모 — **side-effect** (R1 doc): atomic 변경 룰 가이드. mitigation: F6 grep
- 다중 Edit 부분 실패 — **race** (R7): wave finalization git checkout rollback (기존). mitigation: failure isolation 패턴 재활용

## 3. 롤백 전략

- **Code**: revert commits SHA-A..SHA-N (per-task commits via wave finalization). 각 task 단위 atomic commit 이라 부분 revert 가능.
- **Subagent prompt**: implementer-prompt 와 reorder-prompt + SKILL.md W-2 분기는 atomic 으로 묶어 revert (한 wave 안 commit 들).
- **plan_byte_check 차단**: 사용자가 helper 일시 우회 원할 시 writing-plans 본문에 추가된 호출 스킵 가능 (env var 또는 주석 토글). 본 release 미포함, 향후 v2.0.X 후속.
- **DAG fix**: 단일 file fix + test. 기존 plan 영향 가능 (wave 구조 변경) → release notes 안내.
- **manifest bump**: revert 시 6 manifest 동시 (`bash scripts/bump-version.sh 1.1.17`).

---

## 변경이력

<!-- change-history skill auto-appends entries here, oldest first -->

### [2026-05-10 19:00] [구현계획서-수정]
- **id**: CH-20260510-003
- **이유**: 신규 구현계획서 (v2.0.0 메이저 — 14 task / 8 wave / TDD bite-sized + Before/After 컨벤션)
- **무엇이**: v2.0.0-implementer-byte-copy-implementation-plan.md 전체 (Task 1~14, §2 위험 코드 지점 R1~R11 mapping, §3 롤백 전략)
- **영향범위**: 없음 (최초 생성)
- **연관 항목**: CH-20260510-001 (PRD), CH-20260510-002 (tech-design)

### [2026-05-10 19:20] [코드-수정] (batch: tasks 1..14)
- **id**: CH-20260510-004
- **이유**: 서브에이전트 모드 task batch 종합 (end-of-run consolidation)
- **무엇이**: .claude-plugin/marketplace.json, .claude-plugin/plugin.json, .codex-plugin/plugin.json, .cursor-plugin/plugin.json, CLAUDE.md, gemini-extension.json, package.json, scripts/dag_builder.py, scripts/plan_byte_check.py, scripts/preflight.py, scripts/tests/test_dag_builder.py, scripts/tests/test_plan_byte_check.py, scripts/tests/test_preflight.py, skills/auto-writing-plans/SKILL.md, skills/js-super-subagent-driven-development/SKILL.md, skills/js-super-subagent-driven-development/implementer-prompt.md, skills/js-super-subagent-driven-development/reorder-prompt.md, skills/js-super-subagent-driven-development/tests/H11-user-edit-reorder/README.md, skills/js-super-subagent-driven-development/tests/README.md, skills/writing-plans/SKILL.md
- **영향범위**: 누적 (task별 세부 참조)
- **위험 카테고리**: breaking, side-effect
- **task별 세부 (14건)**:
  - Task 1: `scripts/dag_builder.py:62-74` — fix indegree decrement scope to placed_this_wave (`side-effect`) — commits: 
  - Task 1: `scripts/tests/test_dag_builder.py:append` — add 2 strict deps ordering tests (`none`) — commits: 
  - Task 2: `scripts/preflight.py:31-35` — split → rsplit so footer detection picks last occurrence (`side-effect`) — commits: 
  - Task 2: `scripts/tests/test_preflight.py:append` — add 2 code block false-positive tests (`none`) — commits: 
  - Task 3: `scripts/plan_byte_check.py:1-100` — new helper for plan **원본** byte-equal check (`none`) — commits: 
  - Task 3: `scripts/tests/test_plan_byte_check.py:1-100` — 6 unit tests (`none`) — commits: 
  - Task 4: `skills/js-super-subagent-driven-development/implementer-prompt.md:6-9,49-57,135-145` — haiku fixed + STRICT BYTE-COPY section + Status enum extension (`breaking`) — commits: 
  - Task 5: `skills/js-super-subagent-driven-development/reorder-prompt.md:1-90` — new prompt for reorder/resolve subagent (sonnet, conditional) (`breaking`) — commits: 
  - Task 6: `skills/js-super-subagent-driven-development/SKILL.md:W-2/W-3 boundary` — add v2.0.0+ Stage 1/2/3 dispatch flow (`breaking`) — commits: 
  - Task 7: `skills/writing-plans/SKILL.md:Self-Review section` — add plan_byte_check helper bash one-liner (`side-effect`) — commits: 
  - Task 8: `skills/auto-writing-plans/SKILL.md:Step 4.5` — add auto plan_byte_check 3-retry between Step 4 and Step 5 (`side-effect`) — commits: 
  - Task 9: `skills/js-super-subagent-driven-development/tests/H11-user-edit-reorder/README.md:1-50` — new H11 fixture README — user mid-flight edit + reorder dispatch scenario (`none`) — commits: 
  - Task 10: `skills/js-super-subagent-driven-development/tests/README.md:append` — add v2.0.0+ section with H11 row (`none`) — commits: 
  - Task 11: `CLAUDE.md:append` — new section: implementer-prompt + reorder-prompt + plan_byte_check 결합 (v2.0.0+) (`none`) — commits: 
  - Task 13: `package.json:-` — bump version 1.1.17 → 2.0.0 (`none`) — commits: 
  - Task 13: `.claude-plugin/plugin.json:-` — bump (`none`) — commits: 
  - Task 13: `.claude-plugin/marketplace.json:-` — bump (`none`) — commits: 
  - Task 13: `.cursor-plugin/plugin.json:-` — bump (`none`) — commits: 
  - Task 13: `.codex-plugin/plugin.json:-` — bump (`none`) — commits: 
  - Task 13: `gemini-extension.json:-` — bump (`none`) — commits: 
- **연관 commits**: 
- **변경 전/후 코드**: 생략 — `git show <SHA>` 로 조회


### [2026-05-10 19:20] [검증] (task: Task 12 — F1~F8 grep + pytest 통합)
- **id**: CH-20260510-005
- **이유**: 정적 grep + pytest 로 v2.0.0 + v1.1.16 변경 검증
- **무엇이**: F1~F6 grep (모두 ≥1 매치) / F7 og-* unchanged (0 lines) / F8 executing-plans unchanged (0 lines) / pytest 60 PASS / H11 fixture 존재
- **결과**: PASS — 모두 통과
