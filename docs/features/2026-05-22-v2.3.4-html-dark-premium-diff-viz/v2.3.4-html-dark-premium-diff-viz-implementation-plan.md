---
slug: v2.3.4-html-dark-premium-diff-viz
commit_policy: per-task
total_tasks: 3
---

# v2.3.4 — generating-html `.html` 산출물 룰 강화 (구현계획)

**연관**: `v2.3.4-html-dark-premium-diff-viz-requirements.md` (CH-001) / `v2.3.4-html-dark-premium-diff-viz-tech-design.md` (CH-002)

## §1 작업 단위 (3 task)

Same-file mechanical 묶음 검토:

- Task 1 단독 (`html-companion-prompt.md` 단일 파일) — 묶음 N/A
- Task 2 다중 파일 (6 manifest) 이지만 mechanical version bump + 통합 검증 (release sanity) → 1 task multi-step 으로 묶음
- Task 3 별도 (백로그 mv, 다른 디렉토리)

## §2 위험 코드 지점

| R-id (tech-design §6) | file:line | mitigation |
|---|---|---|
| R-1 톤 자유 가치 손상 | `skills/generating-html/html-companion-prompt.md` (Dark premium 섹션) | "다크 프리미엄 안에서 variety 보존" 명시 + 6 톤 inspiration sub-variation 으로 흡수 |
| R-2 다크 프리미엄 LLM 해석 모호 | 동 (Dark premium 섹션) | raw hex 예시 + Anti-Pattern 명시 + WCAG AA |
| R-3 4 요소 의무화 | 동 (4-element 섹션) | "콘텐츠와 안 맞으면 사용 X" + Anti-Pattern |
| R-4 WCAG AA 자동 검증 부재 | 동 (Dark premium 섹션) | subagent self-check + Anti-Pattern muted 4.5:1 미만 |
| R-5 C diff 의역 | 동 (Impl-plan diff 섹션) | Anti-Pattern word-level / semantic 금지 |
| R-6 C raw blocks 모호 | 동 (Impl-plan diff 섹션 C-3) | LLM 자율 + Anti-Pattern context 강제 collapse 금지 |
| R-7 변경이력 생략 자율 일관성 | 동 (Change-history footer 섹션 A-1) | subagent B 자율 + 푸터 안내 한 줄 |
| R-8 v2.3.3 ↔ C-2 충돌 | 동 (Impl-plan diff 섹션 C-2) | code raw secondary 명시 + 변경 라인 항상 visible |
| R-9 v2.0.0 byte-copy 회귀 | 동 (Impl-plan diff 섹션 본문) | `.md` 원본 무변형 명시 + writing-plans / plan_byte_check.py 영향 0 |
| R-10 라이트 톤 override 부재 | 동 (Dark premium 섹션 끝) | `prefers-color-scheme: light` 분기 옵셔널 + Non-goal carry-over |

---

## §3 Task 명세

### Task 1 — `html-companion-prompt.md` 룰 8개 + Anti-Patterns 15개 추가

**Files**: `skills/generating-html/html-companion-prompt.md`
**Model**: sonnet
**Risk**: side-effect (사용자 `.html` 결과물 변화 — 다크 프리미엄 baseline + diff 시각화 + 변경이력 옵셔널 생략)

**원본** (`skills/generating-html/html-companion-prompt.md:165-165`):

```
You have one job: make a sibling `.html` that humans say "**wow**" the moment they open it — while preserving every word, every decision, every number 1:1.
```

**수정 후** (`skills/generating-html/html-companion-prompt.md:165-?`):

```
# Dark premium default (v2.3.4+)

**비주얼 테마 = "다크 프리미엄"** 이 모든 `.html` 산출물의 default. v2.2.4 의 6 톤 자유 (editorial / docs portal / playful / brutalist / experimental / dashboard) 는 이제 다크 프리미엄의 sub-variation 으로 흡수 — baseline 톤만 통일, variety 는 보존.

**팔레트 예시** (정확 값 X — LLM 자율 선택):

- **배경 (dark base)**: `#0a0b0f` / `#0d1117` / `#111218` 류 (깊은 어두운 톤)
- **본문 텍스트 (high contrast)**: `#e6edf3` / `#f0f6fc` / `#e8eaf0` 류 — muted 도 `#8b949e` 보다 어둡지 않게
- **악센트 (premium)**: 보라/시안/핑크 그라데이션 또는 단색 강조 — 매 호출 다르게
- **카드 / 표면**: 반투명 흰색 overlay (`rgba(255,255,255,0.04~0.07)`) — glassmorphism 깊이감
- **그림자 / glow**: subtle drop shadow + accent glow (`rgba(124,109,255,0.25)` 류)
- **타이포**: heading bold + body 1.6~1.7 line-height + 한글 친화 폰트 스택

"Premium dashboard / 고급 분석 도구" 톤. 사무 / 회의록 / 흰 배경 editorial 톤 금지.

라이트 모드는 `prefers-color-scheme: light` 분기로만 옵셔널 제공 (default 가 dark, OS 설정 따라 명시적 라이트).

## 4 요소 옵셔널 (v2.3.4+)

산출물에 포함 **가능** 한 요소 (모두 옵셔널 — 본문 콘텐츠에 안 맞으면 사용 X):

| 요소 | 적용 케이스 |
|---|---|
| **데이터 차트 / 그래프** | 숫자 비교 / 분포 / 시계열 / 비율 데이터 |
| **인터랙티브 요소** | TOC / dark-toggle / copy 버튼 / smooth scroll / 보조 disclosure (primary 본문 X) |
| **다이어그램 / 플로우** | 아키텍처 / 데이터 흐름 / DAG / 컴포넌트 관계 |
| **핵심 요약 카드** | FR / 결정 / 위험 등 묶음 형태로 일관 표현 가능할 때 |

4 요소 모두 적용해야 한다는 강제 X. LLM 이 콘텐츠 보고 판단.

## WCAG AA baseline (v2.3.4+)

다크 배경에서도 모든 텍스트 (muted 포함) contrast 4.5:1 이상 보장. 자동 검증 도구는 후속 후보 (현 버전 self-check).

---

# Impl-plan diff visualization (v2.3.4+)

`impl-plan` 류 산출물에서 `**원본**` + `**수정본**` 쌍 (또는 동등한 before/after 코드 블록 쌍) 을 감지하면, `.html` 에서 **unified diff 시각화** 로 렌더:

| 라인 종류 | 시각 표현 |
|---|---|
| **추가 라인** (수정본에만 존재) | 초록 배경 (`rgba(46,160,67,0.15)` 류) + 좌측 `+` 마커 + 초록 텍스트 톤 |
| **삭제 라인** (원본에만 존재) | 빨강 배경 (`rgba(248,81,73,0.15)` 류) + 좌측 `-` 마커 + 빨강 텍스트 톤 |
| **변경 X 라인** (양쪽 동일) | context — 기본 톤, 좌측 공백 마커 |

GitHub dark 류 톤 사용. WCAG AA contrast 4.5:1 보존 (다크 프리미엄 정합).

## Context 라인 진보적 공개 (C-2)

- 변경 라인 ±N (기본 3) 만 default-visible
- 그 외 unchanged 영역은 `"... unchanged N lines ..."` 형태로 collapse OK (`<details>` 사용 OK)
- v2.3.3 "primary content default-visible" 룰의 **예외** (코드 raw 는 secondary 성격) — 단 변경 라인 자체는 항상 default-visible

## Raw 원본/수정본 병행 옵셔널 (C-3)

LLM 자율 — 두 raw 블록을 collapse 형태로 같이 제공 OK. 단:

- raw 블록 default-collapsed (`<details>`) OK
- diff 시각화가 primary, raw 는 보조

## 신규 / 삭제 파일 fallback (C-4)

- 신규 파일 (원본 X / 수정본 only) → 단순 코드 블록 (초록 배경 전체 적용 OK)
- 삭제 파일 (원본 only / 수정본 X) → 단순 코드 블록 (빨강 배경 전체 적용 OK)
- 진짜 before/after 쌍일 때만 unified diff 시각화

self-contained inline (외부 diff lib 금지 — diff2html CDN 등). semantic / word-level / character-level diff 금지 — line-level 단순만.

---

# Change-history footer omission (v2.3.4+)

`## 변경이력` 섹션 (또는 동등 footer) 은 `.html` 에서 **생략 OK** (사람 가독성 우선). 단:

- 생략 시 `.html` 푸터 한 줄 안내: `"변경이력은 .md 원본 참조"` (또는 동등 문구)
- 생략 vs 노출은 subagent B 자율 판단 (정보 hierarchy 기준 — entry 수 / 정보 밀도 등)

기존 의미 1:1 보존 룰의 변경이력 footer 한정 예외. FR / AC / 배경 / 결정 / NFR / Non-goals / 미정 등 본문 콘텐츠는 그대로 1:1 보존 (v2.3.3 primary visibility 정합).

---

# Anti-Patterns (v2.3.4 추가)

- **Default 흰 배경 (`#fff` / `#f8f9fa` 류) + 회색 muted 본문** (v2.3.4+) — 금지. 신고 사례 직격. 한글 가독성 망가짐.
- **`prefers-color-scheme: dark` 만 dark 분기 (default light)** (v2.3.4+) — 금지. OS 설정 의존. default 가 dark 여야 함.
- **6 톤 (editorial / playful / brutalist 등) 자유 baseline 선택** (v2.3.4+) — 금지. 다크 프리미엄 1 톤 baseline 강제. sub-variation 으로만 흡수.
- **4 요소 모두 의무 사용** (v2.3.4+) — 금지. 4 요소는 옵셔널. 콘텐츠와 안 맞으면 사용 X.
- **muted 텍스트 contrast 4.5:1 미만 (WCAG AA)** (v2.3.4+) — 금지. dark 배경 위 너무 어두운 회색 (`#6c757d` 류) 금지.
- **원본 / 수정본 두 블록 별개 렌더 (diff 시각화 X)** (v2.3.4+) — 금지. C-1 룰 위반. 검토 효율 ↓.
- **diff 시각화에 외부 라이브러리 의존 (e.g. diff2html CDN)** (v2.3.4+) — 금지. self-contained 룰 위반. inline CSS / SVG 만.
- **word-level / character-level diff 적용** (v2.3.4+) — 금지. 의역/오류 위험. line-level 단순만.
- **semantic diff (의미 변경 요약 / hint 텍스트)** (v2.3.4+) — 금지. 의미 1:1 룰 위반. 해석은 plan 본문에 맡김.
- **diff 라인 색상 contrast 4.5:1 미만 (dark 배경 기준)** (v2.3.4+) — 금지. WCAG AA 와 충돌.
- **context 라인 collapse 강제 (전부 hide)** (v2.3.4+) — 금지. 변경 라인 ±N 은 default-visible 보장.
- **신규 / 삭제 파일도 억지 unified diff 형식** (v2.3.4+) — 금지. C-4 fallback 위반. 단순 코드 블록 OK.
- **변경이력 entry 본문을 `.html` 에 그대로 1:1 렌더 강제** (v2.3.4+) — 금지. A-1 룰로 생략 허용.
- **변경이력 footer 만 의미 변형 / 요약** (v2.3.4+) — 금지. 생략은 OK, 변형은 X. 의미 1:1 보존.
- **변경이력 외 본문 콘텐츠도 같이 생략** (v2.3.4+) — 금지. A-1 은 변경이력 footer 한정. FR / AC 본문 생략은 v2.3.3 위반.

---

You have one job: make a sibling `.html` that humans say "**wow**" the moment they open it — while preserving every word, every decision, every number 1:1.
```

**TDD Steps**:

1. Edit — 위 byte-copy 패턴 적용 (마지막 라인 `You have one job: ...` 보존 + 위에 신규 본문 append). 라인 165 가 마지막 EOF 라인.
2. 회귀 grep 검증:
   ```bash
   grep -c "Dark premium default" skills/generating-html/html-companion-prompt.md  # ≥ 1
   grep -c "Impl-plan diff visualization" skills/generating-html/html-companion-prompt.md  # ≥ 1
   grep -c "Change-history footer omission" skills/generating-html/html-companion-prompt.md  # ≥ 1
   grep -c "v2.3.4+" skills/generating-html/html-companion-prompt.md  # ≥ 17 (룰 + Anti-Pattern 라인 합)
   grep -c "diff2html\|CDN" skills/generating-html/html-companion-prompt.md  # ≥ 1 (Anti-Pattern catch)
   grep -c "word-level\|character-level\|semantic diff" skills/generating-html/html-companion-prompt.md  # ≥ 1
   ```
3. Self-review — primary content 가림 룰 v2.3.3 보존 (`grep -c "Primary content default-visible" skills/generating-html/html-companion-prompt.md` ≥ 1)

---

### Task 2 — 6 manifest version bump 2.3.3 → 2.3.4

**Files** (6):

- `gemini-extension.json`
- `package.json`
- `.cursor-plugin/plugin.json`
- `.claude-plugin/marketplace.json`
- `.claude-plugin/plugin.json`
- `.codex-plugin/plugin.json`

**Model**: haiku (mechanical version bump)
**Risk**: none

**원본** (`gemini-extension.json:4-4`):

```
  "version": "2.3.3",
```

**수정 후** (`gemini-extension.json:4-4`):

```
  "version": "2.3.4",
```

**원본** (`package.json:3-3`):

```
  "version": "2.3.3",
```

**수정 후** (`package.json:3-3`):

```
  "version": "2.3.4",
```

**원본** (`.cursor-plugin/plugin.json:5-5`):

```
  "version": "2.3.3",
```

**수정 후** (`.cursor-plugin/plugin.json:5-5`):

```
  "version": "2.3.4",
```

**원본** (`.claude-plugin/marketplace.json:13-13`):

```
      "version": "2.3.3",
```

**수정 후** (`.claude-plugin/marketplace.json:13-13`):

```
      "version": "2.3.4",
```

**원본** (`.claude-plugin/plugin.json:4-4`):

```
  "version": "2.3.3",
```

**수정 후** (`.claude-plugin/plugin.json:4-4`):

```
  "version": "2.3.4",
```

**원본** (`.codex-plugin/plugin.json:3-3`):

```
  "version": "2.3.3",
```

**수정 후** (`.codex-plugin/plugin.json:3-3`):

```
  "version": "2.3.4",
```

**TDD Steps**:

1. 6 Edit (각 manifest 의 version 라인) — byte-copy 적용
2. 검증:
   ```bash
   grep -l '"2\.3\.3"' gemini-extension.json package.json .cursor-plugin/plugin.json .claude-plugin/marketplace.json .claude-plugin/plugin.json .codex-plugin/plugin.json
   # expected: empty (2.3.3 잔존 0)
   grep -c '"2\.3\.4"' gemini-extension.json package.json .cursor-plugin/plugin.json .claude-plugin/marketplace.json .claude-plugin/plugin.json .codex-plugin/plugin.json
   # expected: ≥ 1 per file
   ```

---

### Task 3 — 백로그 shipped/ 이관

**Files**:

- `docs/backlog/v2.3.4-html-omit-change-history.md` → `docs/backlog/shipped/v2.3.4-html-omit-change-history.md`

**Model**: haiku
**Risk**: none

**TDD Steps**:

1. `ls docs/backlog/shipped/` 디렉토리 존재 확인 (없으면 mkdir)
2. `git mv docs/backlog/v2.3.4-html-omit-change-history.md docs/backlog/shipped/`
3. 검증:
   ```bash
   test ! -f docs/backlog/v2.3.4-html-omit-change-history.md  # 원위치 부재
   test -f docs/backlog/shipped/v2.3.4-html-omit-change-history.md  # shipped/ 존재
   ```

Byte-copy 블록 N/A (파일 이동만 — `.md` 본문 무변형).

---

## §4 commit 정책 + 변경이력

`commit_policy: per-task`. 각 task 별 1 commit + 끝 일괄 `[코드-수정] (batch: tasks 1..3)` consolidated entry (End-of-Run Consolidator 룰 — `commit_policy: per-task` mode 의 slim form).

**병렬 wave**: 3 task 모두 독립 (dependency 없음). subagent dispatch 시:

- Wave 1: Task 1 (sonnet, html-companion-prompt.md) + Task 2 (haiku, 6 manifest) + Task 3 (haiku, mv)
- 또는 메인 직접 처리 (v2.3.0~v2.3.3 답습 — quick patch 라 메인 직접 적용 적절)

Wave-parallel dispatch 결정은 `auto-executing-plans` 가 메인 직접 실행 vs subagent dispatch 의 trade-off 판단 (Gate #14 override 자동승인).

## §5 Acceptance

- Task 1: html-companion-prompt.md 의 신규 3 섹션 (Dark premium / Impl-plan diff / Change-history footer omission) + Anti-Patterns 15 라인 추가 — line 165 의 마지막 sentence 보존
- Task 2: 6 manifest 모두 `"2.3.4"`. `"2.3.3"` 잔존 0
- Task 3: 백로그 `shipped/` 이관 완료
- 메인 push: v2.3.4 tag + marketplace update + reload-plugins (사용자 직접 — `git push origin main --tags` 안내)
- 회귀 catch: §6.1 grep 7개 (tech-design) 모두 통과
- 영향 0 보장: writing-plans / executing-plans / js-super-sub-driven / plan_byte_check.py / auto-* / og-* / preflight.py / change-history / change-propagation `git diff HEAD~3 HEAD --` empty

## 변경이력

### [2026-05-22 11:32] [구현계획서-수정]
- **id**: CH-20260522-003
- **이유**: v2.3.4 patch — `html-companion-prompt.md` 룰 8개 + Anti-Patterns 15개 추가 + 6 manifest version bump + 백로그 shipped/ 이관 atomic patch 작업 단위 분해. `commit_policy: per-task`. 3 task (Task 1 sonnet / Task 2 haiku multi-step / Task 3 haiku). Same-file mechanical 묶음 룰 검토 — Task 2 의 6 manifest 가 다른 파일이지만 mechanical version bump 통합 검증으로 묶음 (3 조건 중 같은 파일 X 이나 mechanical + 통합 검증 정합).
- **무엇이**: 신규 `v2.3.4-html-dark-premium-diff-viz-implementation-plan.md` 작성 — frontmatter (commit_policy per-task / total_tasks 3) + §1 작업 단위 + §2 R-1~R-10 ↔ file:line 매핑 + §3 Task 1/2/3 명세 (byte-copy `**원본**` + `**수정 후**` 블록 7개) + §4 commit 정책 + §5 Acceptance.
- **영향범위**: 단일 파일 변경 (`skills/generating-html/html-companion-prompt.md`) + 6 manifest bump + 백로그 mv. CLAUDE.md 결합 메모는 v2.3.0 catch 답습으로 추가 X (단일 파일 patch 라 명분 약함). 다른 skill / scripts 영향 0. plan_byte_check 7 blocks PASS — Task 1 원본 (line 165 EOF marker 1 라인) + Task 2 원본 6 (각 manifest version 라인).
- **연관 항목**: CH-20260522-001, CH-20260522-002
