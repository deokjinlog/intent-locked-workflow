# v2.0.3 AskUserQuestion 도구 강제 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or js-super:og-executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 4 js-super 신규 skill (`brainstorming` / `designing-direction` / `writing-plans` / `executing-plans`) + 4 auto-* 변형 skill 본문 상단에 "사용자 질문 룰 (v2.0.3+)" boilerplate 추가 + CLAUDE.md 결합 메모 + 6 manifest bump 2.0.2 → 2.0.3.

**Architecture:** skill body 상단 section ("# 제목" 후 / "<HARD-GATE>" 또는 "## Checklist" 전) 에 `## 사용자 질문 룰 (v2.0.3+)` markdown 섹션 삽입. self-contained mirror 패턴. CLAUDE.md 결합 메모 1건이 8 skill atomic 동기 룰 보장.

**Tech Stack:** Markdown (skill bodies + CLAUDE.md). JSON (6 manifests). Bash (version bump + grep verification). Python (pytest 회귀).

---

## File Structure

**8 skill body** (boilerplate 추가):
- `skills/brainstorming/SKILL.md`
- `skills/designing-direction/SKILL.md`
- `skills/writing-plans/SKILL.md`
- `skills/executing-plans/SKILL.md`
- `skills/auto-brainstorming/SKILL.md`
- `skills/auto-designing-direction/SKILL.md`
- `skills/auto-writing-plans/SKILL.md`
- `skills/auto-executing-plans/SKILL.md`

**부속**:
- `CLAUDE.md` — "## 8 skill AskUserQuestion 강제 룰 결합 (v2.0.3+)" 신규 섹션
- 6 manifest — `.claude-plugin/plugin.json` / `.claude-plugin/marketplace.json` / `package.json` / `.codex-plugin/plugin.json` / `.cursor-plugin/plugin.json` / `gemini-extension.json`
- HANDOFF.md (local only, gitignored)

**Immutable**:
- og-* 3 skill — upstream mirror 보존
- `skills/js-super-sub-driven/` — execution skill, 사용자 질문 거의 없음

---

## Boilerplate 본문 (모든 8 skill 에 동일 내용 삽입)

````markdown
## 사용자 질문 룰 (v2.0.3+) — 항상 AskUserQuestion

이 skill 흐름 안에서 사용자에게 질문할 일이 생기면 **반드시** `AskUserQuestion`
도구로 호출한다. 산문으로 "~ 할까요?" 한 줄 던지지 마라.

### Why

Notification 훅 (`elicitation_dialog` 매처) 이 알람을 발화하려면 도구 호출이
실제로 일어나야 함. 산문 질문은 훅이 못 잡아서 사용자가 놓침 (v1.1.8 신고 재발).

### How to apply

- clarifying / Socratic / 모호점 확인 / 게이트 / 모드 선택 — 모두 포함
- 단답 yes/no 도 prose X → `AskUserQuestion` choices `[yes, no]` 사용
- 다중 선택은 enum choices 또는 multi-question batching (의미 결합 시 max 4 questions[])
- **Socratic 자유 응답**: AskUserQuestion 의 question 본문에 "자유롭게 답해주세요. 별도 옵션 선택 불필요" + dummy choice `[알겠음]` 1개 → 트리거만 발화, 응답은 다음 turn prose
- **예외**: 본문 자체의 알람-friendly 안내문 (`ℹ️ Auto-proceeding ...`) 는 질문 아니라 안내 — 도구 호출 불필요
````

삽입 위치: 각 skill body 의 "# 제목" 라인 직후 (`**Announce at start:**` 라인 후, `<HARD-GATE>` 또는 `## Checklist` 전).

---

## Task 1: 4 js-super 신규 skill body 에 boilerplate 추가

**Files:**
- Modify: `skills/brainstorming/SKILL.md` (boilerplate 추가)
- Modify: `skills/designing-direction/SKILL.md`
- Modify: `skills/writing-plans/SKILL.md`
- Modify: `skills/executing-plans/SKILL.md`

- [ ] **Step 1: Write failing tests (grep — boilerplate 부재 확인)**

```bash
grep -c "사용자 질문 룰 (v2.0.3+)" skills/brainstorming/SKILL.md skills/designing-direction/SKILL.md skills/writing-plans/SKILL.md skills/executing-plans/SKILL.md
```

Expected: 0 / 0 / 0 / 0

- [ ] **Step 2: 각 skill body Read 로 삽입 위치 catch + Edit insertion**

각 skill 의 `**Announce at start:**` 라인 직후 (또는 첫 H1 후 적절한 위치) 에 다음 boilerplate 를 Edit 으로 insert:

````markdown
## 사용자 질문 룰 (v2.0.3+) — 항상 AskUserQuestion

이 skill 흐름 안에서 사용자에게 질문할 일이 생기면 **반드시** `AskUserQuestion`
도구로 호출한다. 산문으로 "~ 할까요?" 한 줄 던지지 마라.

### Why

Notification 훅 (`elicitation_dialog` 매처) 이 알람을 발화하려면 도구 호출이
실제로 일어나야 함. 산문 질문은 훅이 못 잡아서 사용자가 놓침 (v1.1.8 신고 재발).

### How to apply

- clarifying / Socratic / 모호점 확인 / 게이트 / 모드 선택 — 모두 포함
- 단답 yes/no 도 prose X → `AskUserQuestion` choices `[yes, no]` 사용
- 다중 선택은 enum choices 또는 multi-question batching (의미 결합 시 max 4 questions[])
- **Socratic 자유 응답**: AskUserQuestion 의 question 본문에 "자유롭게 답해주세요. 별도 옵션 선택 불필요" + dummy choice `[알겠음]` 1개 → 트리거만 발화, 응답은 다음 turn prose
- **예외**: 본문 자체의 알람-friendly 안내문 (`ℹ️ Auto-proceeding ...`) 는 질문 아니라 안내 — 도구 호출 불필요
````

- [ ] **Step 3: Run test (grep — boilerplate 존재 확인)**

```bash
grep -c "사용자 질문 룰 (v2.0.3+)" skills/brainstorming/SKILL.md skills/designing-direction/SKILL.md skills/writing-plans/SKILL.md skills/executing-plans/SKILL.md
```

Expected: 1 / 1 / 1 / 1

- [ ] **Step 4: Commit**

```bash
git add skills/brainstorming/SKILL.md skills/designing-direction/SKILL.md skills/writing-plans/SKILL.md skills/executing-plans/SKILL.md
git commit -m "feat(v2.0.3): 4 js-super 신규 skill body 에 AskUserQuestion 강제 boilerplate 추가"
```

---

## Task 2: 4 auto-* skill body 에 boilerplate 추가

**Files:**
- Modify: `skills/auto-brainstorming/SKILL.md`
- Modify: `skills/auto-designing-direction/SKILL.md`
- Modify: `skills/auto-writing-plans/SKILL.md`
- Modify: `skills/auto-executing-plans/SKILL.md`

- [ ] **Step 1: Write failing tests (grep)**

```bash
grep -c "사용자 질문 룰 (v2.0.3+)" skills/auto-brainstorming/SKILL.md skills/auto-designing-direction/SKILL.md skills/auto-writing-plans/SKILL.md skills/auto-executing-plans/SKILL.md
```

Expected: 0 / 0 / 0 / 0

- [ ] **Step 2: Each Edit insertion**

각 auto-* skill body 의 `**Announce at start:**` 라인 직후 (또는 첫 H1 후 적절한 위치) 에 Task 1 Step 2 의 동일 boilerplate 삽입.

- [ ] **Step 3: Run test (grep after)**

```bash
grep -c "사용자 질문 룰 (v2.0.3+)" skills/auto-brainstorming/SKILL.md skills/auto-designing-direction/SKILL.md skills/auto-writing-plans/SKILL.md skills/auto-executing-plans/SKILL.md
```

Expected: 1 / 1 / 1 / 1

- [ ] **Step 4: Commit**

```bash
git add skills/auto-brainstorming/SKILL.md skills/auto-designing-direction/SKILL.md skills/auto-writing-plans/SKILL.md skills/auto-executing-plans/SKILL.md
git commit -m "feat(v2.0.3): 4 auto-* skill body 에 AskUserQuestion 강제 boilerplate 추가"
```

---

## Task 3: CLAUDE.md 결합 메모 추가

**Files:**
- Modify: `CLAUDE.md` (신규 섹션 append)

- [ ] **Step 1: Write failing test (grep)**

```bash
grep -c "8 skill AskUserQuestion 강제 룰 결합 (v2.0.3+)" CLAUDE.md
```

Expected: 0

- [ ] **Step 2: Edit — CLAUDE.md 끝에 신규 섹션 append**

`CLAUDE.md` 의 마지막 결합 메모 섹션 (`## setting-up-worktrees ↔ commands/worktree.md 결합 (v2.0.2+)`) 뒤에 다음 섹션 append:

```markdown

## 8 skill AskUserQuestion 강제 룰 결합 (v2.0.3+)

v2.0.3+ 에서 4 js-super 신규 skill + 4 auto-* 변형 skill 의 사용자 질문 시
`AskUserQuestion` 도구 호출 강제. 8 skill body 의 "사용자 질문 룰 (v2.0.3+)"
boilerplate 가 동기. 변경 시 atomic patch.

### 회귀 패턴 (한 곳만 누락 시)

| 누락 | 증상 |
|---|---|
| skill body 한 곳 boilerplate 누락 | 그 skill 안 메인이 prose 질문 → 알람 X → 사용자 놓침 |
| AskUserQuestion 호출 부재 | elicitation_dialog 매처 미발화 |

### 영향 범위

- 8 skill body 변경. og-* / js-super-sub-driven / 보조 skill 영향 0
- AskUserQuestion 도구 schema 자체 변경 X (호출 빈도만 ↑)
- Notification.elicitation_dialog 매처 + repeat-alert.sh — 변경 X (기존 인프라 활용)

요약: 8 skill body + CLAUDE.md 결합 메모 변경은 묶어서 처리. 5+ 파일 atomic patch.
```

- [ ] **Step 3: Run test (grep after)**

```bash
grep -c "8 skill AskUserQuestion 강제 룰 결합 (v2.0.3+)" CLAUDE.md
```

Expected: 1

- [ ] **Step 4: Commit**

```bash
git add CLAUDE.md
git commit -m "docs(v2.0.3): CLAUDE.md 8 skill AskUserQuestion 강제 룰 결합 메모"
```

---

## Task 4: 검증 (정적 grep + pytest 회귀)

**Files:**
- None (verification only)

- [ ] **Step 1: 8 skill body boilerplate 존재 검증**

```bash
grep -c "사용자 질문 룰 (v2.0.3+)" \
  skills/brainstorming/SKILL.md \
  skills/designing-direction/SKILL.md \
  skills/writing-plans/SKILL.md \
  skills/executing-plans/SKILL.md \
  skills/auto-brainstorming/SKILL.md \
  skills/auto-designing-direction/SKILL.md \
  skills/auto-writing-plans/SKILL.md \
  skills/auto-executing-plans/SKILL.md
```

Expected: 1 each (총 8개 매치)

- [ ] **Step 2: AskUserQuestion 식별자 등장 카운트 (baseline 측정)**

```bash
grep -c "AskUserQuestion" \
  skills/brainstorming/SKILL.md \
  skills/designing-direction/SKILL.md \
  skills/writing-plans/SKILL.md \
  skills/executing-plans/SKILL.md \
  skills/auto-brainstorming/SKILL.md \
  skills/auto-designing-direction/SKILL.md \
  skills/auto-writing-plans/SKILL.md \
  skills/auto-executing-plans/SKILL.md
```

Expected: 각 ≥1 (모든 skill 본문에 boilerplate + 기존 게이트 호출 모두 합산)

- [ ] **Step 3: 산문 질문 패턴 잔재 catch (sanity check)**

```bash
grep -nE "할까요\?\s*—\s*yes\s*/\s*no|\?\s*—\s*yes\s*/\s*no" \
  skills/brainstorming/SKILL.md \
  skills/designing-direction/SKILL.md \
  skills/writing-plans/SKILL.md \
  skills/executing-plans/SKILL.md \
  skills/auto-*/SKILL.md 2>/dev/null | head -20
```

Expected: hits may exist in **Prose fallback** sections (intentional — for non-Claude-Code harnesses). 그 외 위치 잔재 없어야 함.

- [ ] **Step 4: CLAUDE.md 결합 메모 catch**

```bash
grep -c "8 skill AskUserQuestion 강제 룰 결합" CLAUDE.md
```

Expected: 1

- [ ] **Step 5: pytest 회귀**

```bash
source .venv/bin/activate && pytest -v --tb=short
```

Expected: 모든 case PASS (기존 61 baseline, 변경 X — skill body markdown 만 변경)

- [ ] **Step 6: No commit** (검증만)

---

## Task 5: 6 manifest bump 2.0.2 → 2.0.3

**Files:**
- Modify: `.claude-plugin/plugin.json`
- Modify: `.claude-plugin/marketplace.json`
- Modify: `package.json`
- Modify: `.codex-plugin/plugin.json`
- Modify: `.cursor-plugin/plugin.json`
- Modify: `gemini-extension.json`

- [ ] **Step 1: Write failing test (current version check)**

```bash
grep -h '"version"' .claude-plugin/plugin.json .claude-plugin/marketplace.json package.json .codex-plugin/plugin.json .cursor-plugin/plugin.json gemini-extension.json 2>&1 | grep -c '"2.0.2"'
```

Expected: ≥6 (6 manifest 모두 2.0.2 매치)

- [ ] **Step 2: Bump script 실행**

```bash
bash scripts/bump-version.sh 2.0.3
```

- [ ] **Step 3: Run test (grep after)**

```bash
grep -h '"version"' .claude-plugin/plugin.json .claude-plugin/marketplace.json package.json .codex-plugin/plugin.json .cursor-plugin/plugin.json gemini-extension.json 2>&1 | grep -c '"2.0.3"'
```

Expected: ≥6

- [ ] **Step 4: Commit**

```bash
git add .claude-plugin/plugin.json .claude-plugin/marketplace.json package.json .codex-plugin/plugin.json .cursor-plugin/plugin.json gemini-extension.json
git commit -m "chore: bump version 2.0.2 → 2.0.3"
```

---

## Task 6: HANDOFF.md 갱신 (local only, no commit)

**Files:**
- Modify: `HANDOFF.md` (gitignored)

- [ ] **Step 1: HANDOFF 갱신**

- 마지막 갱신 line → 2026-05-16 (v2.0.3 완료 — AskUserQuestion 강제 8 skill)
- Goal 26 → 완료 마킹 + 다음 후보
- Current Progress → v2.0.3 push 결과 + git history + Wave 실행 결과
- Backlog 색인 → v2.0.3 archived
- Next session entry patterns 갱신

- [ ] **Step 2: 확인 (HANDOFF gitignored)**

```bash
git status HANDOFF.md 2>&1 | head -3
```

Expected: "Your branch is ahead" + HANDOFF.md not listed (gitignored)

- [ ] **Step 3: No commit** — HANDOFF 는 gitignored. local 보관만.

---

## Self-Review

**1. Spec coverage:**

| Design 결정 | 매핑 |
|---|---|
| 1. Scope 8 skill | T1 + T2 |
| 2. Socratic 자유응답 trigger | T1/T2 boilerplate 본문 "Socratic 자유 응답" bullet |
| 3. multi-Q batching | T1/T2 boilerplate 본문 "multi-question batching" bullet |
| 4. Boilerplate 위치 + 본문 | T1/T2 Step 2 |
| 5. CLAUDE.md 결합 메모 | T3 |
| 6. 검증 전략 | T4 |
| 6 manifest bump | T5 |

모든 design 결정 → task 매핑 ✅

**2. Placeholder scan:** TBD / TODO / "implement later" — 없음 ✅

**3. Type consistency:** "사용자 질문 룰 (v2.0.3+)" 헤더 문자열 + "AskUserQuestion" 식별자 — 6 task 본문 전반 일관 ✅

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-05-16-v2.0.3-ask-user-question-mandatory.md`. Two execution options:**

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using og-executing-plans, batch execution with checkpoints

**Which approach?**
